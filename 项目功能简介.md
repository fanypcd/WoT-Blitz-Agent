# WoTB Blitz Tactics Agent — 项目功能简介

> 一个面向《坦克世界：闪击战》(World of Tanks Blitz) 的本地智能分析助手。
> Rust 解析回放与游戏数据，LLM Agent 通过 10 个工具自主完成战绩查询、
> 回放复盘、装甲穿透分析与 3D 可视化。

---

## 它能做什么

### 1. 读懂回放文件（部分功能处于实验性阶段）

回放 `.wotbreplay` 是加密压缩的二进制黑盒。本项目将其解码为结构化数据：

- **玩家战绩**——双方全部玩家的伤害、击杀、生存、命中率等完整数据
- **战斗事件时间线**——逐秒还原生命值变化、击杀事件、伤害曲线（实验性）
- **射击事件**——自动推断每一发炮弹：谁开的火、打中了谁、造成多少伤害、是否击杀（实验性）

### 2. 分析你的表现

- **回放 vs 历史对比**——近期 N 场的实际表现与 WG API 累计战绩量化对比，
  回答"我最近是超常发挥还是正常水平"
- **数据快照**——定期采集 API 数据，追踪任意时间段的能力变化趋势
- **对局前瞻**——开打前批量查询双方阵容战绩，指出威胁最大的对手和可以针对的薄弱点

### 3. 拆解装甲机制

- **3D 装甲查看器**——加载坦克的视觉模型与碰撞模型，车体/炮塔/炮管全部可交互；
  点击任意装甲板即弹出道具面板：该板厚度、实际入射角、有效厚度、是否跳弹
- **穿透热力图**——GPU 实时着色，从任意视角看整车哪里的击穿概率高（绿）哪里低（红），
  间隙甲、履带、炮管的阻挡都计入渲染
- **击穿判定内核**——完整还原游戏规则：跳弹角（AP/APCR 70°，HEAT/HE 永不跳弹）、
  转正效应、3×口径 overmatch、多层间隙逐层消耗、HE 溅射半径、
  装备修正（穿深校准/装甲强化），距离单位统一为米

### 4. LLM Agent 自然语言驱动

对话即可完成所有操作。Agent 理解意图后自主调用工具、串联数据、生成结论：

```
> 查询Anonyme的排位战绩              → search_player + get_player_stats
> 查看E100的装甲模型                 → view_tank（自动打开 3D 查看器）
> 渲染E100正面的穿透热力图            → render_heatmap（保存 PNG 截图）
> 模拟IS-7用AP弹打E100正面        → simulate_penetration（逐层判定 + 伤害）
```

Agent 工具共 11 个：战绩查询/回放扫描/单场解析/对比分析/3D 查看器/
装甲明细/击穿模拟/热力图截图/射击复现截图等。支持中途打断、Token 预算控制、
历史保存与恢复。

### 5. 射击复现（实验性）

从回放二进制中解析射击事件
然后在 3D 查看器中还原每次射击：

- 相机放在射手的真实相对方位与俯仰处（考虑地形高度差）
- 受击坦克按回放时刻的炮塔朝向、炮管俯仰呈现
- 沿真实弹道方向自动执行穿透判定，模型上标注弹着点

---

## 界面一览

**Web GUI**（`wotb-agent web`，六个标签页）：

| 标签页 | 内容 |
|--------|------|
| Agent | 自然语言对话（流式进度、工具调用轨迹、打断、历史保存） |
| Tankopedia | 723 辆坦克图鉴：模糊搜索 + 筛选 + 逐车装甲/弹种详情 |
| Player | WG API 战绩查询 |
| Replay | 回放批量扫描报告 + 射击复现 |
| Compare | 回放 vs API 对比 + 对局前瞻 |
| Settings | 模型/密钥/预算在线配置 + Token 用量统计 |

**3D 查看器**（自动打开或 `wotb-agent view <坦克名>`）：
拖拽旋转、滚轮缩放、左键点击判定、右键调炮塔/炮管、
弹种切换、装备开关、热力图与碰撞模式切换。

---

## 技术栈

- **Rust**：回放/游戏文件解析、击穿判定内核、Agent Loop、Web 服务器（axum）
- **Three.js**：3D 渲染（热力图管线对齐 BlitzKit 双 RenderTarget 架构）
- **数据源**：BlitzKit `tanks.pb` / `models.pb` / GLB 模型（723 辆坦克，全部离线可用）
- **LLM**：OpenAI 兼容 API（可切换清华 AI 平台等任意兼容端点）

全部数据随仓库分发，克隆即用，无需安装游戏。
