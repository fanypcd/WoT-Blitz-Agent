# 项目结构说明

WoTB Blitz Agent —— 坦克世界闪击战回放解析与 AI 复盘工具。Rust 编写，提供 CLI 与 Web GUI 两种交互方式，核心能力：回放解析（射击事件/伤害归属/弹道复现）、装甲穿透模拟、3D 可视化、WG API 统计查询、LLM 对话教练。

## 顶层结构

```
Project/
├── Cargo.toml
├── src/                    源码（5 个模块，见下文）
├── data/                   运行时数据缓存（git 忽略部分）
│   ├── tank_cache.json     坦克百科缓存（id → 名称/等级/装甲/弹种/hp…，723 车）
│   ├── armor_cache.json    逐装甲板明细（厚度/icosahedral 分区）
│   ├── gun_angles.json     火炮仰俯角极限
│   ├── models.pb           游戏车辆 YAML + 模型原点数据（BlitzKit 快照）
│   ├── tanks.pb            坦克列表
│   └── game_data/          每车提取的装甲模型 JSON（451 车，extract-game 从游戏客户端提取）
├── glb_cache/              每车 glb 模型缓存（model.glb 视觉 + collision.glb 装甲）
├── replay_samples/         测试回放样本
├── screenshots/            无头截图输出
└── *.md                    文档（README / 设计文档 / 逆向分析 / 项目简介）
```

## src/ 模块结构

```
src/
├── main.rs         CLI 入口（clap）+ 全部子命令分发
├── data.rs         数据目录路径（data/ 相对路径）
├── agent/          LLM 对话教练
├── models/         配置与报表数据结构
├── replay/         回放解析（包头/事件流/射击事件）
├── wargaming/      游戏数据提取、装甲穿透、3D 查看器、WG API
└── web/            Web GUI 服务端
```

## src/main.rs（1132 行）—— CLI 入口

clap 子命令分发，共 19 个命令：

| 命令 | 功能 |
|---|---|
| `single` | 解析单个回放文件（战绩汇总） |
| `scan` | 扫描目录聚合统计（近 N 天/全部） |
| `fetch-tanks` | 从本地 BlitzKit 数据构建 tank_cache.json |
| `prematch` | 开场阵容分析（WG API 查询双方玩家） |
| `snapshot` | WG API 战绩快照（take/list/diff） |
| `parse-game` | 从游戏目录解析 models.pb（dev_name 指定车辆） |
| `extract-game` | 从游戏目录提取全部车辆的装甲/碰撞数据（当前 451 车有本地数据源） |
| `fetch-blitzkit` | 拉取 BlitzKit 快照（models.pb/tanks.pb） |
| `fetch-icons` | 拉取坦克图标 |
| `view` | 启动 3D 查看器（独立端口，单坦克） |
| `web` | 启动 Web GUI（设置/对话/统计/回放/对比） |
| `chat` | CLI 交互式 LLM 对话 |
| `config` | 查看/设置 config.toml |
| `usage` | Token 用量统计 |
| `player` | 按昵称查询玩家战绩 |
| `compare` | WG API 累计战绩 vs 回放聚合对比 |
| `combat` | 解析单场事件流 + 推断射击 + 导出射击复现 JSON（`--dump` 原始包分析） |
| `dump-methods` | 逆向工具：导出 method38/method8/type=32 原始包字节（wotinspector 对齐） |
| `dump-entity` | 逆向工具：导出某实体时间窗内全部数据包（支持 payload 十六进制模式搜索） |

## src/agent/ —— LLM 对话教练

| 文件 | 行数 | 功能 |
|---|---|---|
| `mod.rs` | 319 | Agent 循环：接收用户消息 → 组装上下文（系统提示 + 工具结果）→ 调用 LLM → 解析工具调用 → 执行 → 回传；全局中断信号（Ctrl-C 置位 is_interrupted）；多会话管理（SavedSession 保存/加载/切换）；`chat_async` 流式回调（进度事件推送到 Web 前端） |
| `llm_client.rs` | 183 | OpenAI 兼容 HTTP 客户端（chat completions + tool calls），预算检查（check_budget 超额停止） |
| `tools.rs` | 1222 | Agent 工具执行器：get_tank_armor / render_heatmap / get_player_stats / parse_replay / replay_shot 等工具的参数解析与分发；replay_shot_smoke 等集成测试 |

## src/models/ —— 数据结构

| 文件 | 行数 | 功能 |
|---|---|---|
| `battle.rs` | 158 | 单场战斗数据结构（BattleSummary / AuthorStats / PlayerSummary：地图/模式/胜负/逐玩家战绩） |
| `config.rs` | 244 | config.toml 读写（llm/wg_api/replay 三节）+ TokenUsage 统计 |
| `report.rs` | 403 | 战斗汇总与聚合报表（AggregatedReport，print_summary 终端输出） |
| `mod.rs` | 8 | 模块声明 |

## src/replay/ —— 回放解析

| 文件 | 行数 | 功能 |
|---|---|---|
| `parser.rs` | 211 | 单文件解析：meta.json + battle_results.dat → BattleSummary（队伍/车辆/伤害/击杀） |
| `scanner.rs` | 166 | 目录扫描 + 过滤（近 N 天/随机战过滤）+ 聚合 |
| `combat.rs` | 1130 | **射击事件核心**（详见下文） |

### combat.rs 射击事件确定性解析

数据链全部基于 WotbTools PROVEN 协议（11.19 corpus），fail-fast 无保守降级：

```
method29 (0x1d) 弹丸发射   shotId + 炮口坐标 + 发射速度向量
    ↓ shotId 精确配对
method20 (0x14) 弹道终点   shotId + endPoint
    ↓ 时钟窗口 ±0.5s（歧义报错）
method38 (0x26) 命中结果   victimVehicleId + resultFlags16
    ↓ flags & (击穿|HE)
method1  (0x01) 血量/来源/原因   currentHp + sourceEntity + causeFlag
```

伤害归属（WotbTools deriveLosses 同款）：受害者血量样本（同钟折叠）的相邻降幅区间，区间末事件 source=作者 ∧ cause=0（撞击/火伤被 cause 天然排除）；命中时刻落入哪个区间，伤害即该区间差值。首击无前值 → 计数器增量（type7 sub10）兜底（撞击/火伤污染由 method1 cause≠0 同批剔除后顺序配对）。

附带提取：弹药槽位（type=28）、发射/命中时刻的炮塔角（type7 prop2）、炮线高（models.pb 原点链）、作者/目标 type10 状态快照（命中时刻）。

### combat.rs 命中结果位解码（与 wotinspector 对齐）

- **命中结果位图**（method38 resultFlags16）：0x0001 直接击杀 / 0x0008 跳弹 / 0x0010 材料击穿 / 0x0020 未击穿（材料止）/ 0x0040 间隙层被穿透 / 0x0080 间隙层未穿 / 0x0100 内部模块被击穿 / 0x0400 履带受损 / 0x0800 火炮受损 / 0x1000 HE 爆炸伤害分支
- **模块受损/摧毁位掩码**：bit = componentToken − 31（token 31..43 → bit 0..12，与 WI crit_modules 同源）
- **游戏原生命中段 u64**（type=32 警告包尾 8 字节）：`[result u8][shell_global_id u24 LE][0x00][X][Y][Z]`；shell_global_id = (shells.xml 局部 id << 8) | 国家基数字节（nation_id×16+10），与 WI shell_id 同值同源
- **game_hit_result 枚举**（method8 b9 / type=32 segment 低字节同源）：0=无命中 1=未击穿 2=间隙层止 3=有伤害（击穿/HE）4=跳弹
- **type10 多 tick 采样**：受击/射手双方命中前后位姿样本（相对锚点偏移 + 绝对坐标），供 3D 查看器渲染幽灵帧与 tick 延迟验证

## src/wargaming/ —— 游戏数据与可视化

| 文件 | 行数 | 功能 |
|---|---|---|
| `viewer.rs` | 3582 | **3D 查看器**（Three.js 内嵌 HTML）：模型加载（glb 原点=场景原点，原生米制）、装甲热力图、点击穿透判定、射击复现（相机=真实炮口、弹道射线、弹着点标记、轨迹管）、装甲穿透交互 |
| `penetration.rs` | 481 | 穿透判定内核：等效厚度/转正角/跳弹角/Ludendorff 间隙衰减/HE 溅射——`/api/penetration_check` 后端 |
| `blitzkit.rs` | 1075 | BlitzKit 数据（models.pb/tanks.pb）解析：车辆 YAML → 碰撞包围盒/装甲厚度/模型原点链/炮塔旋转初始值 |
| `dvpl.rs` | 483 | Blitz DVPL 容器解压（zlib）+ 车辆 YAML 的 collision 解析（hull/turret/gun 包围盒与 points） |
| `game_extract.rs` | 234 | 从游戏目录提取每车数据：装甲模型 JSON + collision + glb 下载到 glb_cache/ |
| `tank_resolver.rs` | 278 | 坦克 ID → 名称/等级/装甲/弹种/hp（tank_cache.json 加载与查询） |
| `api_client.rs` | 181 | WG API 客户端（玩家搜索/战绩查询） |
| `prematch.rs` | 167 | 开场阵容分析（双方玩家统计汇总） |
| `snapshot.rs` | 241 | WG API 战绩快照存储与差分 |
| `heatmap_ready.rs` | 53 | 热力图会话就绪标记（跨进程信号） |
| `mod.rs` | 15 | 全局 resolver + 模块声明 |

### game_extract.rs 车辆文件匹配（防错车装甲）

tanks.pb dev_name（如 "xm66f"）与游戏文件名（"A153_XM66F.xml.dvpl"）不一致，`resolve_vehicle_file` 在民族目录内按归一化名三级匹配：
**精确 > 正向包含**（文件名 ⊇ dev_name，最短优先）**> 反向词缀**（dev_name 以文件名开头/结尾，最长优先）。反向匹配锚定词缀边界——禁止纯内嵌子串（曾导致 "xm66f" 被 "M6" 抢走匹配、装上 M6 的 20mm 炮管装甲；已修复并全量重提取，30 车炮管/炮盾数据纠正）。

### viewer.rs 射击复现定位体系

- **模型原点 = 场景原点**：glb 原点即引擎车体锚点（= 回放 type10 位置锚点），数据零偏移直通；场景原生米制（1 单位 = 1 米，模型不缩放）
- **坐标系变换**：`toModel`（水平旋转 −hullYaw + z 取反，viewer 北=−Z）→ `qHullInv`（车体 pitch/roll 姿态逆变换）——相机、射线、弹着点标记三者共用
- **相机 = 真实炮口**（method29 launchPoint，不压缩；OrbitControls 上限动态抬高）
- **命中点 = 弹道射线 ∩ 装甲模型**（刚体等价：平放模型 + 车体系射线 ≡ 真实俯仰车体 + 世界系）
- **弹着点标记** = method20 弹道终点 − 命中时刻目标位置（车体系），黄色透视标记
- **炮管俯仰** = 反向弹道的车体系仰角（受击坦克瞄准射手）
- **debug=1**：标注原点三轴 + 视觉顶点质心 + 碰撞包围盒中心

### viewer.rs world=1 双模型世界坐标模式（`?world=1`，射击复现专用）

不做 toModel/qHullInv 相对转换，射手/目标两模型用 type10 世界坐标直接摆放：

- **目标模型**：命中时刻 pos + yaw + pitch + roll 直通（type10 原始欧拉 → 场景四元数 `Q = Ry(−yaw)·Axis(右轴,−pitch)·Axis(前轴,−roll)·Rx(−π/2)`）
- **目标炮塔/炮管**：炮塔 = type7 prop2 绝对朝向 − 车体偏航；炮管俯仰 = launch_velocity 反向弹道车体系反解（prop9 与真实弹道不相关，实测弃用）
- **射手模型**：并行加载，开火时刻 type10 位姿直通；炮塔/炮管枢轴 = models.pb 原点链（与目标模型同语义）
- **多 tick 幽灵帧**：目标 ghost = tick_samples（锚点相对偏移），射手 ghost = shooter_tick_samples（绝对坐标）；信息面板 Ghost ticks 开关
- **tick 下拉**：目标模型移到所选 tick 位姿并重算弹着点（测试 tick 延迟对命中位置的影响）
- **弹道标注（世界系）**：① launchPoint 橙色标记 + 速度标签 ② launch_velocity 青色虚线轨迹 ③ method20 终点黄色标记（击穿弹终点在车体内部 ≠ 弹着点）④ 服务器弹着点 = 服务器弹道射线 ∩ 目标装甲模型（首个非 deco 命中）

## src/web/ —— Web GUI 服务端

| 文件 | 行数 | 功能 |
|---|---|---|
| `mod.rs` | 769 | axum 服务端：/api/chat（LLM 对话，会话缓存 Agent）、/api/sessions + /api/session/{id}（多会话列出/删除/Markdown 导出）、/api/config（读写 config.toml）、/api/scan、/api/replay/shots（射击事件 JSON）、/api/penetration_check、/api/tank/{id}（装甲/弹种/模型 URL）、/armor_view/*（3D 查看器托管）、静态页托管 |
| `index.html` | 860 | Web GUI 前端：Chat / Tanks / Scan / Replay / Compare / Settings 六标签页；会话栏（会话下拉/新建/删除/导出 Markdown）、进度缓冲面板 + 中断按钮；射击回放卡片（逐发列表 + KILL 徽标，点击行 → 可编辑 3D 查看器 URL，`world=1` 双模型模式 + `shooter`/`shell` 槽位透传，脱靶弹不提供 3D）；设置页含重启提示 |
| `tank_detail.html` | 201 | 坦克详情页（百科联动） |

## 数据流概览

```
.wotbreplay (ZIP)
  ├─ meta.json ──────────→ parser.rs → BattleSummary
  ├─ battle_results.dat ─→ parser.rs（结算交叉验证）
  └─ data.wotreplay ─────→ combat.rs → CombatTimeline
                              ├─ infer_shots（伤害计数器推断）
                              └─ extract_shot_replays（确定性射击复现数据：
                                  method29/20/38 + type32 命中位 + type10 tick 采样）
                                    ↓ JSON
                            web/index.html（射击列表）
                                    ↓ shot=N&shell=槽位&world=1&shooter=射手
                            viewer.rs 3D 查看器（world=1 双模型弹道复现 + 幽灵帧 + 穿透判定）
                                    ↓ /api/penetration_check
                            penetration.rs（米制穿透内核）
```

## 外部数据依赖

| 数据 | 来源 | 用途 |
|---|---|---|
| models.pb / tanks.pb | BlitzKit 快照（fetch-blitzkit） | 车辆 YAML（碰撞/原点/装甲）、坦克列表 |
| tank_cache.json | fetch-tanks（从 BlitzKit 数据构建） | 坦克 ID → 名称/装甲/弹种/hp |
| armor_cache.json | BlitzKit | 逐装甲板厚度/分区 |
| glb_cache/{id}/ | 游戏目录提取（extract-game）或 BlitzKit | 3D 模型（视觉 + 装甲） |
| game_data/{id}.json | extract-game（游戏客户端 XML/YAML） | 装甲模型 JSON（逐板厚度/spaced）+ collision；451 车有本地游戏文件可提取，其余无数据源 |

## 测试

- `cargo test`（12 项）：解析冒烟（GB109/XM551 回放）、热力图渲染、装甲查询、模糊搜索、射击复现截图等
- 回放回归基准：GB109（11 发）/ T110（8 发）/ GB84（13 发）/ XM551（6 发）/ J39（12 发），伤害序列逐发比对
