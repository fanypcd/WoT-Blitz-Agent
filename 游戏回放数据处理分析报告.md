# WoT Blitz 游戏客户端回放数据处理分析报告

> 分析对象：Steam PC 版 wotblitz.exe（32 位 PE，2026-09-02 build；镜像基址 0x400000）
> + 游戏资源 `Data/`（DVPL 解码）。
> 方法：RTTI（TypeDescriptor → COL → vtable）定位类与虚表；断言/日志字符串中的
> `类::方法` 限定名还原成员函数面；512 条 `C:/ba/tc/work/t/client/Classes/...` 源码
> 路径还原工程结构；objdump 定向反汇编；DVPL 配置解码。
> 证据分级：**[实证]** = 二进制符号/反汇编直接证据；**[对照]** = 与 BigWorld OSS 14.4.1
> 源码同构对照；**[推断]** = 命名与行为合理性推断，待验证。
> 关联文档：《回放射击事件逆向分析.md》（协议细节与射击复现，本报告 §4.2/§5 引用其 §编号）。

---

## 一、总览：回放的本质是"网络包流重放"

Blitz 客户端是 BigWorld 网络层 + DAVA 渲染引擎的混合体（源码路径双命名空间印证：
`Classes/BW/*` 与 `Classes/Battle|Replay|Lobby/*`）。回放文件的数据段就是录像者客户端
当时收到的 **BaseApp 包流**（bundle 消息），回放播放不重新模拟任何游戏逻辑，而是把
录制包按录制时钟原样回灌进与实战完全相同的处理管线 [实证]：

```
回放文件(.wotbreplay)
  └─ ReplayFile::readBytes/readData            [实证] 包读取
  └─ ReplayPlayer::StartPlayback               [实证] 播放控制（速度：replayPlaybackSpeedChanged / replayTimeWarp）
      └─ ReplayMessageHandler                  [实证] 15 个 on* 回调（见 §3.1）
          ├─ onBasePlayerCreate / onCellPlayerCreate / onEntitiesReset / onEntityControl
          ├─ onEntityCreate / onEntityLeave / onRestoreClient / onSwitchBaseApp
          ├─ onEntityMethod  ──→ 按 (实体类, methodID) 分发，如 AvatarGameLogic::showTracer
          ├─ onEntityProperty / onNestedEntityProperty ──→ 按 (实体类, propertyID) 分发 set_*
          └─ spaceData / spaceGone
  └─ 与实战共享的下游：
      ├─ volatile(type=10) → 实体滤波器（WGVehicleFilter2，逆向分析文档 §七）→ 每帧 output()
      ├─ VehicleFilterSyncSystem / DetachedTurretFilterSyncSystem（DAVA ECS）写渲染 transform
      └─ 呈现系统（TracerManager / GunRecoilSystem / MinimapObjectsController / UI...）
```

**结论 [实证]**：回放里的每一种数据（volatile/属性/方法/竞技场状态）在游戏中都有唯一
的处理入口，且回放与实战走同一入口。因此"游戏如何处理回放数据" = "客户端如何处理
服务器数据"，本报告即按数据类别给出该映射。

---

## 二、回放系统自身（Classes/Replay/）

| 符号 | 语义 [实证] |
|---|---|
| `ReplayFile` / `NewWritableReplayFile` | 文件读写（readBytes/readData/fileWriterThreadMessage；GenerateReplayFilename/Meta/PrepareDirectory） |
| `ReplayManager` | 回放库管理（AddBattleResults/CopyReplayToDirectory） |
| `ReplayPlayer` / `ReplayRecorder` | 播放 / 录制（StartPlayback / StartRecording / StopRecording） |
| `ReplayMessageHandler` | 包流 → 客户端管线回调（15 个 on*，§3.1） |
| `ReplayIgnoresHandler::InitIgnoresForMap` | 按地图初始化忽略清单；对应 `Data/devReplaysIgnores.yaml.dvpl`：仅含 `idle/grossberg` 两图的 `ignored_destructables`（可破坏物实体 ID 黑名单，dev 回放用） |
| `AvatarContextReplay` / `AvatarControllerReplay` | 回放上下文（与 `AvatarContextBattle` 同继承 `AvatarContextBase`；滤波器工厂在其虚表 §4.1，实战/回放同源） |
| `ExternalReplayManager` | 外部回放入口（ShowErrorDialog/ShowPlayingReplayDialog） |
| `ReplayCameraController` / `replayTimeWarp` | 观战相机与时间缩放 |

---

## 三、实体类型体系与方法/属性命名空间

### 3.1 实体类（10 类，`X::onMethod` [实证]）

`Account`、`AreaDestructibles`、`Avatar`、`DetachedTurret`、`Flock`、`FlockExotic`、
`KineticObject`、`Login`、`OfflineEntity`、`Vehicle`。

回放战斗段中活跃的实体类 [实证+推断]：

| 实体类 | 职责 | 对回放数据的意义 |
|---|---|---|
| **Avatar** | 录像者本人的"玩家视角"实体 | Avatar 专属方法（showTracer 等 32 个，§4.2）只对作者可观测——与本项目"作者/他人双路径"数据不对称的根因一致 |
| **Vehicle** | 每辆坦克 | set_health/set_hitMarks 等 23 方法 + 10 属性（§4.3） |
| **DetachedTurret** | 被击飞的炮塔（物理实体） | 解释部分未知 entity 的 type=10 流；炮塔击飞动画可复现 |
| **KineticObject** | 运动物体（`KineticObjectSystem`/`KineticObjectEntityComponent`） | 同上；`KineticsFilter`（§七）的服务对象候选 |
| **AreaDestructibles** | 区域可破坏物状态 | 属性：destroyedFragiles/destroyedModules/fallenColumns/fallenTrees——场景破坏状态回放 |
| Flock/FlockExotic | 环境鸟群 | 环境实体，与战斗无关 |

**方法/属性 ID 是按实体类分命名空间的**（本项目文档 §三 旧结论"Vehicle/Avatar 两套"
扩展为"10 类各一套"）；ID ↔ 名字的对应表编译为数字结构，方法名只以
`类::方法` 限定形式存在于断言/日志字符串 [实证：独立裸名字符串检索为零]。

### 3.2 protobuf 消费者（10 个 `::initFromStream` [实证]）

| 类 | 消费位置 | 与本项目数据的关系 |
|---|---|---|
| `PrivateShotResult` | method38 args | 已解码（resultFlags32 + 组件状态表 + modifiers，逆向文档 §三） |
| `HitMarkPlacementInfo` | Vehicle 属性 `hitMarks` | **未解码**——命中贴花放置描述符，与 method8 尾部 (b0,b1,b2) 指纹的交叉验证入口（§六） |
| `ModuleDamageInfo` | 受损模块信息 | 与 method38 组件 token 族同域 |
| `PublicVehicleInfo` | Vehicle 属性 `publicInfo` | 未解码（公开状态：血量档位/存活等） |
| `PublicArenaInfo` | 竞技场公共信息 | 未解码 |
| `EquipmentStateInfo` | 装备状态 | 未解码 |
| `RespawnAbilityStatus` / `AccountRenameInfo` / `ServerStatistics` / `ChatActionData` | 复活能力/改名/服务器统计/聊天 | 未解码 |

**WG protobuf-lite 细节 [实证，反汇编 `HitMarkPlacementInfo` 解析器]**：字段号分派经
描述符表（12 字节/项向量）；**字段号 0xFF 转义后接 u24 LE 扩展字段号**（标准 protobuf
字段号上限的 WG 私有扩展）。

---

## 四、逐类数据处理映射（核心）

### 4.1 volatile 移动流（type=10）

- 处理入口：`BWEntity::pFilter` → **WGVehicleFilter2**（接口槽与 KineticsFilter 共享，
  核心 = OSS AvatarFilterHelper，`NUM_STORED_INPUTS=8`）；工厂 =
  `AvatarContextBase` 虚表 slot66/68（实战/回放同源）；
- 每帧 `VehicleFilterSyncSystem` 把滤波输出写车辆 transform；`DetachedTurretFilterSyncSystem`
  同步击飞炮塔；
- 已完整移植并落地为渲染层锚点（详见逆向分析文档（按数据段重构版）"渲染层"注记与备份存档 §七（backup_pre_wi_align），裁判实验 4/4 通过）。

### 4.2 方法流（type=8）——Avatar 实体方法全表（32 个 [实证]）

作者 Avatar 专属 = 本项目"作者路径"可观测数据的完整方法面：

| 方法 | 语义 | 本项目对应 |
|---|---|---|
| `showTracer` / `stopTracer` | 弹道显示/终止 | method29 发射流 ✓ |
| `explodeProjectile` | 弹道终点/爆炸 | method20 ✓、0x1b 地形命中 ✓ |
| `showShotResults` | 命中结果反馈 | method38 ✓（PrivateShotResult） |
| `showSplash` | HE 溅射 | 识别为 0x1000 分支 ✓ |
| `showOwnVehicleHitDirection` | 被击方向指示 | type=32 命中通知 ✓ |
| `updateVehicleAmmo` | 弹药状态 | type=28 弹药槽 ✓ |
| `updateVehicleGunReloadTime` | 装填计时 | method0x0d/0x23 部分解码 ◐ |
| `updateVehicleHealth` | 全场车辆 HP 广播 | method1 血量链 ✓（与 Vehicle::set_health 的关系见 §4.3 注） |
| `updateVehicleMiscStatus` / `updateVehicleOptionalDeviceStatus` / `updateVehicleSetting` | 车辆杂项/设备/设置状态 | 未逐项解码 ◐ |
| `updateArena` | 竞技场状态总入口 | **已解码**（method 48，见 §4.5） |
| `updateTargetingInfo` / `updateGunMarker` / `GetOwnVehicleShotDispersionAngle` | 瞄准/扩散 | method36 瞄准快照 ✓（部分字段未定） |
| `moveATGM` / `stopATGM` | ATGM 制导（`AtgmAimingSystem`） | 已知边界：制导弹非直线 ✓ |
| `set_playerVehicleID` / `set_isGunLocked` | 作者车辆绑定 / 炮管锁定 | 元数据 |
| `showVehicleDamageInfo` | 车辆受创信息广播 | 未解码 ◐ |
| `showLightning` | 雷击特效（特殊模式） | 未观测 |
| `receiveFactorsAndBoosters` | 增益因子 | 未解码 |
| `receiveNotification` / `onQuestCompleted` / `onChatAction` | 通知/任务/聊天 | 未解析（**回放含聊天流**） |
| `onBecomePlayer` / `onBecomeNonPlayer` / `onLeaveWorld` / `onAutoAimVehicleLost` | 生命周期 | 元数据 |

### 4.3 方法流（type=8）——Vehicle 实体方法全表（23 个 [实证]）+ 属性（10 个）

方法（`set_*` 即属性写入回调，type=7 属性流最终归宿）：

| 方法 | 语义 | 本项目对应 |
|---|---|---|
| `set_health` / `set_maxHealth` / `set_isAlive` / `set_healingHealth` | HP 族 | method1 血量链 ✓（伤害归属/击杀判定） |
| `set_hitMarks` | **命中贴花放置**（payload = HitMarkPlacementInfo protobuf） | **未解码**——method8 尾部 (b0,b1,b2) 指纹的交叉验证入口 |
| `set_gunAnglesPacked` | 炮塔/炮管角度打包写入 | 与 prop2（u16 炮塔朝向）关系待核 [推断：prop2 即此属性或其相邻项] |
| `set_criticalDevices` / `set_destroyedDevices` | 模块受损/被毁 | method38 组件 token 的最终归宿 ✓（crit/destroyed_modules 位图） |
| `set_activeEquipments` / `set_publicStateModifiers` / `set_engineMode` / `set_isStrafing` / `set_healingHealth` / `set_debugStrings` | 装备/公开状态修正/引擎/很态 | 部分对应 type=7 sub4 状态二元组 ◐ |
| `showDamageFromShot` / `showDamageFromExplosion` / `showAmmoBayExplosion` | 受创表现（受击者侧） | 结果呈现（game_hit_result 枚举 ✓） |
| `showShooting` | 开火表现（他人开火） | method0x00 开火事件 ✓（AoI 覆盖差已量化） |
| `showRammingEffect` | 撞击 | 未解析 |
| `onHealthChanged` / `onStaticCollision` / `onEnterWorld` / `onLeaveWorld` / `ConnectToModel` | 生命周期/物理回调 | 元数据 |

属性名清单（type=7 属性流的语义命名空间 [实证]）：
`activeEquipments`、`armorsStates`（**装甲受击状态**，未解析）、`clientEffects`、
`criticalDevices`、`debugStrings`、`destroyedDevices`、`hitMarks`、`optDevices`、
`publicInfo`（PublicVehicleInfo）、`publicStateModifiers`。

**属性载荷探针实测**（J39，9 个 propID）[实证]：prop2 = **2 字节纯偏航 u16**
（20843 条/14 实体，无俯仰位——`set_gunAnglesPacked` 假设否定，远端车辆属性流
不含炮管俯仰，与逆向分析文档 §4.1"俯仰不广播"结论一致）；prop3 = HP u16（81 条）；
prop4 = 状态二元组 u16（4075 条，即 type7 sub4）；prop7/8 = 稀疏 u16（每实体 ≤1 条，
含 0x21/0x23 值，未定名）；prop9 = 作者 avatar 专属 f32 炮管俯仰（2190 条）；
prop10 = 伤害计数器 u32（即 sub10）；prop11/12 = 作者专属稀疏 u32（未定名）。
（探针：`examples/prop_probe.rs`、`examples/method_probe.rs`）

**type=5 车辆全量状态包 = 满血锚点 [2026-09 实证]**：包头 `[eid u32][02 00][dc 0f 00 00]
[00 00 00 00]` 后跟位置浮点与属性块，**偏移 51 的 u16 = 当前血量**（包内第二处出现
~偏移 190 随载荷长度漂移，取固定偏移 51）。20 实体 × GB109 全数验证：首条 type=5
恒早于该实体首次掉血，且 (seed − 首条 method1) 精确等于首刀伤害（KingScopion
1650−1279=371 ✓、CARRY5001 1200−885=315 ✓、Th3De4th 链 1838→…→0 全闭合 ✓）。
用途：**血量链 seed**——受害者首个 method1 已是掉血后血量时（受击前无任何 method1），
无锚点则首刀降幅不可推导（CLI 掉血表缺首行 + `dmg_unattributed` 误报）。
已落地：`combat.rs collect_initial_hp()` + `derive_dmg_losses(initial_hp)` 前插 seed
（时钟回退 1ms 避免同钟冲突去重）；type=7 prop3 首事件伤害同享此锚点。
（探针：`examples/create_probe.rs`）

> 注：本项目 prop2/prop9 等数字 ID ↔ 上述属性名的对应表需按实体类 def 结构序还原
> （数字表在二进制中无名字）；`armorsStates`/`hitMarks`/`publicInfo` 三个属性建议优先
> 攻克（§六）。

### 4.4 其它实体的流

- `AreaDestructibles`：属性 `destroyedFragiles` / `destroyedModules` / `fallenColumns` /
  `fallenTrees`——场景可破坏物状态；配合 `DestructiblesCache` / `AreaDestructibles::onMethod`
  与 `devReplaysIgnores.yaml`（按图忽略的可破坏物 ID）可做场景破坏重建；
- `DetachedTurret` / `KineticObject`：独立 type=10 移动流（各自挂滤波器；
  `KineticsFilter` helper 的 200.0f 钳位字段见逆向分析文档 §7.5）；
- `Account`：`AccountRenameInfo`、`ServerStatistics`、`personalArtifacts`、`respawnAbilities`。

### 4.5 竞技场状态：`UpdateArena` 子类型 ID 表与解码 [实证]

`AvatarGameLogic::updateArena` = Avatar 实体 **method 48 (0x30)**（方法流直方图 + args[0]
分布双重实证；全部分布在作者 Avatar 实体上）。**args 布局 = `[subtype u8][len u8][protobuf]`**
（len = 其后字节数，逐条吻合）。子类型 ID → 名字表位于 `0x40391D8`（分派函数
@`0x1636C90`：`payload[0]−1` 查表跳转，越界 → UNKNOWN_TYPE）：

| ID | 子类型 | ID | 子类型 |
|----|--------|----|--------|
| 1 | VEHICLE_LIST | 14 | PLAYER_NAME |
| 2 | VEHICLE_ADDED | 15 | RELOAD_TIME |
| 3 | PERIOD | 16 | OBSERVED_STATUS |
| 4 | STATISTICS | 17 | RELOAD_TIME_LIST |
| 5 | VEHICLE_STATISTICS | 18 | GAME_MODE_DATA |
| 6 | VEHICLE_KILLED | 19 | VEHICLE_WAIT_RESPAWN |
| 7 | AVATAR_READY | 20 | VEHICLE_RESURRECT |
| 8 | BASE_POINTS | 21 | TEAM_RESPAWNS_LEFT |
| 9 | BASE_CAPTURED | 22 | VAMPIRIC_CURSE |
| 10 | former_teamkills（原表小写） | 23 | BATTLE_HINTS_INFO |
| 11 | VEHICLE_UPDATED | 24 | BOSSMODE_INFO |
| 12 | STRATEGIC_POINT_STATUS | 25 | TOTAL_GAME_MODE_INFO |
| 13 | WIN_POINTS | 26 | TIER_EQUALIZER_DATA / 27=UNKNOWN_TYPE |

实测分布（J39，307 条）：RELOAD_TIME×227、OBSERVED_STATUS×23、BASE_POINTS×16、
AVATAR_READY×14、VEHICLE_STATISTICS/VEHICLE_KILLED×9、PERIOD×3、其余各 1。

已解码子类型：

- **PERIOD (3)**：protobuf field3 嵌套 `{field1 varint period, field2 fixed64 剩余秒, field3 varint 阶段时长}`；
  实测 1=准备(60s) → 2=倒计时(7s) → 3=战斗(420s)，剩余秒与包时刻互洽
  （t=0.17 时准备期剩 59.7s）——**战局真实时长与阶段时间线可从回放直接提取**；
- **VEHICLE_KILLED (6)**：`len | 32 <len> {field1/2/3 varint}` = 击杀三元组（受害者/击杀者/
  [第三者]），ID 为战斗局部车辆编号空间（J39 全场集中在 927260–927404 的 14 个值 ≈
  14 辆车；与实体 ID/账号 ID 均不同域，映射需经 VEHICLE_LIST (1) 大包交叉）；
- 其余子类型为 protobuf 原样透传（`collect_arena_updates`），按需逐个解码。

`ClientArena` 消费（`OnVehicleAdded/OnVehicleListUpdated/OnBasePointsUpdated/OnWinPoints/
GetCaptureState/ParseProtobufMessageFromString`；`PublicArenaInfo::initFromStream`）。

### 4.6 战斗结果与统计

`BattleResultsController / BattleResultsCache / BattleResultsExtendedCache /
BattleResultsStructs（FullResultData / VehInteractionDetails）/
BattleResultsViewModelHelper`——战后结算独立于回放包流（ReplayRecorder::AddBattleResults
单独落盘），与回放数据的关联键 = account/vehicle id。

### 4.7 呈现层（消费上述数据的下游系统 [实证]）

**着弹点/受击表现链路（2026-09 二轮逆向补充）**：

- **`VehicleGameLogic::showDamageFromShot`**（@0x16A9110→0x16A9130，另有 `DetachedTurretGameLogic`
  变体）：受击表现入口。wire 参数含一个 **8 字节/元素的 segment 描述符数组**，
  元素 **byte0 = cmpIndex（部件索引，0..4）**，死亡/存活状态分支下 4 会被重映射为 2
  （`cmp esi,4; cmove esi,2`）[实证：反汇编]；
- **`BWUtils::DecodeShotSegment`**（BWUtils.cpp，@0x1334FD0，调用方即 showDamageFromShot
  与 DetachedTurret 变体）：签名近似 `(seg64, ..., bboxes[4], partMatrixes[4])`。
  `cmpIndex = (seg64>>8)&0xFF`，断言 `cmpIndexInt < 4`、`bboxes[cmpIndexInt]`、
  `partMatrixes[cmpIndexInt]`——**用部件包围盒 + 部件矩阵把 segment 位域换算成部件
  局部命中点** [实证：反汇编前段；位域→坐标的完整数学未解完]；
- 贴花承载：`TankHitMarkComponent` + DAVA `DeferredDecalComponent`
  （`~res:/Materials/DeferredDecal.material`），延迟应用链
  `VehicleGameLogic::AddHitMarks`（std::function 延迟 lambda）→
  `HitMarksCacheComponent` / `HitMarksDeferredQueueComponent`（模型未就绪时缓存）；
- **结论**：弹着点一致性的来源 = **服务器下发的 segment 描述符里带有部件归属
  （cmpIndex）**，客户端按服务器指定的部件 + 部件几何放置，而非在渲染位姿上自行
  猜测。本项目复现的残余偏差正是缺这个字段（本地 raycast 可能选错部件）。
- **描述符的线上来源（已确认）**：**method8 本身就是 `showDamageFromShot` 的 wire 调用**。
  args 实际布局 = `[shooter u32][victim u32][元素数 u8=01][元素 8B][尾 4B]`，其中
  元素 8B = `[result u8][cmpIndex u8][hash6 u48]`——项目旧标注的 "extra u8"
  （args[10]）实为 **cmpIndex（受击部件索引）**。**部件对应已实证**（命中离地高度分层，
  3 场 26 发）：**0=底盘/履带（med 0.70m，结果全为跳弹/间隙止）、1=车体（med 1.17m）、
  2=炮塔（med 2.84m）、3=炮管（直射弹未观测）**。J39 100 条分布 {0:22, 1:40, 2:34, 3:4}
  无一越界。**已落地**：`ShotReplayData.server_part_index`（combat.rs `DirectHit8.component_index`）；
  viewer world 模式弹着点 raycast 已按该部件约束（优先与服务器指定部件求交，无交点回退
  全模型），与游戏 `DecodeShotSegment` 部件约束同构。
  **hash6 6 字节语义（2026-09 三轮：DecodeShotSegment 完整数学解出，见《客户端弹道与
  命中位置逆向报告.md》§二）**：**客户端把 hash6 解码为部件 AABB 内两个 1/255 量化点
  （入点/出点）**——`showDamageFromShot` 逐元素将 8B 元素 u64 原样传入
  `BWUtils::DecodeShotSegment`（内层 @0x1334E30）：
  `P = min + (max−min)×字节/255`，轴序 x←b2/b5、y←b4/b7、z←b3/b6（交错序，对应游戏
  部件块 {右,高,前} 存储序——与 itemDefs JSON {右,前,高} 轴序互换恰好补偿，故
  hitpoint_probe 的直序映射正确，J39 84 发 med 1.87 m 与二进制自洽）；
  `out1==out2` 单点分支（未穿/点终止）；两点经部件矩阵变换后放置受击特效/贴花——
  **车辆弹孔位置 = segment 解码点，非 raycast**（修正旧结论）。`DecodeHitMarkSegment`
  （@0x1337C40，消费 `hitMarks` 属性的 HitMarkPlacementInfo）共用同一内层数学。
  与服务器侧 [shell u16][来向 yaw u16][抵达 pitch u16] 编码解释并存不矛盾（见逆向
  报告 §2.4）。残余偏差来源 = itemDefs 轴对齐盒近似，升级路径 = CollisionMeshes
  scg 部件 AABB。另更正：`showDamageFromShot` 中 result==4（跳弹）在受害者死亡/零血时
  改写为 2（@0x16A9271），非 cmpIndex 重映射。

| 系统 | 职责 |
|---|---|
| `TracerManager`（ShowTracer/OnExplodeProjectile/ConfigureTracerColor/MakeAtgmExplosionMarkerEntity）+ `TracerSystem` | 弹道显示与爆炸标记（ATGM 专用爆炸标记） |
| `GunRecoilSystem`（OnShoot + per-vehicle 映射） | 炮身后座 |
| `VehicleFilterSyncSystem` / `DetachedTurretFilterSyncSystem` | 滤波位姿 → 渲染 transform |
| `MinimapObjectsController` / `MinimapObjectsViewImpl` | 小地图标记（ShowVehicleMarker/SetObjectPosition/SetObjectAngle/RemoveKilledObject） |
| `HitMarkPlacementInfo` → `TankHitMarkComponent` → DAVA `DeferredDecal` | 弹孔贴花（位置=客户端弹道线×模型求交；精确位置数据不在回放中，逆向分析文档 §七"死路清单"） |
| `HealthBarController` / `DamagePanel` 族 / `ShootingFeedback` 资源键 | 血条/伤害面板/模块反馈文案 |
| `ArmorVisualizationSystem`（源码路径泄露 `client/dava.framework/Modules/GameplayCommon/...`） | 装甲可视化高亮 |

---

## 五、服务器判定 vs 客户端呈现的边界（结论 reaffirm）

- 命中判定/穿深/伤害结算在服务器（cell 端直存 raw volatile，引擎源码实证）；
- 客户端方法面（§4.2/§4.3）全部是"接收广播 → 呈现"：`showShotResults`（结果）、
  `set_hitMarks`（贴花描述符，位置自算）、`set_criticalDevices/destroyedDevices`（模块状态）；
- 因此存在两层位姿语义：**判定层**（服务器 raw volatile = 本项目 WI 对齐锚点，装甲几何
  用）与**渲染层**（滤波器输出 = 本项目 filter.rs 渲染锚点，"玩家看到的"用）；
  裁判实验 4/4（隐含滞后 0.210s / 渲染-raw 偏差 med 3.8cm；滤波器实现详见 src/replay/filter.rs，原始推导见备份存档 §七）。

---

## 六、对本项目解析管线的对照结论与建议

### 6.1 已对齐清单（本项目 ↔ 游戏处理语义）

| 数据 | 游戏入口 | 本项目状态 |
|---|---|---|
| volatile 移动流 | WGVehicleFilter2 | 判定锚点（WI 对齐 99/99）+ 渲染锚点（filter.rs 移植，4/4 裁判） |
| method29/20/0x1b 弹道 | showTracer/explodeProjectile | 全解码（shotId 确定性配对） |
| method38 结果 | showShotResults | 全解码（resultFlags32/组件/modifiers） |
| method8 直击通知 | （Vehicle 呈现链） | 全解码（result 枚举 + hash6 + 尾部指纹） |
| type=32 命中警告/segment | showOwnVehicleHitDirection 等 | 解码（result/弹种/segment u64） |
| method1 HP | Vehicle::set_health 族 | 血量链伤害归属 ✓ |
| type=28 弹药 | updateVehicleAmmo | 弹药槽 ✓ |
| method36 瞄准 | updateGunMarker | 部分（field1 ✓，field2+ 未定） |
| 0x0d/0x23 装填 | updateVehicleGunReloadTime | 部分 |
| **updateArena (method 48)** | **AvatarGameLogic::updateArena** | **已解码：子类型 ID 表 + envelope + PERIOD 阶段时间线（combat.rs `collect_arena_updates`/`parse_arena_periods`，CLI combat JSON `arena_updates`/`arena_periods`）** |

### 6.2 本次新发现、可解析项（按价值排序）

1. **~~UpdateArena 26 子类型~~ → 已落地**（本轮）：子类型 ID 表、envelope `[subtype][len][protobuf]`、
   PERIOD 阶段时间线已实现进 combat 输出；RELOAD_TIME/VEHICLE_STATISTICS/VEHICLE_KILLED
   的字段级解码可按需继续；
2. **Vehicle 属性名对齐 + segment 部件字段**：prop 数字 ID ↔ 属性名的统计对齐已完成一轮
   （prop2=2B 纯偏航（gunAnglesPacked 假设否定）/prop3=HP/prop9=作者俯仰/prop10=伤害计数，
   见 §4.3 探针实测）；**下一步突破口已明确**：解完 `DecodeShotSegment` 位域（§4.7）并
   确定 showDamageFromShot 的 8 字节描述符数组在线上数据中的来源（与 type=32 segment
   u64 不同域——type=32 B1 为弹种国家字节 0x5A/0x6A/0x2A，喂给解码器必触发断言；
   候选 = method8 尾部 (b0,b1,b2) 指纹或尚未定位的专用方法），即可用服务器 cmpIndex
   校准本地 raycast 的部件选择，收敛命中位置偏差；
3. **`hitMarks` 属性（HitMarkPlacementInfo）**：命中贴花放置描述符——与 method8 尾部
   (b0,b1,b2) 装甲片指纹交叉验证的最直接入口（逆向分析文档 §七未竟项第一條的突破口）；
4. **`DetachedTurret` / `KineticObject` 实体流**：炮塔击飞等物理事件的复现数据源
   （解释回放中部分未知 entity 的 type=10 流）；
5. **`onChatAction` 聊天流**（ChatActionData）与 `AreaDestructibles` 场景破坏状态；
6. **特殊模式标记**（`showLightning` / `BOSSMODE_INFO` / `VAMPIRIC_CURSE`）——扫描报告
   的对局类型标注。

### 6.3 方法论附注

- 符号面来自断言/日志限定串与 RTTI，def 分发表为纯数字——**方法/属性 ID ↔ 名字**的
  映射无法从字符串直读，需描述符表反汇编或行为统计（如 prop2↔set_gunAnglesPacked 的
  验证：比较属性载荷与炮塔角换算）；
- `ClientArena::ParseProtobufMessageFromString`、`PublicArenaInfo::initFromStream` 等
  入口说明 WG 在 BW 消息层上自建了 protobuf-lite 子系统（0xFF 转义 u24 字段号），
  解析 updateArena/hitMarks 等结构化载荷时按此实现。

---

## 七、证据索引

| 证据 | 位置 |
|---|---|
| 源码路径 512 条 | `C:/ba/tc/work/t/client/Classes/...` 字符串（含 BW/Replay/Battle/Vehicle 子树） |
| `类::方法` 限定名 | 断言/日志字符串（AvatarGameLogic 32、VehicleGameLogic 23、ReplayMessageHandler 15 等） |
| 实体类 10 个 | `X::onMethod` 符号 |
| UpdateArena 26 子类型 | `UpdateArena::XXX` 枚举字符串 |
| protobuf 消费者 10 个 | `X::initFromStream` 符号 |
| 滤波器类簇 | RTTI + vtable + 反汇编（逆向分析文档 §7.2，vtable @VA 0x36EF874 等） |
| WG protobuf-lite 字段号扩展 | `HitMarkPlacementInfo` 解析器反汇编 @0x15BE2E0（0xFF→u24 标签、12B 描述符项） |
| updateArena methodID/envelope/子类型 ID 表 | 分派函数 @0x1636C90（payload[0]−1 查表 @0x40391D8）；`examples/prop_probe.rs`、`examples/method_probe.rs` 统计探针 |
| devReplaysIgnores.yaml | `Data/devReplaysIgnores.yaml.dvpl` 解码（idle/grossberg 两图可破坏物黑名单） |
| debug 图表（滤波遥测） | exe 字符串 + `Data/optionsGlobal.yaml.dvpl` 调试图表段 |
