# WoTB Blitz 回放数据段参考（按数据段组织）

> 本文档是回放文件 `data.wotreplay` 包流的**数据段权威参考**：每个数据段一节，
> 给出字节布局、已破解语义、以及**在本项目中的使用状态**。不按逆向时间线叙述——
> 历史推导过程存档于 `backup_pre_wi_align/*.pre_restructure.bak.md`；
> 客户端渲染管线与位置滤波器见《游戏回放数据处理分析报告.md》；
> wotinspector 破解与命中几何见《客户端弹道与命中位置逆向报告.md》§二。
> 样本回放：GB109（11 发）、T110（8 发）、J39（16 发）、J20（13 发）、XM551（6 发）
> + 受控实验 20260923_1153 / 1617（俯仰与炮塔整圈定标）。
>
> **使用状态标注**：
> - 【使用中】射击复现提取链（src/replay/combat.rs）实际消费的字段；
> - 【辅助】已破解，供交叉验证/诊断/质量标记，非复现主路径；
> - 【未使用】提取链未消费——含"已破解可用"与"语义未定"两种，分别标注。

---

## 一、坐标系与全局约定

- 回放世界系 = BigWorld 系：水平 `(x, z)`，`y` = 接地高度（米）。车体前向
  `forwardWorld = (sin(hullYaw), cos(hullYaw))`【使用中】。
- type=10 姿态符号：`pitch 正 = 车头下坡（nose-down）`（GB109 20 实体 corr=−1.00）、
  `roll 正 = 车体向左倾`（假设，样本缺纯横移）【使用中】。
- 服务器 hitscan 判定：method20/38/8/1/32 与 method29 开火同钟（+0~1 tick），
  命中在开火 tick 一次性判定【辅助：fire_tick 对齐键的理论基础】。
- eid→tank_id 精确映射 = type=5（eid→昵称，昵称 @payload[57]）× battle_results
  （account_id→tank_id）联表【使用中；俯仰极限锚定表的数据源】。

---

## 二、车辆实体流（每坦克一份）

### 2.1 type=10 — 实体移动/姿态包（len=49，~10Hz）【使用中】

```
[0..4]   entityId
[4..8]   spaceId（样本恒 4060）                        【未使用】
[8..12]  attachment/parent entity ID（样本恒 0）        【未使用】
[12..24] 位置 3×f32 (x, y, z) ✓                        【使用中】锚点/轨迹/弹着基准
[24..36] 状态/滤波误差 3×f32 小量（~1e-4，语义未解）    【未使用·语义未定】
[36..48] 车体 yaw/pitch/roll 3×f32（rad）✓             【使用中】车体姿态
[48]     trailingStateRaw（恒 01）                      【未使用】
```

### 2.2 type=7 prop2 — 打包角（len=14，2B 载荷，变化驱动广播）【使用中】

```
u16 = (炮塔偏航 coarse10 << 6) | 炮管俯仰比例 frac6
偏航 = (u16>>6)/1024 × 2π − π        （相对车体，0.35°/级）
俯仰 = ele − frac/63 × (dep + ele)    （炮塔系，正=仰角，按车型极限锚定）
```

- **frac=63 ↔ 俯角极限（炮管最低）、frac=0 ↔ 仰角极限（最高）**（T110 1617
  受控实验极限钳位 + 实战常态贴 63 + 反向锚定消除对照系统偏差，三方定案；
  弹道绝对锚回归斜率 0.997 排除其他刻度）。
- **frac 恒定 ≠ 冻结**（2026-09-24 用户实战确认修正）：快速偏航段 frac 钉极限
  是炮管物理贴极限的**实时如实上报**（第三人称预瞄点在近地面的正常操作常态），
  非"发送端只更新粗位"的通道冻结。prop2 变化驱动，值不变即不重发语义不同——
  服务器按需重发不变值。`pitch_frozen` 标记仅用于 prop2 整流断流 >2s（AoI
  边界/补发簇）的陈旧提示。
- 主视角/队友/敌方**编码完全统一**；全套车辆（含作者）炮管俯仰的主来源。
- 车型极限（dep/ele）来自 battle_results × tank_cache
  （`TankResolver::pitch_limits_from_battle_results`）；无锚定时回退链见 §八。
- 注意：偏航**只能取高 10 位**——整 u16 解码会把 frac 变化混入偏航（±0.3° 假跳变）。

### 2.3 type=7 prop3 — 当前 HP（len=14，2B）【使用中】

正 i16 = HP；0x0000 = 零；0xFFFD = 死亡哨兵。与 method1 血量链共同构成伤害归属与击杀判定。

### 2.4 type=7 prop0 — 点亮布尔（1B）【未使用·已破解】

0/1 交替（AoI 内全实体广播，战斗结束清零）。点亮时间线数据可用，提取链未消费。

### 2.5 type=7 其他 prop 【混合】

| prop | 内容 | 状态 |
|---|---|---|
| prop1（1B） | 死亡清零/结算广播 | 【辅助】死亡事件流 |
| prop4（2B） | 状态二元组：s1∈{0..3} 随速度（0≈静止/1 低速/2 快速/3 过渡）；s2 = 高位(0/4/8)+低位(0/1/2)（油门/制动模式；hi 与速度无关） | 【未使用·部分定性】 |
| prop7/8/9（1-2B） | 设备/模块状态标志（受击后广播，0=完好） | 【未使用·已破解】模块损伤时序可用 |
| prop10（4B u32） | 实体累计伤害计数器（每造成伤害后 ~0.7s 广播，与 shot 伤害累计字节级吻合） | 【使用中】`CombatTimeline::infer_shots`（combat CLI） |
| prop11（4B u32） | 命中奖励计数 | 【未使用·已破解】 |
| 1B 结算版 prop2/3 | 战斗结束清零广播 | 【辅助】流结束标记 |
| prop7/8/9 完整命名空间、method16/17 等 | WotbTools GUESS/UNKNOWN 族 | 【未使用·未知】 |

### 2.6 type=5 — 实体创建【使用中】

eid→昵称（昵称 @payload[57]，ASCII 长度前缀串）；初始属性 blob 含 prop2 初值
（与进世界后首个 prop2 采样一致）。作者解析 = 文件名昵称匹配。

**尾部属性表**（>100B 车辆实体包；按 (属性 id u8)(定长值) 序列，值长随 id 定）【使用中】：

- **id 13 = 进世界血量 u16 LE**（含耐久加成装备；跨场探针 30+ 实体与 @51 首包一致；
  重广播包恒为开局值）→ `loadout::initial_hp_from_type5`；尾部锚 `0c 00 0d ?? ?? 0e`
  （id12=1B、id13=u16、id14=u16），值域 100..10000 防误配；
- id 11 = 匿名乱码名（长度前缀串，长度随账号变化）；id 12/14 = 1B/2B；id 15 = 8B；id 16/17 = 1B；
- 旧客户端无尾部表：血量回退 payload[51] u16（该偏移在重广播包为**当前**血量，
  首包才等于开局值；GB109 旧格式验证通过）。

### 2.7 type=4 — 实体离开 AoI（[eid]）【辅助】

AoI 边界诊断。

---

## 三、Avatar 方法流（envelope = 作者 avatar；弹丸流为全局广播需按 shooterEntityId 过滤）

### 3.1 method29 (0x1d) — 弹丸发射（args 37B）【使用中】

```
[0..4]   shooterEntityId u32                           【使用中】
[4..8]   shotId u32（确定性配对键）                     【使用中】
[8]      rawFlag u8（bit2 恒置 + 低位 0..3 + 高位 8/16；逐射手分布不同）【未使用·语义封存】
[9..21]  launchPoint 3×f32（炮口发射位置，世界系）      【使用中】ball_a
[21..33] launchVelocity 3×f32（m/s；服务器权威弹道方向，与连线夹角 <0.1°）【使用中】
[33..37] terminalRaw f32（恒 6.278，五回放 502 发全同 = 协议占位常量）【未使用】
```

### 3.2 method20 (0x14) — 弹道终点（args 16B）【使用中】

```
[0..4]  shotId u32                                     【使用中】
[4..16] endPoint 3×f32（弹道停止点：穿透弹在目标另一侧/内部、未击穿弹≈表面点、
        miss=空地终点）                                 【使用中】ball_b / aim_point
```

### 3.3 method38 (0x26) — 命中结果反馈（仅作者射击）【使用中】

```
[0..4]  victimVehicleId u32（服务器权威目标）           【使用中】
[4..8]  resultFlags32 u32 = flags16 + headerHi16<<16   【使用中】
[8]     resultCount u8 + n×(componentToken+rawState)   【使用中】
[尾]    modifierCount u8 + n×modifierId u32            【使用中】
```

resultFlags 位图（WotbTools PROVEN + 样本复现）：0x0001 直接击杀 / 0x0008 跳弹 /
0x0010 材料击穿 / 0x0020 未击穿 / 0x0040 间隙层穿 / 0x0080 间隙层未穿 /
0x0100 内部模块穿 / 0x0400 履带受损 / 0x0800 火炮受损 / 0x1000 HE 爆炸伤害 /
0x20000 headerHi 基础位；0x2000/0x4000/0x8000 高位子分支未逐一命名。
组件号 31=引擎 32=弹药架 33=油箱 34/35=右/左履带 36=火炮 38=观察装置
（crit/destroyed 位图 bit = token − 31，与 WI 实测对齐）；rawState 0=无变化 1=受损 2=摧毁；
modifierId 1=精准火力 2=钨芯弹。

### 3.4 method0x1b (27) — 地形命中（args 34B）【使用中】

```
[0..4]  shotId u32                                     【使用中】
[4..8]  shell_global_id u32 LE                         【使用中】
[8]     material u8（每地图子集的全局表面类型；mat0/5→tail1、mat2→tail0；命名未定）【辅助】
[9..21] impactPoint 3×f32（== method20 终点，逐发一致） 【使用中】脱靶弹精确落点
[21..33] segmentStartPoint 3×f32（直线弹=发射点；跳弹=弹跳点）【使用中】
[33]    tail u8（与 material 相关）                     【未使用】
```

仅当弹丸未命中坦克且撞到**地形**时广播（岩石/建筑等静态物无此包，覆盖约 2/3 地形弹）。
**作用域 = 全局广播**：args 不含 shooter，shotId 全局唯一——五回放实证含所有玩家的脱靶弹
（J39 场 30 发他人命中，探针 loadout_probe 按 shotId × method29 联表），作者/他人路径共用：
他人脱靶弹的 `terrain_impact` 精确落点 + 弹种兜底第三级 `shell_from_terrain`（shell_global_id 直取）。

### 3.5 method0x07 — 弹种广播（args 5B）【使用中】

```
[a0 u8][shell_global_id u32 LE]
```

a0=0/1 = 弹药槽位（同钟成对同值 = 双槽同弹）；a0=18 = 异构子命令（非弹种，解析排除）。
发射时刻最新广播 = 该发实际弹种（与 type=32 segment 一致 30/30）——命中通知未转发时
（含全部脱靶弹）兜底 `shell_id`（`shell_from_broadcast` 标记）。
**作用域 = 作者 avatar 弹药状态**：他人弹不可得（他人弹种走 0x1b 兜底，见 §3.4）。

### 3.6 method0x00 — 开火事件【辅助】

envelope = **射手车辆实体**（区别于弹丸流的 avatar envelope）；args=[01]；
XM551 的 [03] = 弹匣末发标记（8/8 与第 3 发同钟）。覆盖与 method29 同域但 AoI
独立裁剪——只作交叉验证与他人射击识别源，不作配对键。

### 3.7 method36 (0x24) — 瞄准快照（args = [len u8][protobuf]）【辅助】

```
field1 f64 = 炮塔相对车体偏航（与 prop2 同语义，f64 全精度，|Δ|≤0.024 rad）【辅助】偏航公式定标真值
field2 f64 = 疑似俯仰族（与 prop9/弹道仰角均不吻合，参照系未定）          【未使用·语义未定】
field3/4/5 f64 = 车辆慢变参数（0.34~2.5）                                【未使用·语义未定】
field6 { f1=当前扩散度（成对快照 ≈0.842→0.906）, f2, f3{…} }             【未使用·语义未定】
```

开火时刻成对出现（前后各一，f6.f1 差 ≈ 后座跳散）；另有开局快照与瞄准变化单发。
落地为 `shooter_aim`（turret_rel_yaw + state_before/after）。

### 3.8 method0x0d / method0x23 — 装填状态【未使用·已破解】

- 0x0d：args(9) = `[entity][code][f32 秒]`。code=0 = 当前装填阶段时长
  （T110E5 8.805 / Type5H 12.494 / Type2605 19.013 / XM551 14.675；GSOR 鼓内 2.5、
  整鼓 14.486）；**code=3 恒 = code0 − 0.1s**（疑似换弹种惩罚后时长，跨四车一致）；
  code=4/7 仅弹鼓车（段时长）。
- 0x23：args(13) = `[entity][f32 剩余装填秒][f32 鼓内秒][u8]`（倒计时递减确证；
  非弹鼓车第二值恒 0）。
  装填时间线数据可用，提取链未消费。

### 3.9 method0x31 — 玩家设置快照【未使用·已破解】

zlib 压缩 Python pickle（PlayerSynchronizedOptions + 全坦克短名表 + 键位映射），开局 1 次。

### 3.10 其他 Avatar 方法【未使用·已鉴别】

| mid | 内容 | 状态 |
|---|---|---|
| 0x30 | 竞技场 protobuf 快照/占领进度（大包=开局全量） | 【辅助】`collect_arena_updates`/`parse_arena_periods`（combat CLI 战局阶段） |
| 0x11 | 12B 成对嵌套 id + 缓增尾 u16（疑似 gun marker 状态对） | 【未使用·语义未定】 |
| 0x0c | `[01 00][模块 u16][u16]`（与 method38 模块损伤时刻相邻） | 【未使用·疑似模块面板转发】 |
| 0x12 | `[eid][u16][00×6]` 单发通知 | 【未使用·语义未定】 |
| 0x27 | 每 10s 心跳（args=00 00） | 【未使用】 |
| 0x04 | 16B = 4×f32，车辆成对同钟近似载荷（疑似碰撞/挤推） | 【未使用·语义未定】 |
| 0x02/0x05/0x10/0x13/0x17/0x19/0x1c/0x1e/0x26/0x29/0x2c/0x2e/0x2f | 开关/实体引用对/任务进度（0x29 含 ASCII "&tj_damage_one_battle_lvl4_"）/booster 配置（0x2c "rareStaticBooster"）/设置 protobuf（0x2f）等 | 【未使用·会话级】 |

---

## 四、Vehicle 方法流（envelope = 受击者；全局广播）

### 4.1 method8 (0x08) — 直击通知（showDamageFromShot wire 调用）【使用中】

```
args = [shooter u32][victim u32][元素数 u8][元素 8B][尾部 4B]
元素 8B = [result u8][cmpIndex u8][hash6 6B]
尾部 4B = [b0][b1][b2][b3]
```

- **shooter / victim**【使用中】：他人路径的目标配对（±0.05s 窗）与伤害归属；
  victim 快照 = method8 处理时刻受击者运行状态（WI 对齐锚点语义）；
- **result**【使用中】：0=无 1=未穿 2=间隙止 3=有伤害（击穿/HE）4=跳弹
  （与 type=32 同域 86/86）。受害者死亡时 result 4→2 改写（@0x16A9271，作用于 result 字段）；
- **cmpIndex**【使用中】：服务器指定受击部件 0=底盘/履带 1=车体 2=炮塔 3=炮管
  （离地高度分层实证 med 0.70/1.17/2.84m）→ `server_part_index`，弹着点部件约束；
- **hash6**【使用中】：游戏客户端 DecodeShotSegment 两点编码——服务器在命中判定
  时刻写入的出入点部件 AABB 量化坐标：P1=(b2,b4,b3)、P2=(b5,b7,b6)，轴序
  x右←b2/b5、y前←b4/b7、z高←b3/b6，1/255 线性插值（b4 恒 255 = 入点钉盒前界面）。
  解码需配游戏原生 collision.*_bbox 部件盒与部件枢轴位姿（详见《WI 射击参数与
  命中位置分析》§5）。用途：射击复现 3D 视图的出入点标注 + P1→P2 判定射线基准
  （位置与弹向均取服务器编码值）；早期 [弹种 u16][yaw u16][pitch u16] 解读与
  "纯特效字节"结论均已被穷举校准 + 客户端反汇编取代；
- **尾部 4B**【未使用·封存】：
  - b3 = 该次交互造成伤害/摧毁标志（res=3 → 1 占 228/229；res=1/2 → 0 100%；
    跳弹致模块损伤 → 1）——可区分多交互事件中哪次造成损伤；
  - b0 = 服务器装甲片编号空间（每型号独立、跨部件共享、非空间局部片，更像
    功能分组/模块簇；五回放 385 事件；与 method38 受损模块可联表：
    10785 件 4 ↔ 右履带摧毁×6、GSOR 件 05 = 油箱+双履带+引擎）；
  - b1/b2 与局部命中坐标无稳定相关（|r|<0.8 且组间不一致）。
    完整语义需服务器端配置（客户端文件无此数据），封存。

### 4.2 method1 (0x01) — 血量事件【使用中】

`[victim][hp i16][source u32][cause u8]`——伤害归属链（source=射手 ∧ cause=0 炮弹直击）
与死亡判定（hp_cur==0）；配 type=5 满血锚点预置。WotbTools PROVEN：火/撞伤害要求
外部源（cause 语义校验）。

---

## 五、type=32 — 命中通知族

### 5.1 26/27B 命中通知（b4=1，method 0x11/0x12）【使用中】

```
[eid][01][method][seq u8@9][flag/hash 头][cmpIndex@11（26B）][hash6 6B][segment u64]
```

- **hash6**【使用中】：与 method8 同事件完全一致（86/86）——令牌精确配对（优于时间窗）；
- **segment u64** = `[result u8][shell_global_id u24 LE][0x00][X][Y][Z]`：
  - result【使用中】`game_hit_result`（与 method8 同域）；
  - shell_global_id【使用中】`shell_id` = `(shells.xml 局部 id << 8) | 国家基数
    （nation_id×16+10：uk=0x5a、japan=0x6a、usa=0x2a）`（与 WI shell_id 同值同源；
    实测 GSOR AP 2040→522330、Type2605 112→28778 等）；
    国家序 9 国 ussr/germany/usa/china/france/uk/japan/other/european
    → 基数 0x0a/1a/2a/3a/4a/5a/6a/**7a/8a**（0x1b 广播 × 射手国家联表实证）；
    注意 tanks.pb field1 = `(局部 id << 8) | (国家序×16+1)`（items id 形式，usa 低字节 0x21），
    组表时取 field1>>8 还原局部 id（loadout::ShellKindTable）；
  - B7【使用中】`armor_group` = plateId（五回放 27 发 raycast 对照
    23 匹配/3 相邻板/1 miss；15 型号 Python 复刻 viewer 变换链）；
  - B5B6（BE u16 作 i16）：res=1 全负值（47/47，可作未击穿冗余校验位）、res=0 恒
    ~71-83（17/17）、res=3 全域——有符号量化值，参照系未定【未使用·语义未定】；
  - 不含服务器片元编号（WI 的 segment = [result][layer][hash6] 同样没有）；
- 字节9 = 每实体消息序号（244/244 单调 +0..4 mod 256）【辅助】；
- 26B 字节11 = cmpIndex（与 method8 args[10] 一致 62/62）【辅助】；
- 27B 字节10（7bit，bit0 恒 0）/字节11∈{2..7} 参照系未定【未使用·语义未定】；
- 服务器仅转发部分命中（GB109 覆盖 7/10）——缺失时 method8 result 兜底。

### 5.2 24/25B — 模块/组件状态流（b4=0，method 0x0f/0x10）【未使用·已破解】

protobuf field21 varint(1408) + 子字段{1,2,3,ff} + f64 服务器时间 + f32 值
（模块损伤百分比流；0=正常、ff=删除哨兵、-1 哨兵；值域 0~85.5）。

### 5.3 11~16B 短广播（b4=1，method 2/3/5/6/7）【未使用·结构已解】

`[seq u8][数据 0..2B][00][80 80][XX]`；seq 每实体递增（2700/3109 单调）；XX 全局
随时间 ~1.3s/unit（饱和 0xE0）；数据字节与速度/转角无相关——语义未定
（"量化方位角/距离"假设已证伪）。

---

## 六、标量/控制流

### 6.1 type=35 — 服务器竞技场 tick 计数器【使用中】

u8 递增 @10Hz、自然回绕；展开为时间线（`collect_tick_timeline`）。开火/命中 tick
判定 100/100 对齐 → `fire_tick`；双方窗口 tick 对齐键（滑块跨侧对齐用编号）。

### 6.2 type=28 — 弹药槽选择【使用中】

payload = u32 LE 槽位（作者本人的选择状态）→ `shell_slot`。

### 6.3 type=23 — 弹丸生命周期切换【未使用·已破解】

args=1B（0/1）。

### 6.4 type=31 — 观察者相机 FOV/变焦档位【未使用·已破解】

单 f32 ∈ {6.75, 16.875, 27.0}；27=标准第三人称、更小=高倍狙击（27/6.75=4x）；
成对广播（同钟两条）。非瞄准圈尺寸。

### 6.5 type=39 — 作者观战相机流（len=28，~5×f32/帧）【未使用·已鉴别】

录像者相机方位/距离/位置；@20 f32 ≈ 作者瞄准角（−0.2886 rad ↔ prop9 冻结值）。
非坦克数据，排除于战斗分析。

### 6.6 其余标量流【未使用】

| type | 内容 |
|---|---|
| 0/1/2 | BasePlayerCreate / 边界 / 流控制（头部解析用，非战斗数据段） |
| 11/13/14/17/33 | 地图加载/结算/流关闭/瞄准初始化/实体物化通知 |
| 26 | 弹道清理事件（4B 全零，开火后 0.2~0.6s） |
| 29 | 广播事件（约 4 条/场） |
| 36 | 周期事件（约 1 条/场） |
| 0xFFFFFFFF | 流终结包（每场 1 个） |

---

## 七、确认不在回放中的数据（死路清单）

| 数据 | 结论 |
|---|---|
| **精确弹孔位置（hitMarks 属性）** | 4 回放属性流全量扫描零出现——hitMarks（HitMarkPlacementInfo protobuf，每击 ≥8B）只发受击者本人客户端且未被录制。游戏弹孔 = 客户端弹道线×模型求交（=项目弹着点射线语义），segment 字节仅用于特效种类/朝向 |
| set_gunAnglesPacked 包 | 未被录制器收录；炮管俯仰在 prop2 打包角（§2.2） |
| 服务器片元编号 | type=32 与 WI 均无（B7=plateId 已是最细粒度） |
| 敌车精确俯仰的独立流 | 不存在——prop2 frac6 即全车辆俯仰（§2.2）；prop9 仅作者且为瞄准角 |
| method8 hash6 的坐标语义（终版） | 两点编码成立（客户端反汇编 + 穷举校准，见 WI 文档 §5.1）；被撤销的是中间两轮错误解读——[弹种/yaw/pitch] 与"特效字节"。旧弹孔复现被移除的真正原因是当时盒源/轴序错误，非语义不实 |
| 弹着点的其他未知载体 | 命中窗口 ±0.12s 逐字节 f32 双参照扫描（绝对位置 + 车体系相对，J39/GB109）无新命中；m0x06 = 自体特效事件（76/76 在 envelope 自身 6m、方向竖直、连发沿行驶路径） |

---

## 八、提取链消费地图（字段级）

`ShotReplayData` 字段 → 数据段来源（作者路径 = `extract_shot_replays_with_limits`；
他人路径 = `extract_other_shot_replays_with_limits`；两者共用 `build_entity_indexes`）：

| 字段 | 来源 |
|---|---|
| shooter_pos/ang、target_pos/ang | method29 launchPoint + type=10 状态（锚点选择：method8 快照 > 滤波插值 > 外推） |
| ball_a / ball_b / aim_point / launch_point_rel | method29 + method20（shotId 配对） |
| target_turret_yaw / shooter_turret_yaw | prop2 高 10 位粗值 + type=10 hull yaw（**method8/29 流序快照**：method 包之前的最后已知 prop2——WI battle.json turret_yaw 逐位同基准，2026-09-24 T110E5 21/21 验证；时钟序"≤t 最后采样"在同 tick 包序错位时差 1~8 步） |
| target_gun_pitch / shooter_gun_pitch | **prop2 frac6**（§2.2；取样 = 同上 method8/29 流序快照，扇区选择用同一快照的偏航角——与 WI gun_pitch 逐位同值反号：WI 正=下压角，项目正=仰角，T110E5 11/11 精确；回退：作者 prop9（`shooter_pitch_from_prop9`）、他人弹速反解（`shooter_pitch_from_velocity`）、受击方车体 pitch——均打 `gun_pitch_degraded`） |
| target/shooter_gun_timeline、*_turret_timeline | prop2 序列窗口（frac 解码 / 粗位）；作者俯仰时间线无锚定时回退 prop9 |
| hit_flags / crit_modules / destroyed_modules / modifiers | method38 |
| game_hit_result / shell_id / armor_group / segment | type=32 segment（hash6 令牌 ↔ method8 精确配对） |
| shell_id 兜底 | method0x07 广播（`shell_from_broadcast`） |
| shell_slot | type=28 |
| server_part_index | method8 cmpIndex |
| fire_tick | type=35 |
| terrain_impact | method0x1b（作者弹） |
| shooter_aim | method36 field1（f6.f1 扩散度透传） |
| damage / is_kill / target_name | method1 血量链 + prop3 + type=5 |
| shooter_eid / is_author / shooter_name | method29 shooterEntityId + type=5 昵称匹配 |
| quality（state_dt / anchor_src / pitch_frozen / gun_pitch_degraded / turret_degraded / dmg_unattributed / …） | 上游各段时序质量标记 |
| render_anchor / *_render_timeline | type=10 × 客户端位置滤波器（filter.rs，见《游戏回放数据处理分析报告.md》§7） |

---

## 九、非 data.wotreplay 数据段

| 条目 | 内容 | 使用状态 |
|---|---|---|
| meta.json | 作者昵称/tank_id/时长/地图 | 【使用中】单场解析与查看器 |
| battle_results.dat | 花名册（昵称/账号/队别/tank_id）+ 作者战绩（带伤害/命中/点亮等细目） | 【使用中】敌我识别、eid→tank 联表、俯仰极限锚定；作者细目仅单场报告用 |
| `3d/Tanks/*.scg.dvpl` | DAVA 场景 = 完整车辆模型（无装甲材质绑定） | 【未使用·格式已解】可自建 HitTester |
| `3d/Tanks/CollisionMeshes/*.scg.dvpl` | 部件 AABB（比 itemDefs 更精确的弹孔几何源） | 【未使用·格式已解】 |
| item_defs shots 段 / nations.xml | 弹种局部 id→全局 id、国家基数 | 【使用中】shell_id 解码 |
