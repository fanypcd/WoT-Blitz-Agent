# WoTB Blitz 回放射击事件逆向分析

> 分析对象：`.wotbreplay` 数据段 + wotinspector 网页解析结果 + WotbTools 权威协议文档。
> 样本回放：GB109（11 发）、T110（8 发，含 miss/未击穿）、J39（16 发）、
> J20（13 发）、XM551（6 发）——2026-09-11 起新增方法均在 4 回放交叉验证。
> 参考网页：
> - armor viewer `shot=` 参数：`https://armor.wotinspector.com/en/blitz/17793-rinoceronte/?shot=...`
> - replay 解析 JSON：`https://replays.wotinspector.com/zh-hans/blitz/view/.../`
> 权威文档：WotbTools `WOTB_REPLAY_PROTOCOL_11_19_BILINGUAL_COMPLETE_REFERENCE.md`
> （github.com/A158Coke/WotbTools，11.19.0_china，受控实验证据分级 AFFIRMED/GUESS/UNKNOWN）。
> 本文档字段语义凡标注 ✓ 者均已在本项目样本回放中独立复现。

## 一、坐标系约定（弹道方向验证 ✓）

回放世界系（type=10 / 弹丸生命周期数据）：

| 轴 | 含义 | 观测范围 |
|----|------|---------|
| x | 东西 | ±251 |
| y | 接地高度（米） | 25–45（地形） |
| z | 南北 | -90…243 |

方位角约定：`atan2(dx, dz)`，北=0 顺时针为正。

**y = 接地高度的双重验证**：
1. 8 发弹着点 y 偏移 0.47~1.78m 全在车高 1.9m 内；
2. **炮口高度**：method29 launchPoint.y − 射手 type10 y ≈ **恒定 1.95m**
   （= T110 120mm 火炮离地高度，物理吻合）。

**回放系 → 3D viewer 模型系变换（炮口/相机方位硬验证）**：

```
x_v =  vx·cos(hullYaw) − vz·sin(hullYaw)
y_v =  vy
z_v = −(vx·sin(hullYaw) + vz·cos(hullYaw))
```

即**水平旋转 −hullYaw + z 取反**（viewer 北=−Z），与炮塔旋转
`turretDeg=−(abs−hullYaw)` 同一约定。
验证（炮口变换后 vs 相机方位差）：T110 7/7 ≤2.1°、GB109 9/10 ≤1.2°。
（**曾试验 x 侧向镜像已回退**：镜像后相机反侧——原映射正确；
"弹着点侧向反向"的真因是 method20 终点语义，见下节。）

**method20 终点语义（弹着点标记）**：

黄色弹道终点标记 = method20 endPoint = 弹道**停止点**，非表面命中点：
- 击穿弹：终点在坦克**内部/贯穿路径上**（T110 过穿深度 −3.4~+0.9m，
  GB109 −6.9~+3.4m，以车心投影为基准）；
- 未击穿弹：终点在车心投影前 2.5~3.8m（T110）——比车体表面更靠前，
  疑似弹跳/特效终止位置，语义未完全确认；
- 表面命中点请用红色 raycast 标记（弹道线 ∩ 装甲模型），
  与游戏内命中位置对应。
- （历史：曾按实测做"标记侧向取反"，命中时刻锚点修正后证实不需要，已撤销——
  当时的"侧向反向"假象源自目标在开火→命中之间移动/转向，锚点时刻不一致所致。）

**轴向最终结论（回放系内部，弹道方向验证）**：method29 launchVelocity 与
炮塔指向（prop2+hullYaw）夹角 9/10 ≤1.1°；`forwardWorld = (sin(yaw), cos(yaw))`。

单位：1 type10 位置单位 ≈ 1 米（WotbTools 受控标定：Kanonenjagdpanzer 105 前进
15.8364 unit/s = 57.011 km/h；本项目样本同样吻合）。

## 二、wotinspector 的射击事件数据

### 2.1 replay 页面嵌入 JSON（`battle.shots[]`，17 字段/发）

```
time, shooter(account_id), target(account_id), has_damage,
shell_id(全局 u32, 如 534618), turret_yaw(f32 弧度), gun_pitch(f32 弧度),
segment(u64 字符串), distance(f32 米), nominal_pen, nominal_damage,
damage(u32), hit_flags(位掩码), crit_modules(位掩码), destroyed_modules(位掩码),
is_premium_ammo(bool), ammo_type(0=AP/2=APCR/3=HEAT/5=HE?)
```

- `turret_yaw`/`gun_pitch` 为 **f32 全精度** → wotinspector 从方向向量 atan2/asin 计算
  （即 method29 launchVelocity → 角度）。
- wotinspector 的 `shooter`/`target` = **entity_id 直接来自二进制**
  （T110 样本：shooter=4475243(Anonyme)、target=4475244(__kayyyze__)，与我们解析一致）。
- wotinspector 只列出**命中目标**的射击（7 发），不含 miss（我们的 method29 有 8 发）。
- wotinspector 的解析器**闭源**（服务端运行）。

### 2.2 armor viewer `shot=` 参数（base64 解码 58 字节）

```
[0..2]   header: 0x02 0x01 (version=2, type=1)
[2..38]  9×u32 LE 配置 ID：
         [0]=vehicle_id(shooter) [1]=shell_id [2]=turret_id [3]=gun_id [4]=equipment
         [5]=tank_id(target) [6]=target_turret [7]=target_gun [8]=target_equipment
[38..58] 5×f32 LE 射击参数：
         [0]=??(-82.62) [1]=turret_yaw(+2.048 rad) [2]=≈0 [3]=gun_pitch(+0.017 rad) [4]=nominal_pen(342.0 mm)
```

- `shot=` 参数 = wotinspector 服务端从回放解析后传给 armor viewer 的精简令牌。
- armor viewer 在本地用此参数 + 目标坦克装甲几何（collision.glb）做 raycast 模拟。
- **命中位置不在参数中** → 由 raycast 本地计算（射击方向 × 装甲几何）。

## 三、回放二进制包格式（WotbTools 权威命名）

### type=10 — 实体移动/姿态包（len=49，观测期约 10Hz）

```
[0..4]   entityId ✓
[4..8]   spaceId（样本中恒 4060）
[8..12]  attachment/parent entity ID（样本中恒 0）
[12..24] 位置 3×f32 (x, y=接地高度, z) ✓
[24..36] position/filter-error 3×f32（每实体小常数）
[36..48] hull yaw/pitch/roll 3×f32（弧度）✓
[48]     trailingStateRaw（语义 UNKNOWN——WotbTools 已否定 onGround 解释）
```

朝向约定：`forwardWorld = (sin(hullYaw), cos(hullYaw))` ✓
（炮口在车体系偏移验证：炮塔朝前时前向 +2.7m、侧向 0.1m、高 +1.95m）。

**pitch/roll 符号实证（GB109，20 个移动实体 corr=−1.00，斜率≈−1.0）**：
- `pitch ≈ −地形坡度(沿车头方向)` → **pitch 正 = 车头下坡（nose-down）**；
- roll 符号未实证（样本缺少纯横移），假设 正 = 车体向左倾（右侧抬高），
  viewer 侧倾方向若与游戏相反则取反。

### type=7 — EntityProperty 包装（entityId + propId + valueLen + value）

- **prop2 = 炮塔相对车体朝向**（WotbTools AFFIRMED）：`angle = u16/65535×2π − π`，
  绝对角 = 相对角 + hullYaw ✓（**无 π/16 偏移**——旧文档的 π/16 校准是过拟合）。
- prop3 = 当前 HP 族（正 i16=HP / 0x0000=零 / 0xFFFD=死亡哨兵）。

### type=8 — EntityMethod 包装（entityId + methodId + argLen + args）

method ID 按**实体类**分命名空间（Vehicle/Avatar 两套）。

#### Avatar method29 (0x1d) — 弹丸发射 ✓（全局弹丸流，需按 shooterEntityId 过滤）

```
args (len=37):
[0..4]   shooterEntityId u32 ✓
[4..8]   shotId u32 ✓（确定性配对键）
[8]      rawFlag u8（4/5/6/7/8/23 观测，语义未确认）
[9..21]  launchPoint 3×f32 ✓（炮口发射位置，世界系，米）
[21..33] launchVelocity 3×f32 ✓（发射速度向量 m/s；T110 AP=640.0/金币=853.6，
         与 launchPoint→终点连线夹角实测 0.03~0.04°）
[33..37] terminalRaw f32（样本恒 6.278，语义未确认）
```

#### Avatar method20 (0x14) — 弹道终点 ✓

```
args (len=16):
[0..4]   shotId u32 ✓（与 method29 精确配对，样本 21/21）
[4..16]  endPoint 3×f32 ✓（弹道终点绝对坐标；穿透弹在目标另一侧/内部，
         未击穿弹 ≈ 表面命中点；miss 弹 = 空地终点）
```

#### Avatar method38 (0x26) — 命中结果反馈 ✓（**仅作者自己的射击**）

```
args:
[0..4]    victimVehicleId u32 ✓（受击者 entity_id，服务器权威目标）
[4..8]    resultFlags32 u32 ✓ = flags16 + headerHi16<<16（wotinspector hit_flags 同源；
          headerHi 恒 0x0002 → WI 样本所有非零 hit_flags 均含 0x20000 基础位 ✓）
[8]       resultCount u8，其后 resultCount × (componentToken u8 + rawState u8)
          （rawState：0=无变化 1=受损(crit) 2=摧毁；组件号 31=引擎 32=弹药架 33=油箱
           34/35=右/左履带 36=火炮 38=观察装置——WotbTools 枚举）
[尾部]    modifierCount u8 + modifierCount × modifierId u32（1=精准火力 2=钨芯弹）
```

resultFlags 位图（WotbTools 受控实验 AFFIRMED，本项目样本复现 ✓）：

| 位 | 语义 | T110 样本 |
|----|------|----------|
| 0x0001 | 直接击杀 | |
| 0x0008 | 跳弹 | |
| 0x0010 | 材料击穿 | shot4/6 |
| 0x0020 | 未击穿（材料止） | shot2 |
| 0x0040 | 间隙层被穿透 | shot5/7 |
| 0x0080 | 间隙层未穿（=未击穿） | shot3/8 |
| 0x0100 | 内部模块被击穿 | shot6(引擎) |
| 0x0400 | 履带受损 | |
| 0x0800 | 火炮受损 | |
| 0x1000 | HE 爆炸伤害分支 | GB109 shot2(0xD040) |
| 0x2000/0x4000/0x8000 | 高位子分支（HE 溅射/轨迹类，未逐一命名） | GB109 shot3(0xF010) |

伤害归属：`flags & (0x0010 | 0x1000) ≠ 0` → 消费 ShotEvent（HP 下降）。
（GB109 验证：9 PEN + 1 HE-EXPL = 10 发有伤害 ✓）

**模块损伤位掩码（wotinspector 对齐）**：`crit_modules`（state=1）/`destroyed_modules`
（state=2），bit = componentToken − 31。实测对齐：WI crit=0x04(bit2) ↔ token33 受损 ✓；
token35 履带摧毁 → bit4（J20/J39 样本 ✓）。

#### Avatar method0x1b (27) — 地形命中 ✓（2026-09-11 新解，4 回放交叉验证）

```
args (len=34):
[0..4]   shotId u32 ✓（与 method29/method20 精确配对）
[4..8]   shell_global_id u32 LE（=(shells.xml 局部 id<<8)|国家基数，同 type=32 segment 编码）
[8]      material u8（落点材质类，观测 0/1/2/4/5，命名未定）
[9..21]  impactPoint 3×f32 ✓（== method20 弹道终点，逐发一致）
[21..33] segmentStartPoint 3×f32 ✓（弹道末段起点：直线弹=method29 发射点（0.000m），
         其余=弹跳点——地面跳弹后末段的起始位置）
[33]     tail u8（0/1，与 material 取值相关，语义未定）
```

- **仅当弹丸未命中任何坦克（地形落点）时服务器广播**——4 回放 87 条中 86 条
  对应无 method8 直击通知的发射（1 条为同钟他弹误配）；
- 覆盖约 2/3 的地形弹（GB109 16/20、J39 36/40+）：其余命中岩石/建筑/残骸等
  非地形静态物，无此包；
- 价值：脱靶弹的弹种 + 精确落点材质（脱靶弹此前无 type=32 segment，弹种缺失）。

#### Avatar method0x07 — 弹种广播 ✓（2026-09-11 新解，30/30 验证）

```
args (len=5): [a0 u8][shell_global_id u32 LE]
```

- a0=0/1 恒成对同值广播（双份记录/弹鼓双槽，槽位语义未定）；a0=18 为另一类
  数据（非弹种，解析时排除）；
- **发射时刻最新广播的弹种 == 该发实际弹种**：与 type=32 segment 弹种逐发一致
  （4 回放 30/30，含 AP/HE 混合射击）；
- 已落地：命中通知未被服务器转发时（segment=0，含全部脱靶弹）以此兜底补全
  `ShotReplayData.shell_id`。

#### Avatar method0x00 — 开火事件 ✓（2026-09-11 新解）

- envelope = **射手车辆实体**（区别于 method29/20 的 avatar 实体）；args=[01]
  （XM551 观测过 [03]）；时刻 = 开火时刻（与 method29 同钟）；
- 覆盖与 method29 大致同域但受 AoI 独立裁剪（GB109 142/142、J39 156/155、
  J20 103/93、XM551 59/74——多出/缺失均存在，只可作交叉验证源，不作配对键）。

#### Avatar method36 (0x24) — 瞄准快照 ✓（2026-09-11 部分新解）

```
args = [payloadLen u8][protobuf]:
  field1 f64 = 炮塔相对车体偏航（rad）✓——与 prop2 同语义、f64 全精度
    （47 发开火时刻快照 vs prop2 换算 |d| ≤ 0.024 rad）
  field2 f64 = 疑似炮管俯仰族——与 prop9、弹道发射仰角、相对仰角均不吻合，语义待定
  field3/4/5 f64 = 车辆相关参数（慢变；量级 0.34~1.35 / 0.51~1.35 / 1.35~2.5）
  field6 { field1 f64=当前扩散度, field2 f64, field3{ field1{ f64, f64 } } }
```

- 开火时刻**成对**出现（射击前/后各一条，f6.f1 之差 ≈ 后座跳散）；另有战斗初始
  快照（无 field1/2）与瞄准变化时的单发快照；
- 作者瞄准角为 f64 全精度（prop2 为 u16 量化，步距 2π/65535 ≈ 0.0001 rad）。

#### Avatar method0x0d / method0x23 — 装填状态（部分新解）

- method0x0d：args(9) = [entity u32][code u8][f32 秒]。code 7 = 弹鼓内装填时间
  （GSOR1008 = 2.5s ✓）；code 0/3/4/5/8 = 装填计时各阶段（倒计时/切换，枚举未定名）；
  开火时刻必现；
- method0x23：args(13) = [entity u32][f32 全装填时间][f32 弹鼓内装填时间][u8]。
  战斗开始时广播（GB109 16.0/2.5 = GSOR1008 全装填/弹鼓内 ✓，J39 12.49、
  J20 19.01、XM551 14.67）；受击/换弹事件时重播。

#### Vehicle method8 (0x08) — 直击通知 ✓（全局，envelope entityId = 受击者）

```
args: [shooterEntityId u32][victimEntityId u32][01][result u8][extra u8][hash6][tail...]
```
- result 枚举（与 type=32 segment 低字节同域，86/86 事件实测一致）：
  0=无命中结果 1=未击穿 2=间隙层止 3=有伤害（击穿/HE 爆炸）4=跳弹。
- **hash6 = 6 字节命中令牌**：与同事件 type=32 包完全一致（GB109 86/86 ✓），
  同一令牌可出现多条（多次装甲交互）。
- **尾部 4 字节 = [b0][b1][b2][b3] 交互明细（2026-09-11 部分破解，4 回放 364 条）**：
  - **b3 = 该次交互造成伤害/摧毁标志**：res=3（有伤害）→ b3=1 占 228/229
    （另 1 条 b3=3）；res=1 未击穿 → b3=0 **100%**（44/44）；res=2 间隙止 →
    b3=0 100%（30/30）；res=4 跳弹混合（造成模块损伤的跳弹 b3=1，如履带被
    弹跳弹摧毁 comps=(35,2) 的事件）——可确定区分"该次装甲交互是否有损伤"；
  - **(b0,b1,b2) = 命中装甲位置指纹**：对同一装甲部位恒定（同一部位不同射击、
    不同结果、不同受害者（同类坦克）尾部前 3 字节相同，如 J39 的 044e0200、
    04080200 反复出现）——疑似服务器装甲片编号空间。b0 观测 {4,5,8,10,11}
    （GB109 另见 07/09）、b1 任意、b2∈{0..3}；(b2,b3) 联合观测
    {00,01,10,11,20,21,33}，b3=3 仅 2 例且均与 b2=3 共现（受害者存活，非击杀）；
  - **b0 = 每坦克模型自己的装甲件编号（2026-09-11 补充验证）**：与受损模块
    method38 关联后，b0 按坦克型号分组（J39 用 04/05、J20 用 08/09、XM551 用 0b、
    GSOR 用 04/05），且同一 b0（同一装甲件）后挂多个内部模块——如 GSOR 件 05
    被击穿可同时损伤 油箱+右履带+左履带+引擎，J39 件 04 对应 引擎+右履带；
    一件装甲覆盖多模块的物理图像与客户端装甲模型一致；
  - 与 segment 的 B5..B7（tri/armor_group）无字节级对应关系（78 个 hash6
    连接事件 0/78 相等）。

### type=32 — 命中通知（26/27B 变体）✓ 与其他变体（2026-09-11 重分类）

**变体全表（GB109 len 直方图：11×112、12×14、14×8、15×2、24×10、25×188、26×20、27×66）**：

| 变体 | b4 | method | 语义 |
|------|----|--------|------|
| 26/27B | 1 | 0x11/0x12 | **命中通知** ✓（见下，已解码） |
| 24/25B | 0 | 0x0f/0x10 | 实体属性初始化/广播流（战斗开始阶段全车辆批量发送）——**旧文档"24/25B=来袭警告（含 yaw/pitch u16）"证伪**，受击者炮管俯仰继续以反向弹道兜底 |
| 11~15B | 1 | 2/3/5/6 | 未知短广播（payload 恰为 2/3/5/6 字节；"量化方位角+距离"假设已用真实坐标证伪） |

**segment u64 部分破解（2026-09-09，4 回放 30 样本 + 游戏 item_defs 交叉验证 + 反向实验）**：
```
[result u8][(tank_id & 0xFF)+9 u8][shell_id u16 LE][0x00][X][Y][Z]
```
- `result`：命中结果枚举 0=无/1=未穿/2=间隙止/3=穿(有伤害)/4=跳弹（与 method8 b9 同域，86/86 ✓）
- 字节1：`(vehicleCompDescriptor & 0xFF) + 9`（compDescriptor = meta.json 的坦克 tank_id，
  4 回放全中：21329→0x5a、9057→0x6a、8033→0x6a、21793→0x2a；J39/J20 同为日本 0x61 尾 → 同值）
- `shell_id`：**游戏全局弹种 id（u24 LE = B1,B2,B3）** = `(shells.xml 局部 id << 8) | 国家基数字节`
  （国家基数 = nations.xml id×16+10：uk=0x5a、japan=0x6a、usa=0x2a）。实测：
  GSOR AP 局部 2040 → 全局 **522330**、金HE 2039 → **522074**、Type2605 112 → 28778、
  XM551 2018 → 516650——与 WI shotsimulate 状态 blob 的 shell 字段**逐发一致 7/7** ✓
  （早期文档的 "(tank_id&0xff)+9" 为 4 样本巧合，已更正；旧 u16 解码 2040 为局部 id）
- 尾三字节 `[X][Y][Z]`：逐发变化。三种假设（三角形索引/局部命中点量化/装甲组）
  均未获支持；与 WI 同源对照（GB109 罗塞塔实验，10/10 发）后确认——
  **服务器不下发明文片元编号**，见下条。
- **B5B6 统计规律（2026-09-11，4 回放 291 样本，BE u16 作 i16 解释）**：
  - res=0（无结果）→ 恒 ~71-83（17/17 跨回放集中——疑似占位常量）；
  - res=1（未击穿）→ **全部负值**（i16 -8899..-188，J39/J20/XM551 集中 -250±60，
    GB109 达 -2758..-5819）；res=2（间隙止）多数负值（少数正离群）；
  - res=3（有伤害）→ 全域分布（+14..-172 均有）；
  - HE 弹假设已否定（负值 104/108 为非 HE 穿甲）；
  - 推测为命中点/几何量化有符号值，参照系未定——但"res=1 ⟹ i16 负值"可作为
    未击穿判定的冗余校验位（47/47）。
- **WI 罗塞塔对照（2026-09-09，GB109 同场战斗双向解析）**：
  - WI 的 `segment` protobuf 字段 = `[result u8][layer u8][hash6 原样 8 字节]`——
    其 "tri/part/hi" 全部是 hash 字节的巧合重排，**WI 也没有服务器片元编号**，
    他们的命中点由炮塔姿态 + 距离沿瞄准线重建（shotsimulate URL 参数结构印证）；
  - `hit_flags`：WI ≡ 本地（method38 flags32 逐位一致 10/10）✓；
  - `hash6`：WI ≡ 本地（同一包字节 10/10）✓；
  - WI `layer` 字节 ⟺ 本地 type=32 命中通知包存在性（10/10）——WI 同样解析该包；
  - **本地多解析出的**：segment 内的 `shell_id`（游戏 item_defs 原生 id）与
    tank 标识字节——WI protobuf 无此二者（其 shell_id 为自有编号空间）。
- 服务器仅转发部分警告（GB109 覆盖 7/10 命中）；缺失时以 method8 result 兜底。
- **正向发现**：本地 `3d/Tanks/{Nation}/{Tank}.scg.dvpl`（DAVA KeyedArchive 场景，
  顶点 15/16 float 步长，position 首列）即完整车辆模型（Tortoise 66 组 11864 三角），
  可用于自建本地 HitTester——但目前 scg 内未发现三角形→装甲材质的绑定信息。

### wotinspector WASM 逆向（2026-09-11，armor-inspector.wasm）

**WI 的"射击事件本地解析"本体找到了**：armor.wotinspector.com 加载
`ai/webapp4.5.0/armor-inspector.js + .wasm`（emscripten，7.9MB）。字符串表显示
**这就是游戏本体 DAVA 引擎 HitTester 的编译产物**，不是自研模拟器：

- `protobuf.HitTester` / `protobuf.HitTester.modelFile` / `protobuf.CollisionMaterial.name`
- `hitTester/collisionModelClient`、`vehicles/{}/{}/collision_client/{}.model`、
  `vehicles/{}/{}/collision/{}.model`——加载游戏原生碰撞模型（.model，按 hull/turret/gun
  分件），**不是 BlitzKit collision.glb**
- `last_hit_point` / `SurfaceCollisionPoint` / `continue_trace_if_no_hit` / `retraceLastShot`
- `applyPiercingHitskinModulesLow` / `/shell{}/damageArmor/{:.0f}`（伤害结算）
- 模型内材质链：`geometry/primitiveGroup/material` → `material_name`

**replay 页数据流（replays-*.js）**：页面内嵌服务端解析好的 shots JSON
（17 字段/发），`segment` 字段 = **[result u8][layer u8][hash6 原样] 打包成 u64
的十进制字符串**——实测用户样例（J39 同场战斗）第一枪
`0x0201ff6dc5ef6fba`，hash6 与本地 method8 令牌逐字节一致（时钟原点差 ~18.7s）。
**WI 没有片元编号**：replay 页把 segment 原样传给装甲查看器
（`&segment=<BigInt>&shooter.shell=<全局弹种id>`），命中位置由本地 HitTester
沿弹道模拟得出——与我们"raycast + B7 交叉验证"同思路，但几何源是游戏原生碰撞模型。

**本地游戏碰撞模型已找到并解开格式**：
`3d/Tanks/CollisionMeshes/{nation}-{tank}.scg.dvpl`（DAVA SCPG 场景，DVPL-LZ4）：

- SCPG 头 + KeyedArchive（键值类型：04=字符串、02=u32 标量、06=带长字节缓冲；
  每 archive = 一个 PolygonGroup，含 vertices/indices/vertexFormat 等 13 键）
- J39 碰撞场景：**3 个组，#id = 2 / 4 / 6**（三角形 48/2016/1800——gun?/hull/turret?）
- **J39 的 method8 b0 = {4,5,8,10,11}：b0=4 与组 #id=4 对上**（最大组=车体），
  但 5/8/10/11 不在此文件——(b0,b1,b2) 的编号空间与 CollisionMeshes scg 组号
  部分重合，全量对应需要 turret/gun 独立碰撞文件或 wasm 所用 `collision/*.model`
  原格式（游戏包内未发现；wasm 的加载路径指向 WI 自托管的游戏资源包）
- sc2 = 场景层级（SFV20：KA v2 字符串字典 + 二进制节点表；节点 gun_01/hull/turret_01）
- 车辆 YAML（armorScale/collision bbox）无片元编号

**片元→模型对应可行性结论**：
1. WI 路径证明可行但重：需要游戏原生 `.model` 碰撞几何 + 移植 HitTester（弹道
   步进 + 逐层材质），wasm 可作为行为参考（其导出函数 `_malloc`/`_debugUpdateTexture`
   等暴露了胶水接口）；
2. 轻量路径：大样本统计映射——我们 viewer raycast 已产出每发的
   (section, plateId, 命中点)，与 (b0,b1,b2) 配对学习映射表（b0 已证与坦克型号
   和受损模块聚集相关，见 method8 节）；
3. 短期可落地：CollisionMeshes scg 解析器（格式已解）可先用作碰撞包围盒/组级
   几何的第三数据源，与 BlitzKit GLB 交叉验证。
- **注意**：旧文档 4.1 的 "yaw/pitch u16" 布局声明与 26/27B 实测不符（旧偏移读到的是
  hash6 字节）；24/25B 变体已复核为属性初始化流（见上表变体全表），"来袭警告"解释
  整体废弃——受击者炮管俯仰继续以反向弹道兜底。

### type=39 — 作者观战相机流（非坦克数据，排除）

### type=31 — 观察者相机 FOV/变焦档位 ✓（2026-09-11 新解）

- payload = 单 f32；4 回放全部样本值域仅 {6.75, 16.875, 27.0} 三个量化值——
  27=标准第三人称 FOV，更小值=更高倍狙击（27/6.75=4x）；成对广播（同钟两条）；
- **非瞄准圈尺寸**（旧清单猜测修正）；XM551 样本全程 27.0（未开镜）。
- method36 f2 参照系补充排除（2026-09-11）：与炮口→method20 终点的直线瞄准仰角、
  发射速度弹道仰角均无确定性对应（部分射击吻合 ±0.01 rad、部分偏差达 0.57），
  维持"疑似炮管俯仰族、参照系未定"。

## 四、关键结论

### 4.0 服务器瞬时命中判定（hitscan，2026-09-11 确认）

**method20 终点包与 method29 发射包同钟到达**（clock 差中位数 0.000s：
GB109 140/142、J39 119/155、J20 61/93、XM551 61/74 差 <0.05s，最大 ~1.0s 为
批处理抖动）。且一次射击的完整事件链——弹道终点（method20）、命中结果
（method38）、直击通知（method8）、血量扣减（method1）、命中警告（type=32）——
全部在发射后同一批包（+0~1 tick）内到达。

**结论：命中判定在开火 tick 由服务器一次性完成（含命中/跳弹/未穿与目标选择），
弹道飞行只是客户端表现。** 受击者无法在弹丸"飞行"中躲避；目标快照锚定
method20 终点 clock（= 开火判定时刻）物理正确。"method20 clock = 服务器精确
命中时刻"的旧表述修订为"= 开火判定时刻（+0~1 tick）"。

### 4.0' 命中判定的有效目标快照（2026-09-11 实证，4 回放 299 命中）

**判定用的目标位置 ≠ 开火包时刻的位置，而是 ~0.2-0.5s 前的快照**：

- 方法：对每个 method8 命中事件，把弹道射线（method29 launchPoint+launchVelocity）
  与受害者 type=10 逐 tick 位置求垂距；垂距最小的 tick = 弹道几何上"目标在弹道上"
  的有效快照时刻（dmin_tick）。干净子集（沿线速度>3m/s 且垂距<1.2m，n=42）：
  **dmin_tick median = -0.40s**（p25 -0.60 / p75 -0.10），均值 -0.34±0.29；
- **滞后量不随目标速度增大**（3-6m/s 桶 median -0.40、6-10 桶 -0.41、>10 桶 -0.22
  ——时间滞后而非距离噪声；静止目标垂距曲线平坦无信息，已剔除）；
- **method20 终点停在该有效快照位置 ±2m 内**（沿线 |s_end − w(dmin)| median 1.8m，
  车体尺度内）——终点、射线、目标快照三者自洽；
- 物理解释：**共享延迟快照模型**——射手瞄准的是屏幕上（下行延迟 ~0.3s）的目标，
  服务器判定用的是目标客户端（上行延迟 ~0.3s）上送的权威位置，两者天然一致，
  无需服务器逐射手时间回拨；回放 type=10 流记录的正是这份共享快照；
- **对复现管线的含义**：移动目标的几何正确锚点 = dmin tick（弹道与目标垂距最小的
  type=10 采样），而非开火 tick——viewer 的 tick 下拉已支持手动切换重算弹着点，
  可将 dmin tick 设为默认选中以消除移动目标的可见偏差。

### 4.1 受击坦克炮管俯仰（存疑——type=32 旧布局声明已证伪）

旧结论"**type=32 警告包的 eid=受击者 → yaw/pitch 属性是受击者自己的姿态**"基于
misaligned 偏移读取（26/27B 命中通知变体实测为 hash6+result+segment，无 yaw/pitch 字段；
24/25B 变体已复核为属性初始化流）。"来袭警告"解释整体废弃——`target_gun_pitch`
继续以反向弹道兜底（物理约束 ±20° 的校验保留）。

### 4.2 射击事件参数（已全部确定性获取）

| 参数 | 来源 | 精度 |
|------|------|------|
| 弹道方向 | method29 launchVelocity（f32 向量）| f32 全精度 ✓ |
| 炮口位置 | method29 launchPoint | f32 ✓ |
| 射击者位置/朝向 | type=10 @ fire_time | f32 |
| shotId 配对 | method29 ↔ method20 ↔（伤害游标） | u32 精确 ✓ |
| 命中结果 | method38 resultFlags16 | u16 位图 ✓ |
| 目标实体 | method38 victimVehicleId | u32 权威 ✓ |
| 弹道终点 | method20 endPoint | f32 ✓ |
| 地形落点材质 | method0x1b material（仅脱靶弹，2/3 覆盖） | u8 ✓ |
| 受击者姿态 | type=10 + prop2 + type=32 | f32/u16 |
| 伤害/击杀 | ShotEvent（HP 差分） | u32 |
| 弹种（含脱靶弹） | type=32 segment，兜底 method0x07 广播 | u24 全局 id ✓ |

### 4.3 命中位置

- method20 endPoint = 弹道**终点**（穿透后停止点），非表面命中点；
- **segment（wotinspector 对齐，2026-09 新增）**：游戏原生命中段 u64 已从 type=32
  命中通知（26/27B）提取（低字节=结果枚举；覆盖率约 70%，服务器仅转发部分警告）。
  wotinspector 的 segment 为其自算打包（自研 HitTester × 游戏碰撞模型），与游戏原生
  布局不同但低字节同为结果枚举；完整解读游戏原生 segment 中段字节 = 后续工作；
- **现行方案**：表面命中点 = 射线(method29 launchPoint → launchVelocity 方向)
  ∩ 装甲模型（viewer raycast，与游戏引擎/BlitzKit armor viewer 同逻辑）；
  segment 可用于未来与碰撞模型片元直读对齐（替代/校准 raycast）。

## 五、射击复现管线

> **状态：数据解析层 = 全确定性（shotId 配对 + method38 权威目标/结果）；
> viewer 端 = 服务器炮口相机 + 真实弹道线 raycast + 模型地形俯仰/侧倾；
> 命中结果分类（击穿/未击穿/跳弹/HE）已上屏。roll 符号待目视验证。**

```
ShotReplayData {
    shooter_pos, target_pos,           // type=10 @ 开火/命中时刻（接地锚点）
    target_ang (hull yaw/pitch/roll),  // type=10
    target_turret_yaw,                 // prop2 相对角 + hullYaw = 绝对
    target_gun_pitch,                  // （已废弃用于渲染）type10 地形俯仰角
    （渲染用炮管俯仰 = 反向弹道的车体系仰角 asin(−dirH.y)，见 viewer 炮管段）
    shooter_turret_yaw,                // prop2 + 射手 hullYaw @ 开火时刻
    shooter_gun_pitch,                 // type=7 prop9（狙击模式=炮管俯仰）
    aim_point,                         // method20 终点 − 目标位置@命中时刻（偏移，米）
    launch_point_rel,                  // method29 炮口 − 目标位置@命中时刻（偏移，米）
    ball_a, ball_b,                    // 炮口位置 / 弹道终点（绝对坐标）
    launch_velocity,                   // method29 发射速度向量（m/s，真实弹道方向）
    hit_flags,                         // method38 resultFlags32（u32，0=miss；0x20000=headerHi 基础位）
    crit_modules,                      // 模块受损位掩码（bit = componentToken-31；wotinspector 对齐）
    destroyed_modules,                 // 模块摧毁位掩码（state=2；同上映射）
    segment,                           // 游戏原生命中段 u64（type=32 命中通知；低字节=结果枚举）
    game_hit_result,                   // 结果枚举 0无/1未穿/2间隙止/3有伤害/4跳弹（255=未获取）
    hit_token,                         // method8↔type=32 共享 6 字节命中令牌
    modifiers,                         // 特殊弹药效果（1=精准火力 2=钨芯弹）
    terrain_impact,                    // 地形命中（method0x1b，仅脱靶弹 2/3 覆盖）：
                                       // material + 落点 + 弹道末段起点
    shooter_aim,                       // 开火时刻瞄准快照（method36，fail-soft）：
                                       // turret_rel_yaw（f64 全精度炮塔相对偏航）
                                       // + state_before/after（未定名状态对）
    （damage 归属：method1 血量链差值 + 计数器增量兜底，见上）
    shot_id                            // method29↔method20 确定性配对键
}
```

**弹种解析链（2026-09-11 补全，47/47 全覆盖）**：
type=32 segment（命中通知被转发时，权威）→ 兜底 method0x07 avatar 弹种广播
@ fire_time（与 segment 逐发一致 30/30）——脱靶弹与服务器未转发弹的 shell_id
不再为 0。

**射击复现管线消费（2026-09-11 升级）**：
- web 射击列表新增弹种徽标（author_shells[shell_slot]，AP/APCR/HEAT/HE+穿深）
  与结果徽标（hit_flags 解码），脱靶弹也提供 3D 入口；
- viewer world=1 模式：脱靶弹独立分支（射手模型 + 弹道弦 + terrain_impact
  落点/弹跳点/材质标注，无目标装甲判定）；命中弹接入穿透判定管线
  （真实炮口射线 → /api/penetrate → 面板显示本地预测 vs 服务器判定对比）；
- agent 无头截图 URL 补 `&shell={shell_slot}`（修复非 0 槽弹种渲染错误）。

**确定性匹配链**（无时间窗口歧义）：
- 发射→终点：shotId u32 精确配对（21/21 样本）
- 目标实体：method38 victimVehicleId（服务器权威，修复交火误选/miss 误配 bug）
- **method38 同钟同受击者合并**：一发命中的多次装甲交互（如履带弹跳→穿透击杀）
  会产生多条结果消息（J39 shot#9：0x0028 + 0x0031），flags 取并集视为一次命中
  （WotbTools hit-resolution 同原则：同钟重复记录计为一次命中事件）；
- 命中结果→伤害（WotbTools deriveLosses 同款）：**血量降幅区间法**——
  受害者血量样本（method1，同钟折叠）的相邻降幅（区间末 source=作者 ∧ cause=0）
  = 作者炮弹造成的权威掉血，归属区间 = (前样本钟, 后样本钟]（自适应无固定窗口）；
  命中时刻落入哪个区间，伤害即该区间差值；
  无降幅区间（首击/模块-only 击穿）→ 伤害计数器（type7 sub10）增量顺序配对兜底
  （计数器含撞击/火伤污染，method1 cause≠0 事件同批剔除后与伤害命中一一对应；
  计数器静默 = 服务器未记 HP 伤害 → 0）
- 目标位置/姿态快照：@ **命中时刻**（method20 终点 clock，服务器精确命中时刻；
  miss 回退开火时刻）——模型渲染命中时刻的目标，数据锚点必须同时刻
  （GB109 移动目标实测：锚点修正位置差 1.08m、航向差 13°）；
- 射手姿态快照：@ fire_time（射手在开火时刻的姿态）

**viewer 弹道**：
- 回放系向量（炮口偏移/速度方向/终点偏移）经 toModel（旋转−hullYaw+z取反）后，
  **再左乘 qHullInv（pitch/roll 逆变换）换算到受击坦克车体坐标系**——相机位置、
  射线方向、弹着点标记三者共用同一变换；
- 刚体等价：平放模型 + 车体系变换的射线/标记/相机 ≡ 真实俯仰/侧倾车体 + 世界系
  ——车体系射线对平放装甲求交 = 真实弹道对俯仰车体求交（此前射线方向未换算，
  俯仰目标如 GB109 pitch −15° 会偏移数米命中错误装甲板）；
- 射线 = 炮口(launch_point_rel) + normalize(launch_velocity)——真实弹道线
  （场景原生米制，1 单位 = 1 米，模型不缩放，数据直通；
  模型原点 = 场景原点——glb 原点即引擎车体锚点/type10 位置锚点，
  数据零偏移直通，不再做 hull_bbox 中心水平对齐）；
- 命中点标记 = 射线 ∩ 装甲模型（raycast 实际命中点）；
- view_dir = 炮口→命中点（真实入射方向）；
- 命中距离 = 炮口→命中点（真实米数）；
- 轨迹入射段起点 = 服务器炮口（与射线同线）；
- 黄色标记 = 服务器弹道终点（method20 endPoint，穿透弹在目标内部/另一侧）；
- 命中分类：MISS / RICOCHET / PENETRATION / HE BLAST / NO PENETRATION。

**受击坦克炮管俯仰（渲染）**：受击坦克瞄准射手 ≈ 来袭弹道反向——
  dirH（含 qHullInv 车体姿态补偿）的仰角取负 = 车体系瞄准俯仰
  asin(−dirH.y)。旧实现误用 type10 地形俯仰角（车体姿态 ≠ 火炮瞄准），
  坡地交战偏差实测达 19°（GB84 #11）。

**viewer 相机**：
- 相机位置 = 炮口在目标**车体坐标系**中的相对位置：
  `toModel(launch_point_rel)`（yaw）后左乘车体 pitch/roll 姿态的逆
  （模型保持平放不旋转，姿态全部由相机补偿——与 yaw 处理同一哲学）；
- pitch 实证（GB109 20 实体 corr=−1.00, k≈−1.0）：replay 正 = 车头下坡；
  roll 符号未实证（假设 正 = 向左倾），侧倾观感反向则取反；
- 高度 = 炮口离目标地面高度（天然含地形高差 + 火炮离地高）；
- **相机不压缩**：恒在真实炮口位置，OrbitControls maxDistance 动态抬高
  （max(30, 炮口距离+20)）——固定上限 30 会沿目标原点方向把相机拉回，
  而弹道线不过目标原点，相机因此脱离弹道线、轨迹线不再经过视点；
  （历史：径向 clamp、x/z-only clamp 均曾引发相机入地/射线平移，已废除；）
- 注视点 = 弹道上距目标原点最近处（视线沿弹道；此前固定看模型炮线，
  视线与弹道夹角可达 6°，轨迹线偏离画面中心）；
- **clamp 仅作用于相机取景**——射线/轨迹线/命中距离必须用未 clamp 的
  真实炮口位置：clamp 沿目标原点方向缩放相机位置，而弹道线不过目标原点
  （炮口相对车体有偏移），复用 clamp 后位置会使射线直线整体平移
  (1−maxD/d)×(原点到弹道线垂距)——GB109 #1 平移 5.2m，跨地形远射全部
  触发、T110 近距不触发（"平地正确、有地形高低时不正确"的根源）。
- miss 无目标基准时用绝对弹道两点（炮口−终点）的射手侧方位回退。

## 六、未完成

- method8 尾部 (b0,b1,b2) 装甲位置指纹的具体编号空间（疑似服务器装甲片编号，
  与 segment B7=armor_group 及 raycast plateId 均不同域）；b2/b3 的 3 值组合语义。
- method20 终点语义：击穿弹在车内/贯穿路径上，未击穿弹在车体表面前
  2.5~3.8m——与视觉命中点的关系未完全确认（弹跳/特效终止位置？）。
- method29 rawFlag u8（4/5/6/7/8/23）与 terminalRaw f32（恒 6.278）语义。
- type=32 26/27B 的 u16@9..11 / flag 语义；method 0x11 vs 0x12 差异；
  11~15B（method 2/3/5/6）payload 的量化编码（方位角/距离假设已证伪）；
  B5B6 有符号量化值的参照系。
- `shot=` 参数 f32[0]（-82.62）的语义。
- method36 field2（疑似炮管俯仰族；瞄准仰角直线/弹道假设已排除）/field3/4/5/
  field6.f2 及嵌套子消息语义；field6.f1 常量对（0.842/0.906，跨坦克同值）语义。
- method0x1b material 材质类命名（0/1/2/4/5）与 tail u8 语义；无包的非地形命中
  （岩石/建筑/残骸）如何识别。
- method0x07 a0 槽位语义（0/1 恒成对同值；18=非弹种数据）。
- method0x0d code 枚举（0/3/4/5/7/8）完整命名；method0x23 重播触发条件。
- method0x00 args=[03] 变体（XM551 8 条）语义。
- type=7 sub=4 状态二元组（s1∈{1,2,3} × s2∈{0..10}）与移动速度无强相关，
  维持 WotbTools GUESS。
- hull roll 符号未实证（假设 正=向左倾），需侧坡场景目视验证。
