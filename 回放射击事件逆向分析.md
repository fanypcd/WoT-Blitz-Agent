# WoTB Blitz 回放射击事件逆向分析

> 分析对象：`.wotbreplay` 数据段 + wotinspector 网页解析结果。
> 样本回放：`20260905_1745__Anonyme_GB109_GSOR_1008_582843376141591691.wotbreplay`
> 参考网页：
> - armor viewer `shot=` 参数：`https://armor.wotinspector.com/en/blitz/17793-rinoceronte/?shot=...`
> - replay 解析 JSON：`https://replays.wotinspector.com/zh-hans/blitz/view/.../`
> 逆向分析工具（`combat --dump` 的 26 节验证代码）已完成使命并移除，结论沉淀于本文档。

## 一、坐标系约定（弹道方向验证 ✓）

回放世界系（type=10 / 弹道两点）：

| 轴 | 含义 | 观测范围 |
|----|------|---------|
| x | 东西 | ±251 |
| y | 接地高度（米） | 25–45（地形） |
| z | 南北 | -90…243 |

方位角约定：`atan2(dx, dz)`，北=0 顺时针为正。

**轴向的最终结论（弹道方向硬验证）**：弹道两点方向
`normalize(ball_b − ball_a)` 与射手炮塔指向（sub2+hullYaw）夹角
9/10 发 ≤1.1° 的映射为 **viewer (x,y,z) 全直通**——即回放 z 正方向与
viewer z 轴同向（模型车头朝 -Z），回放 y 为接地高度。
（早期"type=32 来袭方向交叉验证"给出的 z 取反结论是过拟合，已废弃。）
type=10 的 y 分量为【接地高度】而非车体中心高度（8 发弹着点 y 偏移分布
0.47~1.78m 全在车高 1.9m 内；车心说下大部分超车高）。

## 二、wotinspector 的射击事件数据

### 2.1 replay 页面嵌入 JSON（`battle.shots[]`，17 字段/发）

```
time, shooter(account_id), target(account_id), has_damage,
shell_id(全局 u32, 如 534618), turret_yaw(f32 弧度), gun_pitch(f32 弧度),
segment(u64 字符串), distance(f32 米), nominal_pen, nominal_damage,
damage(u32), hit_flags(位掩码), crit_modules(位掩码), destroyed_modules(位掩码),
is_premium_ammo(bool), ammo_type(0=AP/2=APCR/3=HEAT/5=HE?)
```

- `turret_yaw`/`gun_pitch` 为 **f32 全精度** → wotinspector 从方向向量 atan2/asin 计算。
- 这些是 **射击者开火时刻** 的数据，**非受击者姿态**。
- wotinspector 的解析器 **闭源**（服务端运行）。

### 2.2 armor viewer `shot=` 参数（base64 解码 58 字节）

```
[0..2]   header: 0x02 0x01 (version=2, type=1)
[2..38]  9×u32 LE 配置 ID：
         [0]=vehicle_id(shooter) [1]=shell_id [2]=turret_id [3]=gun_id [4]=equipment
         [5]=tank_id(target) [6]=target_turret [7]=target_gun [8]=target_equipment
[38..58] 5×f32 LE 射击参数：
         [0]=??(-82.62) [1]=turret_yaw(+2.048 rad) [2]=≈0 [3]=gun_pitch(+0.017 rad) [4]=nominal_pen(342.0 mm)
```

- `shot=` 参数 = wotinspector 服务端从回放解析后传给 armor viewer 的精简令牌。
- armor viewer 在本地用此参数 + 目标坦克装甲几何（collision.glb）做 raycast 模拟。
- **命中位置不在参数中** → 由 raycast 本地计算（射击方向 × 装甲几何）。

## 三、回放二进制包格式

### type=10 — 实体状态包（len=49，每 tick）

```
[0..4] eid | [4..8] 恒 4060 | [8..12] 恒 0
[12..24] 位置 3×f32 (x, y=高度, z=北)
[24..36] 每实体小常数 ×3（疑似速度缩放）
[36..48] 朝向 3×f32 (hull yaw, pitch=地形坡度, roll) 弧度
[48] 恒 0x01
```

### type=7 sub=2 — 炮塔绝对朝向（每 tick u16）

`ang = u16/65535×2π − π + π/16`，归一化 [-π,π)。
校准依据：3 发"目标追踪射手"样本，误差从 -11.25° 修正到 ≈0°。

### type=32 method 0x11/0x12 — 来袭炮弹警告包（eid=受击者）

```
[eid][01][method u32][u16][flag u8][shell_id u16][yaw u16][pitch u16][segment u64]
```
- yaw = (u16-32768)/32768×π = 受击者指向射手的方位角 ✓
- pitch = (u16-32768)/32768×(π/2) = 炮弹抵达垂直角 ✓
- segment u64 = wotinspector 的 segment 字段对齐 ✓
- 交火中需消歧（yaw 与位置推算方位角偏差 ≤15°）；服务器仅转发部分警告。

### type=8 method 0x1d(29) — 开火事件包（args_len=37）

```
args = [shooter_player_eid u32][5B header][shooter_pos 3×f32 ✓][7B 中段][segment u64]
```
- shooter_pos 三连 f32 已验证（与 type=10 位置吻合）。
- 7B 中段含 turret_yaw/gun_pitch（f32 精度）——布局未破解（非简单 f32 对齐）。
- 这是 wotinspector `shots[]` 的数据源。

### type=39 — 作者观战相机流（非坦克数据，排除）

## 四、关键结论

### 4.1 受击坦克炮管俯仰 ✓（type=32 pitch）

**type=32 警告包的 eid=受击者 → yaw/pitch 属性是受击者自己的姿态**：
- yaw = 受击者炮塔朝向（与 sub2 交叉验证，shot 1 差 1.9°）
- pitch = **受击者炮管俯仰**（shot 1 = -2.43°，KingScopion 瞄准略下压 ✓）

shot= 参数的 5×f32 同时包含射击方向 + 受击者姿态（炮塔 + 炮管），
armor viewer 据此 pose 受击坦克模型 + raycast 模拟击穿。

→ `target_gun_pitch` = type=32 pitch（有记录时），无记录回退 hull pitch。
→ pitch 比例尺 (π/2) 需目视校准。

### 4.2 射击事件参数

从回放可提取的 **射击者侧** 参数：

| 参数 | 来源 | 精度 |
|------|------|------|
| 射击方位 turret_yaw | type=7 sub2（u16 量化）/ type=8 0x1d（f32，未破解） | u16: 0.006°/bit |
| 炮管俯仰 gun_pitch | type=8 0x1d 中段（f32，未破解） | f32 |
| 射击者位置 | type=10 / type=8 0x1d shooter_pos | f32 |
| 弹种 ID shell_id | type=8 0x1d（全局 u32） | u32 |
| segment（弹道种子）| type=32 / type=8 0x1d 尾部 | u64 |
| distance | 由 shooter_pos + target_pos 计算 | f32 |
| nominal_pen / damage | 弹种配置（tanks.pb） | — |

从回放可提取的 **受击者侧** 参数：

| 参数 | 来源 | 精度 |
|------|------|------|
| 受击者位置 | type=10 @ 命中时刻 | f32 |
| 车体朝向 hull yaw/pitch/roll | type=10 f[6..9] | f32 |
| 炮塔朝向 turret_yaw | type=7 sub2（u16 校准） | u16 |
| 炮管俯仰 gun_pitch | **无**（hull pitch 近似） | — |
| 来袭方向 | type=32（u16 量化，部分射击有） | u16 |

### 4.3 命中位置

wotinspector 也未暴露命中位置。armor viewer 通过 `raycast(射击方向 × 装甲几何)`
本地计算。我们的 viewer 同样通过 `doPenetrationCheck(0,0)`（相机射线=弹道线）
本地计算。

## 五、射击复现管线（实验性 · 命中位置不完善）

> **⚠ 状态：数据解析层已达确定性配对级别；viewer 端命中点呈现存在偏差，
> 自动穿透判定结果与游戏内回放不完全一致，仅供参考。**

```
ShotReplayData {
    shooter_pos, target_pos,          // type=10 @ 命中时刻
    target_ang (hull yaw/pitch/roll), // type=10
    target_turret_yaw,                // type=7 sub2（相对+ hullYaw = 绝对）
    target_gun_pitch,                 // type=32 pitch（部分缺失回退 hull pitch）
    shooter_turret_yaw,               // sub2 + 射手 hullYaw @ 开火时刻
    shooter_gun_pitch,                // type=7 sub9（狙击模式=炮管俯仰，4/8 发可信）
    aim_point,                        // 0x14 弹着点 − 目标位置@开火（偏移，米）
    ball_a, ball_b,                   // 弹道两点（射手@开火 / 弹着点绝对坐标）
    fire_counter                      // （内部）0x1d/0x14 确定性配对键
}
```

**确定性匹配链**（无时间窗口歧义）：
- 开火→弹着：counter u32 精确配对（0x1d args[4..8] = 0x14 args[0..4]，142/145）
- 目标实体：伤害数值最接近匹配（target_eid 直传，修复交火误选 bug）
- 状态快照（type=10/sub2/sub9）：@ fire_time 就近取值（连续流快照语义）

**viewer 相机**：相对方位 = atan2(目标→射手) − hullYaw；高度 = 炮线 +
tan(真实俯仰角)×压缩距离（修复 clamp 导致俯仰放大）。

**已知不完善（命中位置偏差来源）**：
- 0x14 是穿透后终点（过穿弹在目标另一侧），表面命中依赖 raycast 截断
- 模型未应用 hull pitch/roll（坡上倾斜），装甲布局与数据几何不一致
- 弹着点 <6m 过滤 + 碰撞盒 clamp 的经验兜底在边缘 case 失真

## 六、未完成

- type=8 method 0x1d 的 7B 中段：含 f32 精度 turret_yaw/gun_pitch，非简单对齐。
- type=32 的 u16@9..11 / flag 语义；method 0x11 vs 0x12 差异。
- `shot=` 参数 f32[0]（-82.62）的语义（distance 为负？或其他字段？）。
- pitch 比例尺（π/2）需目视校准。
