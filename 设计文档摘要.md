# 设计文档摘要

> 完整设计见 [`设计文档.md`](设计文档.md)，射击复现逆向细节见
> [`回放射击事件逆向分析.md`](回放射击事件逆向分析.md)。

## 一句话定位

面向 World of Tanks Blitz 的本地智能分析助手：以 Rust 解析回放与游戏数据为底座，
LLM Agent 通过 10 个工具自主完成战绩查询、回放复盘、装甲穿透分析与 3D 可视化。

## 痛点 → 方案（9 项定制）

| 痛点 | 定制方案 | 落点 |
|------|---------|------|
| 回放文件是黑盒 | `wotbreplay-parser` 解包 + 数据包事件流解码（伤害/击杀/射击推断） | `replay/combat.rs` |
| 看不到近期表现 | WG API 快照 + 回放 vs 累计对比（胜率/场均/评级量化差异） | `wargaming/snapshot.rs` |
| 通用 Agent 拿不到游戏数据 | 10 个专用工具（战绩/扫描/对比/装甲/击穿模拟/热力图截图等） | `agent/tools.rs` |
| 装甲机制不可见 | BlitzKit 数据集成（tanks.pb 723 辆 + models.pb spaced 权威 + GLB 几何） | `wargaming/blitzkit.rs` |
| 击穿判定复杂 | 统一 Rust 判定内核：跳弹/转正/overmatch/多层/间隙甲/HEAT 间隙衰减/HE 溅射/装备修正 | `wargaming/penetration.rs` |
| 对局预判 | `prematch` 阵容强度/威胁/薄弱点分析 | `wargaming/prematch.rs` |
| 操作门槛 | Web GUI（SSE 流式 Agent 对话 + 六大功能面板）+ CLI 双入口 | `web/` |
| 战斗过程不可见 | 3D 装甲查看器（双模型对齐/炮塔炮管交互/穿透热力图/弹道轨迹） | `wargaming/viewer.rs` |
| 射击过程不可见 | **射击复现（实验性）**：回放 → 3D 查看器按射手视角复现每发弹道与判定 | 见下 |

## 架构要点

```
CLI (chat/single/scan/combat/view/...)   Web GUI (axum, SSE 流式)
        │                                        │
        └────────────┬───────────────────────────┘
                     ▼
   Rust 核心：Agent Loop (async, AgentEvent) ── 11 Tools
        │                  │
   Replay/Combat 解析   WG API Client (仅战绩)
        │
   BlitzKit 数据源：tanks.pb / models.pb / GLB / DVPL(游戏文件)
```

- **Agent Loop**：async，`AgentEvent` 流式事件（StepStart/ToolCall/ToolResult/…），
  Web 与 CLI 共用；Ctrl+C 打断、Token 预算自动中断
- **击穿判定内核**（`penetration.rs`）：前端与 Agent 共用同一 Rust 实现，
  伤害三分（HP / 模块 / 无），距离单位统一为米
- **数据离线化**：tanks.pb / models.pb / game_data(723) / GLB 缓存 / 坦克图标
  全部随仓库分发或按需缓存，克隆即用

## 射击复现（实验性 · 不完善）

从回放二进制逆向出确定性事件链：开火 `type=8 0x1d` 与弹着 `type=8 0x14`
通过 **u32 计数器精确配对**（142/145，dt=0）；目标实体按"伤害数值最接近"
匹配（修复交火误选 bug）；实体状态 `type=10` 提供位置与车体姿态（y=接地
高度）；炮塔朝向 `type=7 sub2`（相对车体 + hullYaw = 绝对）；弹道两点
（射手@开火 → 弹着点）与射手炮塔指向夹角 9/10 发 ≤1.1°（轴映射全直通的依据）。

viewer 端：模型保持默认朝向，相机按"目标→射手绝对方位 − hullYaw"相对放置
（高度按真实俯仰角等比压缩，修复距离 clamp 导致的俯仰放大）；受击坦克炮塔/
炮管按回放时刻姿态应用；热力图就绪后沿弹道方向自动穿透判定，模型上标注弹着点。

**已知不完善**：0x14 为穿透后终点（过穿弹在目标另一侧），表面命中依赖
raycast 截断；坡度倾斜（pitch/roll）未呈现；部分射击弹着点匹配异常。
**判定结果与游戏内回放存在偏差，仅供参考。**

## 关键文件

| 文件 | 内容 |
|------|------|
| `src/replay/combat.rs` | 战斗事件 + 射击推断 + 复现数据提取（确定性配对） |
| `src/wargaming/viewer.rs` | 3D 查看器（内嵌 Three.js：热力图管线/穿透判定/射击复现） |
| `src/wargaming/penetration.rs` | 统一击穿判定内核 |
| `src/wargaming/blitzkit.rs` | BlitzKit Protobuf 解析（tanks/models/弹种/spaced） |
| `src/agent/tools.rs` | 11 个 Agent 工具 |
| `回放射击事件逆向分析.md` | 回放包格式逆向结论（坐标系/包布局/校准方法） |
