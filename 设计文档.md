# WoTB Blitz Agent — 设计文档

## 一、产品定位

**WoTB Blitz Agent** 是针对 World of Tanks Blitz（坦克世界闪击战）的 AI 游戏分析助手：以 Rust 解析本地回放文件与游戏数据为底座，LLM Agent 通过注册的工具自主完成战绩查询、回放复盘、装甲穿透分析与 3D 可视化等功能。

## 二、痛点分析

### 痛点 1：看不到近期表现

WG API 只返回账号创建以来的累计数据，API 并不支持按时间范围查询。

**现有工具的不足**：blitzstars.com 被 Cloudflare 保护无法程序化访问；游戏内只显示当前评级，不提供历史趋势。

### 痛点 2：回放文件是黑盒

游戏每场自动保存 `.wotbreplay` 回放文件，但是官方并没有对于其数据格式作出说明。

### 痛点 3：通用 Agent 无法获取游戏数据

ChatGPT/Claude 等通用助手无法解析 `.wotbreplay` 二进制文件，不了解 WG API 的端点调用方式，也无法直观理解以及向用户展示坦克的装甲等性能数据。

### 痛点 4：战斗过程不可见

玩家打完一场不知道"哪一炮打到了谁""什么时候被击杀的""全场伤害怎么积累"。回放文件包含完整数据包流（每场 4.5 万~7.5 万个包），但事件子类型语义未文档化，需要逆向解码。

## 三、场景定制方案

### 定制 1：回放解析 + 战斗事件逆向解码

在第三方 crate `wotbreplay-parser`（解析 ZIP 结构、meta 与 battle_results）之上，自主逆向解码数据包流，目前进展可观。

### 定制 2：API 快照

API 不支持按时间查询 → **快照差值法**：定期采集 WG API 累计数据为带时间戳快照，差值即阶段性变化。

### 定制 3：BlitzKit 数据集成

以 BlitzKit 公开数据文件为坦克数据唯一来源（723 辆，不依赖 WG API）：

| 数据文件 | 内容 | 处理方式 |
|----------|------|----------|
| `tanks.pb` | 名称/等级/类型/国家、弹种（穿深/血量伤害/模块伤害/爆炸半径）、装填 | 运行时 Protobuf 解析 |
| `models.pb` | 炮塔/主炮→模型节点映射、装甲板厚度、炮管俯仰角、spaced 分类（击穿判定权威） | 运行时 Protobuf 解析 |
| `collision.glb` / `model.glb` | 装甲板几何 / 视觉模型 | Three.js 加载 |
| 封面图 | 723 辆 big.webp | 本地缓存 |

### 定制 4：DVPL 游戏文件解码

DVPL 是 Wargaming 本地文件压缩格式（参考 DVPL_Converter 的格式规范用 Rust 自主实现解码器，从游戏 XML/YAML 提取装甲板厚度（hull/turret/gun/chassis）、碰撞 points 偏移与 spaced 标记，批量导出 723 辆可移植 JSON（`data/game_data/`）。

### 定制 5：3D 装甲查看器与击穿分析

- **双模型绑定**：collision.glb（隐藏，raycast 取装甲数据）+ model.glb（可见外观），并将履带/炮管/炮根 mesh 纳入判定
- **炮塔旋转/炮管俯仰**：矩阵旋转法（translate(pivot)·rotate·translate(−pivot)），枢轴从视觉模型几何推导，缺 points 的坦克也不漂移；俯仰限制来自 models.pb
- **统一击穿判定（Rust `penetration.rs`，`POST /api/penetrate`）**：**逐行对齐 BlitzKit `shoot()` 语义**——装甲分类同 BlitzKit ArmorType（Primary=hull/turret/炮盾板角度等效；Spaced 角度等效；External=履带/炮管 flat 抵消，按 variant "track"/"gun" 去重）、收集阶段 push 首个 Primary 即停止、跳弹（每发弹 ricochet 值，HEAT/HE 强制 90° 永不跳弹）/转正（每发弹 normalization 值，缺失按 0；2× 口径增强转正 1.4·norm·cal/(2·thickness)）、3× 口径 overmatch（含已过一层/出射射线强制不跳弹）、等厚即 blocked（严格大于才穿透）、HEAT 间隙距离衰减（含 Gap 层记录）、HE 溅射公式 `0.5·dmg·(1−dist/R) − 1.1·(末层 + min(pen, spaced总和))`（HE 仅命中外部模块 → blocked，复现 BlitzKit thicknessAngled 缺失的 NaN 语义）、装备修正（Calibrated Shells +6%/+7% 穿深、Enhanced Armor +4% 装甲）、穿深仅取 near 值无距离衰减、伤害归属按末层状态（末层被穿透即全额 armor_damage——含仅穿透履带/间隙甲的情形）、出射射线未命中 Primary → 无出射段（null 语义）
- **实时穿透热力图（GPU shader）**：与 BlitzKit 双场景 RT 架构一致——间隙甲/外部模块渲染进带深度纹理的 RT，主装甲着色 shader 读取 RT 叠加；fragment shader 逐行对齐 BlitzKit `PrimaryArmorSceneComponent/shaders/fragment.glsl`（角度表达式、±5% 随机化、跳弹/转正/overmatch、HEAT 衰减、HE 溅射、cyan/magenta 高亮条件、alpha 0.5、greenPenetration/advancedHighlighting/opaque 三模式分支）
- **交互**：点击显示弹道轨迹线 + 逐层接触点标记 + 信息窗口；Shooter/Target 独立选择，多炮塔/多主炮坦克（290 辆）Config 切换同步模型/装甲/弹种

### 定制 6：对局前瞻（阵容强度分析）

`prematch`：输入对方昵称或从回放提取双方阵容，批量查询 WG API，计算阵容平均胜率/场均伤害，识别高威胁玩家与薄弱点，生成开局指引。

### 定制 7：Web GUI 与多会话管理（actor-per-session）

`wotb-agent web` 启动独立 Web 应用（axum），把 CLI 功能全部图形化（Agent 对话/玩家查询/回放扫描/对比/前瞻/坦克百科/配置/用量）。核心是 Agent 对话页：聊天气泡 + 实时进度 + 工具调用轨迹 + 打断按钮。

**会话管理采用 actor-per-session 模式**（`src/web/sessions.rs`）：

- 每个活跃会话一个常驻 tokio 任务（actor）**独占 `Agent` 本体**，HTTP 层经 mpsc 命令通道投递对话指令——同会话对话天然串行，忙时返回 409；消灭 `Option<Agent>` 取出/归还竞态与"删除后被后台任务复活"问题
- **会话级取消**：每会话独立 `CancellationToken`（每轮开始前换新），打断/删除精确到会话；CLI Ctrl+C 全局标志保留为回退
- **磁盘持久化**：每轮对话结束由 actor 落盘 `data/sessions/{id}.json`（复用 CLI 的 `SavedSession` 结构），服务重启后历史自动恢复；非活跃会话直接读盘懒加载（查询历史/导出不实例化 Agent）
- **删除 = cancel + abort + 删文件**：运行中的对话立即终止；事件经共享缓冲供前端轮询增量渲染（升级 SSE 只需换 broadcast channel）

### 定制 8：Agent 工具集（10 个）

| 工具 | 功能 |
|------|------|
| `search_player` / `get_player_stats` | WG API 玩家搜索 / 完整战绩 |
| `scan_replays` / `parse_replay` | 批量扫描聚合 / 单场解析 |
| `compare_replay_vs_api` | 近期回放 vs API 累计对比 |
| `view_tank` | 打开 3D 装甲查看器（名称模糊匹配，多候选时列编号清单询问用户） |
| `get_tank_armor` / `simulate_penetration` | 装甲明细查询 / 击穿模拟（aim 预设/显式层序/入射角/距离/装备开关） |
| `render_heatmap` | 无头浏览器渲染穿透热力图截图（Chrome/Edge 自动探测，WSL 路径转换） |
| `replay_shot` | 射击复现截图（实验性） |

系统提示中内置防幻觉约束：所有坦克数据必须来自工具结果，查询失败时如实报告并请求澄清，禁止编造/替换数据。

## 四、系统架构

```
┌─────────────────────────────────────────────────────────────┐
│              用户界面 (CLI + Web GUI + 3D Viewer)             │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────┐   │
│  │ Agent    │ │ 回放扫描  │ │ 玩家查询  │ │ 3D 模型查看器 │   │
│  │ 对话     │ │ + 报告   │ │ + 对比   │ │ + 装甲分析   │   │
│  │(流式进度)│ │          │ │ + 前瞻   │ │ + 击穿判定   │   │
│  └────┬─────┘ └────┬─────┘ └────┬─────┘ └─────┬────────┘   │
└───────┼────────────┼────────────┼─────────────┼─────────────┘
        │            │            │             │
┌───────▼────────────▼────────────▼─────────────▼─────────────┐
│                      Rust 核心逻辑                            │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────┐   │
│  │ Agent Loop   │  │ Replay Parser│  │ WG API Client    │   │
│  │ (async +     │  │ (wotbreplay- │  │ (战绩查询端点)    │   │
│  │  AgentEvent) │  │  parser+逆向)│  │                  │   │
│  └──────┬───────┘  └──────────────┘  └──────────────────┘   │
│  ┌──────▼───────┐  ┌──────────────┐  ┌──────────────────┐   │
│  │ LLM Client   │  │ Combat Events│  │ Tank Resolver    │   │
│  │ (OpenAI兼容) │  │ Decoder      │  │ (BlitzKit 723)   │   │
│  └──────────────┘  └──────────────┘  └──────────────────┘   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────┐   │
│  │ DVPL Decoder │  │ Snapshot     │  │ Penetration      │   │
│  │ (LZ4_HC)     │  │ Store        │  │ (击穿判定内核)    │   │
│  └──────────────┘  └──────────────┘  └──────────────────┘   │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ Web GUI Server (axum)：会话 actor + 面板 API         │    │
│  │  ┌ SessionManager ── 每会话 actor 独占 Agent          │    │
│  │  │  mpsc 命令通道 + CancellationToken + 磁盘持久化    │    │
│  └──┴────────────────────────────────────────────────────┘   │
└──────────────────────────────────────────────────────────────┘
        │                 │                  │
┌───────▼─────┐  ┌────────▼────────┐  ┌─────▼──────────┐
│ LLM API     │  │ 本地回放文件     │  │ WG API + BlitzKit│
│ (OpenAI 兼容)│  │ (.wotbreplay)   │  │ (tanks.pb       │
│             │  │ + 游戏DVPL文件  │  │  models.pb      │
│             │  │                 │  │  collision.glb) │
└─────────────┘  └─────────────────┘  └────────────────┘
```

**数据流**：用户消息 → HTTP handler → 会话 actor（mpsc）→ Agent Loop（LLM ↔ 工具循环，AgentEvent 写入事件缓冲）→ 前端轮询增量渲染；每轮结束 actor 落盘会话。工具内部按需读取回放文件/游戏数据/调用 WG API。

### 关键数据结构

| 结构 | 字段/内容 | 来源 |
|------|------|------|
| `BattleSummary` | 14 名玩家完整数据、作者统计、地图、模式、时长 | `battle_results.dat` |
| `AggregatedReport` | 胜率、场均伤害、评级变化、坦克/地图统计 | 多场回放聚合 |
| `CombatTimeline` | 生命值变化、死亡事件、累计伤害、射击事件 | `data.wotreplay` 解码 |
| `ShotEvent` | 时间、伤害、目标名、目标剩余血量、是否击杀、复现数据 | 伤害计数器 + 生命值关联 |
| `PlayerStats` | 随机战+排位战统计、mm_rating | WG API |
| `Snapshot` | 带时间戳的 API 数据快照（diff 求阶段变化） | WG API 定期采集 |
| `ArmorModel` / `CollisionData` | 装甲板厚度（含 spaced）、碰撞包围盒、points 偏移 | 游戏 XML/YAML DVPL |
| `TankInfo` | 名称/等级/类型/国家、弹种、俯仰角、spaced 分类 | BlitzKit pb |
| `AgentEvent` | StepStart/ToolCall/ToolResult/Interrupted/Done/Error | Agent Loop |
| `SavedSession` | datetime、model、消息历史、Token 用量（持久化单元） | 会话落盘 |
| `SessionCmd`/`SessionHandle` | actor 命令（Chat）+ 句柄（事件缓冲/历史镜像/取消令牌槽/忙标志） | Web 会话管理 |
| `TokenUsage` | 每次调用记录（时间、模型、Token、费用、用途） | LLM API 响应 |

### API 端点（Web GUI，`wotb-agent web`）

| 端点 | 功能 |
|------|------|
| `POST /api/chat` | 发起对话（命令投递给会话 actor；忙时 409；会话 id 校验） |
| `GET /api/chat/events` | 拉取会话事件流（增量轮询） |
| `POST /api/chat/cancel` | 打断指定会话当前对话（未带 id 回退全局标志） |
| `GET /api/session` · `GET /api/sessions` | 会话历史（懒加载）/ 会话列表（内存 ∪ 磁盘） |
| `DELETE /api/session/{id}` | 删除会话（终止 actor + 删持久化文件） |
| `GET /api/session/{id}/export` | 导出会话为 Markdown |
| `GET/POST /api/config` · `GET /api/usage` | 配置读写 / Token 用量 |
| `GET /api/player/{n}` · `POST /api/scan` · `POST /api/snapshot` · `POST /api/prematch` | 战绩 / 扫描 / 快照 / 前瞻 |
| `GET /api/tanks` · `/api/tank_detail/{id}` · `/api/tank_image/{id}` | 坦克百科 |
| `POST /api/replay/shots` · `POST /api/penetrate` | 射击复现解析 / 击穿判定 |
| `GET /glb/{id}/{f}` · `/api/vendor/{*p}` 等 | 模型与静态资源代理 |

## 五、技术选型

| 模块 | Crate/库 | 版本 | 选择理由 |
|------|-------|------|----------|
| 回放解析 | `wotbreplay-parser` | 0.4 | 现成 MIT crate 解析 ZIP/元数据结构；数据包事件语义未文档化，为自主逆向 |
| HTTP 客户端 | `reqwest` | 0.12 | 纯 Rust TLS（rustls），无需系统 OpenSSL |
| Web 服务器 | `axum` | 0.8 | Tokio 生态原生，类型安全路由/提取器 |
| 并发原语 | `tokio` + `tokio-util` | 1 / 0.7 | actor 任务模型 + `CancellationToken`（会话级取消） |
| CLI 框架 | `clap` | 4 | derive 宏 + 环境变量支持 |
| 序列化 | `serde`/`serde_json` | 1 | Rust 序列化标准 |
| 配置 | `toml` | 0.8 | 人类可读、支持注释 |
| 时间/正则/解压 | `chrono` / `regex` / `flate2` | — | 成熟标准库 |
| Ctrl+C | `ctrlc` | 3 | CLI 中断信号 |
| 浏览器 | `webbrowser` | 1 | 跨平台打开查看器 |
| Protobuf/DVPL 解码 | 自实现 | — | 无现成 crate；按格式规范逆向（参考 Tankerch/DVPL_Converter） |
| LLM 客户端 | 自实现 | — | OpenAI 兼容 REST，无需 SDK，便于精确 Token 统计 |
| 3D 渲染 | Three.js | 0.169 | 浏览器原生 WebGL，GLB 加载 + 自定义 shader 热力图 |
| 图表 | Chart.js | 4.4 | 报告图表（本地 vendored，离线可用） |

## 六、课程要求实现对照（R1–R6）

**R1 核心逻辑用 Rust 实现**：回放解析与战斗事件逆向解码、WG API 客户端、Agent Loop（async + 工具编排）、BlitzKit Protobuf 解析、DVPL 解码器、Token 统计与预算检查、击穿判定内核、3D 查看器后端、Web GUI 后端——见第三节各定制项。

**R2 用户交互界面**：Web GUI（`web` 子命令，六标签页 + Agent 对话 + 3D 查看器）；CLI（`chat` 交互对话等 19 个子命令，含 `combat`、`DumpMethods`/`DumpEntity` 逆向分析工具）。

**R3 可自定义模型配置**：`config.toml` 支持 LLM endpoint/api_key/model/上下文长度/思考模式/价格/max_tokens/预算，WG API key/服务器，回放目录；Web 设置页在线编辑保存，可在 OpenAI 兼容端点（清华 AI 平台/智谱/本地模型）间自由切换。

**R4 实时进度与打断**：批量扫描进度、Agent 工具调用轨迹流式渲染（`AgentEvent` 事件 + 前端增量轮询）；CLI Ctrl+C 全局中断；Web 会话级 `CancellationToken` 打断（每轮换新令牌）。

**R5 上下文历史管理**：多轮对话完整历史（含工具调用轨迹）；CLI `--save/--load` 会话 JSON；Web 多会话管理（新建/切换/删除/导出 Markdown），每轮自动落盘、重启自动恢复（actor-per-session，见定制 7）。

**R6 Token 用量与价格统计**：从 API 响应精确提取 usage，按配置单价换算费用，`usage` 命令与 Web 面板展示明细，Agent Loop 每步检查预算超限自动中断。

**R1+ 射击复现（实验性）**：从回放逆向提取射击事件链（开火/弹着点 counter 精确配对 142/145、实体状态、炮塔朝向），在 3D 查看器按射手视角复现弹道与判定；并对齐 wotinspector 的命中结果语义（结果枚举 86/86 交叉验证、模块损伤位图）。命中位置呈现仍有可见偏差，判定结果仅供参考——逆向细节见 `回放射击事件逆向分析.md`。

## 七、开源库引用

| 库 | 许可证 | 用途 |
|------|--------|------|
| `wotbreplay-parser` | MIT | 回放文件结构解析（事件解码自主开发） |
| `reqwest`/`axum`/`clap`/`serde`/`toml`/`chrono`/`regex`/`flate2`/`ctrlc`/`webbrowser`/`tokio-util` | MIT / Apache-2.0 | 见技术选型表 |
| Three.js / Chart.js（本地 vendored） | MIT | 前端 3D 渲染 / 图表 |
| [Tankerch/DVPL_Converter](https://github.com/Tankerch/DVPL_Converter) | — | DVPL 格式规范参考（自主 Rust 实现） |
| BlitzKit `tanks.pb`/`models.pb`/GLB/图标 | — | 坦克数据唯一来源（723 辆） |
