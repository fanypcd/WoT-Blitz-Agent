# WoTB Blitz Tactics Agent — 设计文档

## 一、产品定位

**WoTB Blitz Tactics Agent** 是一个针对 World of Tanks Blitz（坦克世界闪击战）的 AI 游戏分析助手。它通过解析本地回放文件和调用 Wargaming Public API，为玩家提供精确的阶段性战绩分析、战术复盘和提升建议。

**目标用户**：WoTB 排位赛玩家，希望了解自己近期表现变化、识别薄弱环节、获得数据驱动的改进建议。

## 二、痛点分析

### 痛点 1：看不到近期表现

WG API 只返回账号创建以来的累计数据（总场次 9852、总胜率 58.4%），无法查询"最近一个月排位打得怎么样"。玩家想知道自己最近是否在进步、哪个坦克胜率在拖后腿，但 API 不支持按时间范围查询。

**现有工具的不足**：blitzstars.com 被 Cloudflare 保护无法程序化访问；游戏内只显示当前评级，不提供历史趋势。

### 痛点 2：回放文件是黑盒

游戏每场自动保存 `.wotbreplay` 回放文件（二进制格式），但没有官方工具批量解析。玩家无法提取"最近 100 场的场均伤害趋势""哪个地图胜率最低""每场评级变化曲线"。

### 痛点 3：通用 Agent 无法获取游戏数据

ChatGPT/Claude 无法解析 `.wotbreplay` 二进制文件，不知道 WG API 的 25 个端点怎么调用，无法从游戏文件中提取 DVPL 压缩格式的装甲数据。

### 痛点 4：战斗过程不可见

玩家打完一场不知道"哪一炮打到了谁""什么时候被击杀的""全场伤害是怎么积累的"。回放文件中包含完整的数据包流（每场 4.5万~7.5万个包），但需要逆向解码二进制格式。

## 三、场景定制方案

### 定制 1：回放解析 + 战斗事件解码

回放文件的结构化解析依赖第三方 Rust crate `wotbreplay-parser`（MIT, v0.4.2, 作者 eigenein），它负责读取 `.wotbreplay` ZIP 包并提取 `meta.json` 和 `battle_results.dat` 中的结构化字段（14 名玩家战绩）。

在此基础上，以下为自主开发的定制逻辑：

- **数据包流解码**：`wotbreplay-parser` 仅提供原始数据包（`data.wotreplay` 中 4.5 万~7.5 万个 `Unknown` 类型包），包内事件子类型的语义未文档化。本 Agent 自行逆向解码 `Unknown(type=7)` 包中的 6 种事件子类型（生命值变化 sub=3、死亡事件 sub=1、累计伤害计数器 sub=10 等），重建生命值时间线、死亡事件、累计伤害曲线
- **射击事件推断**：关联作者累计伤害计数器（sub=10）递增和敌方生命值下降（sub=3），推断每发射击的目标、伤害和击杀。已验证：11 次有效射击、10 次命中、2 次击杀，总伤害 4338 精确匹配
- **Entity ID → 玩家名映射**：从 type=5 数据包中提取（偏移量57处的长度前缀ASCII字符串），已验证全部14名玩家正确映射

### 定制 2：API 数据快照 + 回放 vs 累计对比

- **快照系统**：定期从 WG API 采集玩家累计数据存为带时间戳的 JSON 快照，通过快照差值实现阶段性分析（API 不支持按时间查询，只能靠快照差值计算）
- **回放 vs API 对比**：扫描本地回放获取近期精确数据，与 API 累计数据对比，量化"近期表现 vs 历史平均"的差异
- **实测结果**（玩家 Anonyme，106场排位回放）：
  - 胜率：近期 68.9% vs 累计 58.4%（+10.5%）
  - 场均伤害：近期 3670 vs 累计 2746（+33.7%）
  - 评级变化：+2116（4638→6754）

### 定制 3：BlitzKit 数据集成

从 BlitzKit 的公开数据文件中提取完整坦克数据：

| 数据文件 | 内容 | 解析方式 |
|----------|------|----------|
| `tanks.pb` | 723 辆坦克的名称、tier/类型/国家/血量、弹种、穿深、伤害、炮塔数据、装填（弹夹/弹鼓） | **坦克数据源，运行时直接 Protobuf 解析** |
| `models.pb` | 炮塔/主炮→模型节点（gun/turret_0X）权威映射，多炮塔/多配置正确切换 | **模型数据源，运行时直接 Protobuf 解析** |
| `models.pb` | 723 辆坦克的装甲板厚度 + 炮管俯仰角 | Protobuf 解析 → `armor_cache.json` + `gun_angles.json` |
| `collision.glb` | 装甲板 3D 几何模型（每个 mesh = 一块装甲板，node 名含 plate ID） | Three.js 直接加载 |
| `model.glb` | 视觉模型（带贴图） | Three.js 直接加载 |

**tanks.pb 弹种数据结构**（从 Protobuf 解析）：

| field | 含义 | 示例 |
|-------|------|------|
| 1 | shell ID | 537361 |
| 2 | 本地化名称 | "APFSDS" (en) |
| 5 | 伤害 | 150 |
| 6 | 口径 | 105.0 |
| 7 | 弹种类型 | "ap_cr" / "he" / "hc_premium" |
| 8 | 穿透力（嵌套，sub-field 1） | 236.0 |

**models.pb 炮管俯仰角结构**（field 4 → field 5 → field 2 → field 4）：

| sub-field | 含义 | 示例 |
|-----------|------|------|
| 4.1 | 仰角（负值取绝对值） | -20.0 → 仰角 20° |
| 4.2 | 俯角 | 6.0 |

验证：IS-6 仰角 20°/俯角 6°、WZ-131G FT 仰角 15°/俯角 5°，与 WG API 完全匹配。

### 定制 4：DVPL 游戏文件解码

DVPL 是 Wargaming 游戏的本地文件压缩格式。本 Agent 参考 [Tankerch/DVPL_Converter](https://github.com/Tankerch/DVPL_Converter) 的格式规范（末尾 20 字节 footer：input_size + compressed_size + CRC32 + compression_type + "DVPL" magic），用 Rust 自主实现解码器（含 LZ4_HC 块解压），解析游戏本地文件提取装甲板厚度（hull/turret/gun `<armor_N>` 标签 + chassis `<leftTrack>`/`<rightTrack>` + gun `<gun>` 炮管装甲值）。

同时从 YAML DVPL 文件的 `collision` 节提取各部件的 `points` 偏移数据，用于 3D 查看器中炮塔和炮管的定位。

### 定制 5：3D 装甲查看器与击穿分析

**双模型绑定**：同时加载 `collision.glb`（隐藏，仅用于 raycast 获取装甲板数据）和 `model.glb`（可见，展示外观）。`model.glb` 中还包含履带（`chassis_track_L/R`）、炮管（`gun_01`）、炮根（`gun_01_mask`）等模块 mesh，从中提取并纳入 raycast 目标，配合游戏 XML DVPL 的 `<leftTrack>`/`<rightTrack>` 和 `<gun>` 装甲值参与穿透判定。

**炮塔旋转和炮管俯仰**：使用矩阵旋转法，存储炮塔/炮管节点的原始矩阵，通过 `translate(pivot) * rotate(angle) * translate(-pivot) * origMatrix` 计算新矩阵。枢轴（pivot）优先从**视觉模型几何推导**（`computePivots`：炮塔取 `turret_0X` 组包围盒中心、炮管取离炮塔最近端即炮根），缺 points 的坦克（如 E-100）也能正确绕自身中心旋转不漂移；有 `turret_points`/`gun_points` 时作为回退。俯仰角限制来自 `gun_angles.json`。visual 和 armor 模型同步旋转，确保 raycast 结果正确。

**装甲模块对齐**：部分坦克（如 E-100）的 `collision.glb` 炮塔/炮管 armor 缺少定位数据，`alignArmorModules()` 按视觉模型对应 `turret_0X`/`gun_0X` 组的世界包围盒中心，将碰撞模型的 armor 组整体平移对齐，保证点击判定命中正确部位。

**坦克与配置切换**：Shooter（射击坦克，决定口径/弹药）与 Target（受击坦克，显示模型/装甲）可独立选择，图形化筛选器支持封面图网格 + 等级/国家/类型筛选 + 名称搜索（数据来自 `/api/tank_filter`）。多炮塔/多主炮坦克（290 辆）提供 Config 选择器（如 E-100 的 12.8cm/15cm 双炮），切换时同步更新：火炮模型（`gun_0X` 组显隐，含炮根 mask 跟随旋转）、装甲判定（`gun_0X_armor_*` 分组显隐）、弹种与穿深。3D 模型经 `/glb/{id}/` 代理按需缓存到 `glb_cache/`。

**统一击穿判定（Rust `penetration.rs`）**：前端点击时 raycast 收集所有命中，通过 `POST /api/penetrate` 发送到 Rust 后端，由 `penetration::calculate()` 统一计算：

1. **主装甲盒概念**：炮弹必须**最终穿透 hull/turret 主装甲**才算 `PENETRATION`；穿透炮管安装甲等间隙甲不算击穿坦克（间隙甲只消耗穿深、层收集在第一块主装甲处停止）
2. **跳弹**：入射角 ≥ 跳弹角（AP/APCR 70°；HEAT/HE 强制 90° 永不跳弹），仅当炮弹未穿过外部装甲时检查；跳弹后剩余穿深 ×0.75 并沿反射方向二次判定
3. **转正**：按弹种数据（AP 5°、APCR 2°、HEAT/HE 0），仅当炮弹未穿过外部装甲且未跳弹时施加
4. **Overmatch**：口径 > 3 倍厚度 → 强制不跳弹；口径 > 2 倍厚度 → 增强转正（`1.4 * norm * caliber / (2 * thickness)`）
5. **外部模块**（履带+负重轮/炮管）：flat 抵消穿透力，无角度/转正/跳弹，按 variant 去重；**间隙甲（spaced）**：角度等效消耗、可跳弹转正（对齐 BlitzKit 非外部分支），不阻止判定
6. **多层穿透**：逐层消耗穿透力，穿透主装甲后停止（不计算穿出）；HE 不提前终止（供溅射计算）
7. **HE 弹**：溅射判定（见定制 6），无转正无跳弹

装甲分类：**models.pb 的 `Armor.spaced` 为权威**（`/api/tank` 载荷 `hull_spaced` + 每配置 `turret_spaced`/`gun_spaced`）——炮管安装甲普遍标记 spaced（穿透它不算击穿坦克），XML DVPL 的 `vehicleDamageFactor=0` 作为回退来源。

**弹道可视化**：点击后显示 TubeGeometry 轨迹线（绿/红/橙），每层接触点小球标记，HTML 信息窗口（固定大小，3D→屏幕投影定位，每帧跟随摄像机）。拖曳检测区分旋转视角和点击操作。

**Collision 模式**：Show Collision 按钮按基础厚度着色显示装甲板（绿<30mm → 黄<60 → 橙<100 → 红<200 → 暗红≥200）。

> **实时穿透热力图（GPU shader，对齐 BlitzKit 双场景 RT 架构）**：早期的"逐顶点路径预计算"方案已废弃；现行实现与 BlitzKit `Armor/` 组件一致——`spacedArmorScene`（主装甲 omit/renderOrder 0、间隙甲 additive 2 + 深度 5、外部模块深度 3 + additive 4）渲染进 `spacedArmorRenderTarget`（含 DepthTexture），`primaryArmorScene`（exclude 0 + 着色 shader 1，读 RT）在 `clearDepth()` 后叠加到视觉坦克上。每帧时序、着色数学（三倍口径/两倍口径转正/跳弹/±5% 随机化/HE 溅射/HEAT 间隙衰减）、装甲分类与装备系数均对齐 BlitzKit。注意：three.js 会按 `material.transparent` 拆分渲染列表，RT 内材质不可设 `transparent:true`（否则 renderOrder 顺序失效、层叠消耗丢失——BlitzKit 的 RT 材质同样保持 opaque）。间隙距离按 `metersPerUnit` 换算回米（BlitzKit 模型原生单位=米）。

点击精判始终以 Rust `penetration.rs` 后端为准；热力图为同一套机制的实时预览。

### 定制 6：击穿判定增强（HEAT / HE 溅射 / 装备修正 / 伤害区分）

在 `penetration.rs` 中补齐与 BlitzKit 对齐的机制：

- **HEAT 间隙衰减**：穿透层与层之间跨越空气间隙时，剩余穿深按距离衰减 `remaining -= 0.5 * remaining * distance`（HEAT 弹种特有；热力图 shader 内同机制，距离经 `metersPerUnit` 换算为米）
- **HE 溅射伤害**：炮弹出膛后按 `0.5 * damage * (1 - dist/explosionRadius) - 1.1 * (核心板等效 + 累计 spaced 装甲厚度)` 计算溅射伤害，结果输出为 `SPLASH` 状态；`explosionRadius` 来自 tanks.pb shell field 12（米，643 发 HE 弹全部解析）
- **装备修正**：Calibrated Shells（ID 103）穿深 **+6%（AP/APCR）/ +7%（HEAT/HE）**、Enhanced Armor（ID 110）装甲厚度 +4%，通过请求字段 `calibrated_shells`/`enhanced_armor` 控制（3D 查看器提供 Equip 开关，同时作用于点击判定与热力图 uniforms）
- **血量伤害 vs 模块伤害区分**：BlitzKit `tanks.pb` 的 shell 消息含两个伤害字段（field 4 = HP/alpha 伤害，field 5 = 模块伤害），解析时分别提取。判定结果区分（以最后一层状态为准，对齐 BlitzKit lastLayer.status）：**最终穿透主装甲盒** → 输出血量伤害（如 E-100 12.8cm AP 460）；**最后一层为可破坏模块（履带/炮管）且穿透** → 输出模块伤害（180）；**仅穿透间隙甲后被主装甲阻挡** → 0 伤害。结果弹窗显示 `HP Dmg 460` / `Module Dmg 180 (HP 460)`

### 定制 7：对局前瞻（阵容强度分析）

新增 `prematch` CLI 子命令，进入对局前分析双方玩家阵容：

- 输入对方玩家昵称（逗号分隔，或 `--file` 每行一个），批量查询 WG API 累计战绩
- 计算阵容平均胜率、平均场均伤害
- 识别高威胁玩家（场均伤害超均值 1.2 倍）和可针对弱点（胜率低于均值 0.95 倍）
- 生成开局指引：重点规避最强者、优先打击最弱者与薄弱点

### 定制 8：Web 图形界面（独立 Web 应用）

`wotb-agent web` 启动独立 Web 应用（axum，`src/web/` 模块），把 CLI 功能全部图形化：

- **Agent 对话页（核心）**：聊天气泡 + 实时进度（LLM 步骤 N/M）+ 工具调用轨迹（调用名/参数/结果折叠展示）+ 打断按钮。Agent Loop 重构为 async（`Agent::chat_async`），通过 `AgentEvent` 枚举（StepStart/ToolCall/ToolResult/Interrupted/Done/Error）流式上报事件，前端轮询 `/api/chat/events` 增量渲染；CLI 的 `chat` 命令通过阻塞包装复用同一逻辑并镜像事件到 stderr
- **功能面板**：玩家战绩查询、回放扫描聚合报告（胜率/场均伤害/坦克/地图统计）、回放 vs API 对比、对局前瞻、模型配置编辑（Endpoint/Key/上下文/思考模式/预算，写入 config.toml）、Token 用量统计（调用明细 + 预算进度条）
- **架构**：会话以 `session_id` 区分，每会话独立 `Agent` 实例（tokio Mutex 保护，跨 await 持锁保证单会话串行）；业务逻辑与 CLI 共用同一套服务层函数（WG API 客户端/回放扫描/prematch），阻塞调用经 `spawn_blocking` 包装

### 定制 9：3D 查看器作为 Agent 工具（view_tank 等查看类工具）

Agent 可自主调用查看类工具：
- `view_tank`：用户说"查看 E100 的装甲模型"时，Agent 解析坦克名称/ID → 后台线程启动 3D 查看器服务器并自动打开浏览器，随后结合数据给出装甲弱点分析。工具内部按名称模糊匹配 `tank_cache.json`（精确名优先，其次最短子串匹配）。
- `render_heatmap`：无头浏览器渲染热力图截图保存 PNG（Chrome/Edge 自动探测，WSL 路径自动转换），供 Agent 在对话中输出装甲分析配图。
- `replay_shot`（实验性）：射击复现截图——解析回放指定射击，3D 查看器按射手视角/受击姿态呈现后无头截图。

此外 Agent 还可调用 `get_tank_armor`（装甲明细查询）与 `simulate_penetration`（击穿模拟，支持 aim 预设/显式层序/入射角/交战距离/装备开关），共 10 个工具。


## 四、系统架构

```
┌─────────────────────────────────────────────────────────────┐
│              用户界面 (CLI + Web GUI + 3D Viewer)             │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────┐   │
│  │ Agent    │ │ 回放扫描  │ │ 玩家查询  │ │ 3D 模型查看器 │   │
│  │ 对话     │ │ + 报告   │ │ + 对比   │ │ + 装甲分析   │   │
│  │(流式进度)│ │          │ │ + 前瞻   │ │ + 击穿判定   │   │
│  └────┬─────┘ └────┬─────┘ └────┬─────┘ └─────┬────────┘   │
└───────┼────────────┼───────────┼─────────────┼─────────────┘
        │            │           │             │
┌───────▼────────────▼───────────▼─────────────▼─────────────┐
│                      Rust 核心逻辑                           │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────┐  │
│  │ Agent Loop   │  │ Replay Parser│  │ WG API Client     │  │
│  │ (async +     │  │ (wotbreplay- │  │ (25 endpoints)   │  │
│  │  AgentEvent) │  │  parser)     │  │ (仅战绩查询)      │  │
│  └──────┬───────┘  └────┬────────┘  └────────┬─────────┘  │
│         │                 │                    │            │
│  ┌──────▼───────┐  ┌─────▼────────┐  ┌───────▼──────────┐  │
│  │ LLM Client   │  │ Combat Events │  │ Tank Resolver    │  │
│  │ (async,      │  │ Decoder       │  │ (from_blitzkit   │  │
│  │  OpenAI API) │  │               │  │  723 BlitzKit)   │  │
│  └──────────────┘  └──────────────┘  └──────────────────┘  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────┐  │
│  │ DVPL Decoder │  │ Snapshot     │  │ 3D Viewer        │  │
│  │ (LZ4_HC)     │  │ Store        │  │ (axum + Three.js) │  │
│  │ + points     │  │              │  │ + 装甲穿透判定)   │  │
│  └──────────────┘  └──────────────┘  └──────────────────┘  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────┐  │
│  │ Prematch     │  │ Penetration  │  │ BlitzKit pb      │  │
│  │ (阵容分析)    │  │ (击穿判定+    │  │ 解析 (blitzkit.rs│  │
│  │              │  │  HP/模块伤害) │  │  元数据+图标)     │  │
│  └──────────────┘  └──────────────┘  └──────────────────┘  │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ Web GUI Server (src/web: SSE 事件 + 会话 + 面板 API) │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
        │                 │                  │
┌───────▼─────┐  ┌────────▼────────┐  ┌─────▼──────────┐
│ LLM API     │  │ 本地回放文件     │  │ WG API + BlitzKit│
│ (GLM-5 /    │  │ (.wotbreplay)   │  │ (tanks.pb       │
│  OpenAI等)  │  │ + 游戏DVPL文件  │  │  models.pb      │
│             │  │                 │  │  collision.glb  │
│             │  │                 │  │  model.glb      │
│             │  │                 │  │  icons webp)    │
└─────────────┘  └─────────────────┘  └────────────────┘
```

### 关键数据结构

| 结构 | 字段 | 来源 |
|------|------|------|
| `BattleSummary` | 14名玩家完整数据、作者统计、地图、模式、时长 | `battle_results.dat` |
| `AggregatedReport` | 胜率、场均伤害、评级变化、坦克使用统计、地图统计 | 多场回放聚合 |
| `CombatTimeline` | 生命值变化、死亡事件、累计伤害、射击事件推断 | `data.wotreplay` 解码 |
| `ShotEvent` | 时间、伤害、目标名、目标剩余血量、是否击杀 | 伤害计数器 + 生命值关联 |
| `PlayerStats` | 随机战+排位战完整统计、mm_rating、显示评级 | WG API |
| `Snapshot` | 带时间戳的API数据快照 | WG API 定期采集 |
| `ArmorModel` | hull/turret/gun/chassis 装甲板厚度 + primaryArmor映射 | 游戏XML DVPL |
| `CollisionData` | hull/turret/gun/chassis 碰撞包围盒 + 平均厚度 + points偏移 | 游戏YAML DVPL |
| `TankInfo` | 名称、等级、类型、国家、装甲、弹种（穿深/血量伤害/模块伤害）、俯仰角 | BlitzKit pb 构建 |
| `AgentEvent` | StepStart/ToolCall/ToolResult/Text/Interrupted/Done/Error（流式事件） | Agent Loop |
| `TokenUsage` | 每次API调用记录（时间、模型、输入/输出Token、费用、用途） | LLM API 响应 |

### 3D 查看器 API 端点

| 端点 | 功能 |
|------|------|
| `GET /` | 返回 HTML 页面（Three.js 前端） |
| `GET /api/tank/{tank_id}` | 指定坦克数据（元数据、装甲、configs 配置列表、弹种、俯仰角、模型URL） |
| `GET /api/tank_list` | 723 辆坦克 ID→名称映射（来自 `tanks.pb` 唯一数据源） |
| `GET /api/tank_filter` | 坦克富元数据列表（id/name/tier/nation/type，供筛选器） |
| `GET /api/tank_image/{id}` | 坦克封面图（`tank_images/` 缓存，缺则从 BlitzKit 下载） |
| `GET /api/shells/{tank_id}` | 指定坦克的弹种数据（穿深、伤害、类型，来自 `tanks.pb` 唯一数据源） |
| `GET /glb/{tank_id}/{file}` | 3D 模型代理（`glb_cache/` 缓存，缺则从 BlitzKit CDN 下载） |
| `POST /api/penetrate` | 统一击穿判定（raycast 命中列表 + 弹种参数 → 结果/层数/角度/伤害） |

### Web GUI API 端点（`wotb-agent web`）

| 端点 | 功能 |
|------|------|
| `POST /api/chat` | 发起对话（spawn Agent 异步循环，事件写入会话） |
| `GET /api/chat/events` | 拉取会话事件流（StepStart/ToolCall/ToolResult/Done…） |
| `POST /api/chat/cancel` | 打断当前会话任务 |
| `GET /api/session` | 会话消息历史 |
| `GET/POST /api/config` | 读取/保存模型配置（写入 config.toml） |
| `GET /api/usage` | Token 用量统计 |
| `GET /api/player/{nickname}` | WG API 玩家战绩 |
| `POST /api/scan` | 回放扫描聚合报告 |
| `POST /api/snapshot` | 快照 take/list/diff |
| `POST /api/prematch` | 对局前瞻 |
| `GET /api/vendor/{*path}` | 前端静态资源（Chart.js 等） |

## 五、技术选型

| 模块 | Crate | 版本 | 选择理由 |
|------|-------|------|----------|
| 回放解析 | `wotbreplay-parser` | 0.4.2 | 第三方 crate（MIT），解析 `.wotbreplay` ZIP 包结构，数据包事件解码为自主逆向 |
| HTTP 客户端 | `reqwest` | 0.12 | 纯 Rust TLS (rustls)，无需系统 OpenSSL |
| Web 服务器 | `axum` | 0.8 | Tokio 生态原生，轻量，支持路径参数路由 |
| CLI 框架 | `clap` | 4 | Rust 标准 CLI 库，支持 derive + 环境变量 |
| 序列化 | `serde`/`serde_json` | 1 | Rust 序列化标准 |
| 配置 | `toml` | 0.8 | 人类可读配置格式，支持注释 |
| 时间 | `chrono` | 0.4 | 时区处理和格式化 |
| 正则 | `regex` | 1 | XML 装甲数据解析 |
| 解压 | `flate2` | 1 | zlib 解压（DVPL type=3） |
| Ctrl+C | `ctrlc` | 3 | R4 中断功能 |
| 浏览器 | `webbrowser` | 1 | 跨平台浏览器调用（3D查看器） |
| LLM 客户端 | 自实现 | — | OpenAI 兼容 API，无需额外 SDK |
| DVPL 解压 | 自实现 | — | 参考 [Tankerch/DVPL_Converter](https://github.com/Tankerch/DVPL_Converter) 格式规范，纯 Rust LZ4_HC |
| 3D 渲染 | Three.js | 0.169 | 浏览器原生 WebGL，从 BlitzKit CDN 加载 GLB 模型（本地 vendored + CDN 回退） |
| 图表 | Chart.js | 4.4.1 | Web GUI 报告图表（本地 vendored，离线可用） |
| Protobuf 解析 | 自实现 | — | 解析 BlitzKit `tanks.pb`（元数据 + 弹种 HP/模块伤害）和 `models.pb` |

## 六、R1-R6 实现说明

### R1. 核心逻辑用 Rust 实现

全部核心逻辑使用 Rust 编写，覆盖：
- 回放解析（`wotbreplay-parser` 集成 + meta/battle_results 提取）
- 战斗事件解码（type=7 数据包解析 + 射击事件推断）
- WG API 客户端（25个端点封装，仅战绩查询；坦克数据全部来自 BlitzKit）
- Agent Loop（async，LLM 调用 + 工具编排 + 对话管理 + AgentEvent 流式事件）
- BlitzKit `tanks.pb` Protobuf 解析（元数据 tier/类型/国家/血量 + 弹种血量/模块伤害 + 封面图批量下载）
- DVPL 格式解码（LZ4_HC 解压 + YAML/XML 解析 + points 偏移提取 + spaced `vehicleDamageFactor` 识别）
- Token 统计（精确记录 + 价格换算 + 预算检查）
- 坦克数据构建（`from_blitzkit`：从本地 BlitzKit 数据构建 723 辆坦克解析器，无需 WG API）
- dev_name 自动映射（从 `tanks.pb`（唯一数据源）的 dev_name 字段构建 tank_id → 游戏文件名映射）
- 3D 查看器后端（axum Web 服务器：坦克数据/配置列表、弹种查询、坦克筛选、封面图与模型缓存代理、击穿判定）
- Web GUI 后端（`src/web/`：Agent SSE 事件流、会话管理、各功能面板 API）
- 统一击穿判定模块（`penetration.rs`：跳弹/转正/overmatch/多层/spaced/HE + HP/模块伤害区分，通过 `POST /api/penetrate` 暴露）

### R2. 用户交互界面

- **Web 图形界面**：`web` 命令启动独立 Web 应用（Agent 对话 + 玩家/回放/对比/前瞻面板 + 配置 + Token 用量）
- **CLI 交互式终端**：`chat` 命令支持多轮对话，`history` 查看历史，`usage` 查看 Token 统计
- **3D 查看器**：`view` 命令启动本地 HTTP 服务器，浏览器中显示：
  - 3D 坦克模型（visual model + collision armor model 双模型绑定 + module mesh 集成 + 装甲模块对齐）
  - 炮塔旋转 + 炮管俯仰（右键拖拽，枢轴从视觉模型几何推导，旋转不漂移；炮根 mask 跟随旋转；角度限制来自 `gun_angles.json`）
  - 多层穿透判定（跳弹/转正/overmatch/模块装甲/spaced/逐层消耗，Rust 后端统一计算）
  - 弹道可视化（TubeGeometry 轨迹线 + 接触点标记 + HTML 信息窗口，固定大小跟随摄像机）
  - Collision 模式（按基础厚度着色显示装甲板）
  - 弹种选择器 + 伤害区分显示（穿透=HP Dmg，模块命中=Module Dmg）
  - 坦克/配置切换（Shooter/Target 独立选择 + 图形化筛选器 + 多炮塔/多主炮 Config 切换）
  - 射击复现入口（实验性）：`?shot=N` URL 参数——相机按射手真实相对方位/俯仰放置、受击坦克炮塔/炮管按回放时刻姿态应用、模型上标注弹着点、热力图就绪后沿弹道自动穿透判定（见 R1+）

### R3. 可自定义模型配置

`config.toml` 配置文件，支持：
- WG API：`application_id`、`server`（asia/eu/na）
- LLM：`endpoint`、`api_key`、`model`、`context_length`、`thinking_mode`、`price_input_per_1k`、`price_output_per_1k`、`max_tokens`、`budget`
- 回放：`replay_dir`、`tank_cache_path`

用户可在 OpenAI、本地模型、清华 AI 平台之间自由切换（改 endpoint 即可）；Web GUI 设置页可直接编辑并保存配置。

> **测试回放**：项目附带 `replay_samples/` 目录（3 个示例 `.wotbreplay`，随仓库分发）。将 `replay_dir` 设为 `replay_samples` 即可在无需安装游戏的情况下测试 `scan` / `single` / `compare` 及 Web 的 Replay Scan 面板。Web 界面亦支持直接输入 Windows 回放路径（如 `C:/Users/.../replays`），后端在 WSL 下自动将其转为 `/mnt/...` 访问；Linux / WSL 则用 `/mnt/...` 原样路径。

### R4. 实时进度渲染和打断功能

- **批量扫描进度**：`[1/129] filename.wotbreplay` 实时更新
- **Agent 工具调用进度**：`[Agent] Calling LLM (step 1/5)...` → `[Agent] Tool call: search_player` → `[Agent] Executing... (Ctrl+C to interrupt)` → `[Agent] Done (388 chars)`
- **Web GUI 流式进度**：Agent Loop 重构为 async，通过 `AgentEvent` 事件流（StepStart/ToolCall/ToolResult/Done）推送到前端，实时渲染 LLM 步骤与工具调用轨迹，带打断按钮
- **Ctrl+C 中断**：`ctrlc` crate 设置原子标志，Agent Loop 每步检查，被中断时停止并返回 `[Interrupted by user]`

### R5. 上下文历史管理

- **多轮对话**：messages 列表维护完整对话历史（系统提示 + 用户消息 + LLM回复 + 工具调用 + 工具结果）
- **查看历史**：`history` 命令显示所有对话和工具调用轨迹
- **保存会话**：`--save session.json` 保存完整上下文为 JSON（含系统提示、用户消息、LLM回复、工具调用、工具结果、Token统计）
- **加载会话**：`--load session.json` 加载历史会话，LLM 能记住之前的对话内容（已验证：加载后问"我上次问了什么"，Agent正确回忆）

### R6. Token 用量与价格统计

- **精确统计**：从 API 响应 `usage.prompt_tokens` 和 `usage.completion_tokens` 获取
- **价格换算**：`cost = input_tokens / 1000 * price_input_per_1k + output_tokens / 1000 * price_output_per_1k`
- **界面展示**：`usage` 命令显示总调用数、总Token、总费用、最近10次调用明细（时间、模型、输入/输出Token、费用、用途）
- **预算中断**：`check_budget()` 函数在 Agent Loop 每步检查，超预算时自动中断

### R1+. 射击复现（回放 → 3D 查看器）【实验性 · 不完善】

> **⚠ 状态标注（2026-09-06）：本功能为实验性实现，命中位置计算尚不完善，
> 判定结果与游戏内回放存在可见偏差，不建议作为准确的击穿分析依据。**
> 数据解析层（事件匹配/姿态/弹道两点）已达到确定性配对级别，
> 主要偏差在 viewer 端命中点呈现环节。

将回放中的每次射击事件在 3D 装甲查看器中复现（独立技术文档：
**`回放射击事件逆向分析.md`**）：

**已可靠解析的数据（Rust 端，确定性配对）：**
- 开火事件 `type=8 0x1d`：射手 eid + `fire_counter`（u32 计数器）
- 弹着点 `type=8 0x14`：**counter 精确配对**（142/145 验证，dt=0），
  args = [counter u32][弹着点世界坐标 3×f32]
- 目标实体：`infer_shots` 按"伤害数值最接近"匹配（`target_eid` 直传，
  修复了交火场景下"时间最近"误选他车实体的 bug）
- 实体状态 `type=10`：位置（x=东/y=接地高度/z=南，米）+ 车体 yaw/pitch/roll
- 炮塔朝向 `type=7 sub2`（u16，`raw/65535×2π−π`，相对车体 + hullYaw = 绝对）
- 弹道两点 `ball_a`（射手@开火）/ `ball_b`（弹着点）：弹道方向与射手炮塔
  指向夹角 9/10 发 ≤1.1°（轴映射全直通的依据）
- 相机高度：按真实俯仰角 × 压缩后距离（修复距离 clamp 导致俯仰角放大 1.7 倍）

**已知不完善项（viewer 端）：**
- **命中位置偏差**：射线 raycast 的落点与游戏内实际命中装甲板存在偏移——
  0x14 弹着点为穿透后终点（过穿弹在目标另一侧），表面命中点依赖 raycast
  截断，轴映射/基准点选取仍有残余误差
- 受击坦克坡度倾斜（hull pitch/roll）未应用到模型呈现
- 炮管俯仰：受击者数据部分射击缺失（type=32 覆盖不全），回退 hull pitch
- 标记球位置在部分射击下偏离模型表面

**Web Replay 页**：Shot Replay 卡片，点击行打开 3D 查看器复现该发
（相机 = 射手真实相对方位/俯仰放置；受击坦克炮塔/炮管按回放时刻姿态应用；
热力图就绪后自动穿透判定——结果仅供参考）。

**`combat` CLI**：`--json` 输出射击事件 + 复现数据，`--shots-json` 落盘。
（逆向期的 `--dump` 包分析工具已完成使命并移除，分析结论沉淀在
`回放射击事件逆向分析.md`。）

**Agent 工具 `replay_shot`**：无头浏览器截图 + `/screenshots/` 路由。

## 七、开源库引用

| 库 | 许可证 | 用途 |
|------|--------|------|
| `wotbreplay-parser` | MIT | WoTB 回放文件结构解析（数据包事件解码为自主开发） |
| `reqwest` | MIT/Apache-2.0 | HTTP 客户端 |
| `axum` | MIT | Web 服务器 |
| `clap` | MIT/Apache-2.0 | CLI 框架 |
| `serde`/`serde_json` | MIT/Apache-2.0 | 序列化 |
| `toml` | MIT/Apache-2.0 | 配置文件解析 |
| `chrono` | MIT/Apache-2.0 | 时间处理 |
| `regex` | MIT/Apache-2.0 | 正则表达式 |
| `flate2` | MIT/Apache-2.0 | zlib 解压（DVPL type=3） |
| `ctrlc` | MIT/Apache-2.0 | Ctrl+C 信号处理 |
| `webbrowser` | MIT/Apache-2.0 | 浏览器调用 |
| Three.js (本地 vendored + CDN 回退) | MIT | 3D 模型渲染（前端） |
| Chart.js (本地 vendored) | MIT | Web GUI 报告图表 |
| [Tankerch/DVPL_Converter](https://github.com/Tankerch/DVPL_Converter) | — | DVPL 格式规范参考（自主用 Rust 实现） |
| BlitzKit `tanks.pb` | — | 723 辆坦克完整数据（名称、tier、类型、国家、弹种、穿深、血量/模块伤害） |
| BlitzKit `models.pb` | — | 723 辆坦克装甲板厚度 + 炮管俯仰角 + 三级装甲 spaced 分类（穿透判定权威） |
| BlitzKit tank icons | — | 723 辆坦克封面图（big.webp） |
| BlitzKit `collision.glb` | — | 装甲板 3D 几何模型（每个 mesh = 一块装甲板） |
| BlitzKit `model.glb` | — | 视觉模型（带贴图） |
