# 客户端弹道与命中位置逆向报告

> 分析对象：Steam PC 版 wotblitz.exe（32 位 PE，2026-09-02 build，image base 0x400000）。
> 方法：RTTI TypeDescriptor→COL→vtable 定位类；objdump 定向反汇编；浮点常量释义；
> 结论用项目样本回放（J39/J20/XM551/GB109）与现有探针交叉验证。
> 证据分级同前两篇：**[实证]** = 反汇编直接证据；**[对照]** = 与实测/探针对照；**[推断]** = 待验证。
> 关联：《回放射击事件逆向分析.md》（协议层）、《游戏回放数据处理分析报告.md》（处理入口总表）。
> 本文回答三个问题：①弹道（示踪线）客户端怎么算；②命中位置（弹孔）客户端怎么算；
> ③射击事件从包到表现的完整处理链。

---

## 一、弹道/示踪线：MovementStrategy 类簇 [实证]

### 1.1 类体系与 vtable

RTTI 定位到 6 个弹道运动策略类，同一接口（10 槽 vtable），槽 2（每帧推进）按弹种分实现：

| 类 | vtable | 槽[2]（每帧推进） | 槽[8] | 语义 |
|---|---|---|---|---|
| `ShellMovementStrategy` | 0x3721F14 | 0x1722E40 | 0x6AFF60 | 普通炮弹 |
| `GuidedMovementStrategy` | 0x3721F40 | 0x17228C0 | 0x8A59A0 | ATGM 制导（槽[4]也独有 0x171F820） |
| `TorpedoMovementStrategy` | 0x3721F6C | 0x1722E40 | 0x8A3530 | 鱼雷（特殊模式） |
| `LightningMovementStrategy` | 0x3721F98 | 0x1722A30 | 0x8A0B60 | 雷击（showLightning） |
| `RailgunMovementStrategy` | 0x3721FC4 | 0x1722B90 | 0x8570A0 | 电轨炮（特殊模式） |
| `TracerMovementStrategy` | 0x36BDFA0 | （槽 2..8 同一桩 0x2528D8D0） | — | 基类/公共实现 |

继承关系由构造函数的 vtable 写入序列实证：4 个构造函数都先写 Shell vtable
（0x17113AB→Lightning ctor @0x1711370、0x17117D0→Railgun ctor @0x1711790、
0x1712832→Torpedo ctor @0x17127F0、Guided ctor @0x17110B0），再写各自派生 vtable——
**Guided/Torpedo/Lightning/Railgun 均直接派生自 ShellMovementStrategy**。
基类构造 @0x1712E10，删除式析构显示对象大小 0x180 字节。

### 1.2 对象布局：640 点弹道折线缓冲 [实证]

基类构造（0x17129D0）初始化：

- `+0x00 = 0.15f`、`+0x08 = 4.0f`；`+0x0C = 4.0×0.5 = 2.0`、`+0x10 = 0.5`（0.5 = 常量 @0x36356E4）；
- `+0x18..0x20 = (1.0, 1.0, 1.0)`（三轴系数向量）；`+0x24 = 0.15f`；
- `+0x34` = 构造参数；`+0x38..0x40` = std::vector（begin/end/cap），
  **reserve(640) 元素 24 字节 = {pos vec3, vel vec3}**（0x170FB00，容量 640=0x280）——
  预计算弹道折线的容量上限；

派生类附加字段（各 ctor）：Shell `+0x58..0x60`；Lightning `+0x64=0, +0x68=1.0, +0x6C=−5.0, +0x70=+5.0`；
Railgun `+0x60=参数, +0x64=1.0, +0x68=0`。

### 1.3 数据流：method29 → ShowTracer → 策略 [实证]

```
AvatarGameLogic::showTracer @0x16AAD50（签名 6 参 = ABH0ABEABVVector3@BW@@2ABM：
  shooterId, shotId, rawFlag, launchPoint, launchVelocity, terminalRaw）
  → 打包 0x2C 字节 functor（vtable 0x37168F0）投递
→ TracerManager::ShowTracer @0x16030C0：
  ├─ 射手为作者自己（0x15E9B30 查到）→ thunk 0x171A310：
  │    jmp tracer->field_0x84->vtable[1] = 策略 input(launchPoint, launchVelocity)
  │    【共享槽1 @0x17187B0 = 把 (pos,vel) 24B 样本压入折线缓冲（环形，掩码 [esi+0xC]）】
  └─ 其他车辆开火 → 0x15E3A90 独立路径（另一套表现）
```

### 1.4 折线上的可视推进与固定步长模拟 [实证]

- Shell 槽[2] @0x1722E40 = **沿折线推进**：`距离 = dt × 当前速度模`（`[ebp+8]×[esi+0x24]`），
  超过与下一折线点的距离即吸附到该点并取其速度模为新的速度（`[esi+0x24]`），
  折线耗尽后清空计数并置完成标志 `[esi+0x44]=0`——**示踪线 = 沿预计算折线的匀速可视化**；
- 时间累积器函数 @0x1718D40：`acc += dt`，`acc ≥ 0.03`（常量 @0x363A524）才步进——
  **弹道模拟固定 ~30Hz 步长**，附带 ±80 条 vec3 缓冲管理；
- 弹道积分数学 @0x1718850（被 0x1718D40 调用）：含 **Quake 快速平方根倒数
  （magic 0x5F3759DF + 一次牛顿迭代，常数 0.5@0x36356E4/1.5@0x36356E8）**、
  速度阻尼系数 0.35（@0x3638988）与 70.0（@0x3632A80，速度归一基准）、
  以及"目标点−当前点"的水平距离归一——阻力+指向目标的混合积分，
  直线弹与制导弹（ATGM 修正）共用此骨架；
- 无重力常量（游戏常量区无 −9.8/−9.81 f32；9.81@0x3645540 为配置解析区）。
  与项目实测一致（launchVelocity 方向与炮口→终点连线夹角 0.03~0.04°）：
  **客户端示踪线为直线推进 + 速度阻尼可视衰减，无可感重力弧线**；ATGM 由
  GuidedMovementStrategy 覆盖推进逻辑实现转弯（hitpoint 无关）；
- 表现参数插值常量：105/420（@0x3722544/48）与 5/20（@0x363C640/0x363CAC0），
  由 `[tracer+0x30]` 参数在两档间线性插值（示踪线长度/寿命档位，具体含义未定名）。

---

## 二、命中位置：DecodeShotSegment 位域数学【完整解出】[实证]

### 2.1 完整公式（内层 @0x1334E30，逐指令核对）

输入 segment u64（method8 元素 8B：`[result b0][cmpIndex b1][hash6 = b2..b7]`）：

```
cmpIndex = b1                        （b1 ≥ 4 触发断言 "cmpIndexInt < 4" @0x36E7F10）
box     = partBlock[cmpIndex]        （min = block+0x00 3f，max = block+0x0C 3f，部件局部系）
P1 = min + (max−min) × (b2, b4, b3)/255     （轴序：x←b2，y←b4，z←b3 —— 交错！）
P2 = min + (max−min) × (b5, b7, b6)/255     （轴序：x←b5，y←b7，z←b6）
输出两个部件局部点（int→float 转换 @0x2529660，除以 255.0f @0x364B110）
```

- **6 个量化字节 = 两个点**（入点/出点语义），每个点 3 轴各 1 字节、在部件 AABB 内线性插值；
- 字节→轴映射为**交错序**（x,z,y），注意这作用于**游戏内存中的部件块轴序**
  （见 2.3 与 itemDefs JSON 轴序的补偿关系）；
- `out1 == out2` 时走单点分支（showDamageFromShot @0x16A9380 与 DecodeHitMarkSegment
  @0x13381EC 都先做 3 dword 相等比较）——单点 = 点射终止（未穿/跳弹），双点 = 贯穿段。

### 2.2 showDamageFromShot 消费链（@0x16A9130）[实证]

```
args = (this, …, elements 指针 esi, …)
元素数 = (esi[1] − esi[0]) >> 3                    ；8 字节/元素
逐元素:
  result = elem.b0
  若车辆已毁/无血（vtable+4 对象 +0xBA 字节=0 或 +0xB8 word≤0）
      且 result == 4（跳弹）→ result 改写为 2     ；@0x16A9271 cmp esi,4; cmove esi,2
      【更正旧文档：被重映射的是 result 字段，非 cmpIndex】
  bboxes[4] = 车辆模型对象(this+0x74) + {0xA8, 0xC0, 0xD8, 0xF0}   ；4 个连续 24B 部件块
  partMatrixes[4] = 0x779480 / 0x779950 / 0x77A0C0 / 0x779910 四个调用返回的模型节点矩阵
  DecodeShotSegment(elem u64, &out1, &out2, …, bboxes, matrices)   ；@0x1335363 调用
  out1/out2 经 3x4 矩阵（旋转+平移，@0x16A93A0 起的 SSE 块）变换——
  入点→出点方向决定受击特效/贴花的朝向
```

### 2.3 与 itemDefs JSON 轴序的补偿关系【探针对照证实】

反汇编显示 b3/b4（z、y 字节）与直觉轴序"互换"，但项目现有探针
`hitpoint_probe.rs`（直序 x=b2,y=b3,z=b4）实测 J39 84 发反推点距射线 median 1.87 m
（随机基线 17.3 m），而按字面交错序重测（临时变体探针）劣化到 15.4 m。

**两者自洽的解释**：游戏内存部件块的轴序为 `{右, 高, 前}`（DAVA/BW y-up 惯例），
itemDefs JSON 的轴序为 `{右, 前, 高}`——反汇编的字节交换恰好被块轴序差补偿：

| 量化字节 | 游戏块内轴 | 游戏块轴序 | itemDefs JSON 轴 | 直序探针映射 |
|---|---|---|---|---|
| b2/b5 | x | 右 | [0] 右 | ✓ |
| b3/b6 | z（块内第3） | 前 | [1] 前 | ✓（经块轴序换算） |
| b4/b7 | y（块内第2） | 高 | [2] 高 | ✓（经块轴序换算） |

即：**现有探针的字节序是正确的**；反汇编给出的"交错"是游戏块内 {右,高,前} 存储序的
忠实反映。残余 1.87 m 偏差来自数据源近似（itemDefs bbox 轴对齐盒 ≠ 客户端真实部件
AABB + 部件原点/座高近似），不再是解码问题。

### 2.4 hash6 的双重语义【实证合并】

- **客户端语义（二进制）**：hash6 就是被 DecodeShotSegment 解码的两个 AABB 量化点——
  method8 元素 u64 原样传入解码器，无任何转换。客户端弹孔/受击特效位置 = 这两个点
  经部件矩阵变换后的世界位置，**不是 raycast**（修正旧结论"弹孔位置由客户端
  raycast 得出"——车辆弹孔=弹道线求交，raycast 属环境弹孔/装甲高亮路径）。
- **服务器编码语义（与 type=32 的 86/86 字节一致 + yaw/pitch 公式检验）**：与
  [shell u16][来向 yaw u16][抵达 pitch u16] 解释并存。两种解读并不矛盾：
  服务器把方向类信息打包进 segment，客户端按 AABB 量化点解码后落在部件盒内的
  合理位置——弹孔位置的"近似性"是协议设计使然（每客户端一致、服务器免发几何）；
- 对项目的可操作结论：**`server_part_index` + hash6 按 2.1 公式解码（探针现有直序）
  即与游戏客户端完全同构的弹着点**；欲收敛到厘米级需替换数据源为
  `3d/Tanks/CollisionMeshes/*.scg.dvpl` 的部件 AABB（格式已解）+ 部件矩阵的
  精确原点/座高，替代 itemDefs 轴对齐盒。

> **⚠️ 2026-09 四轮定论（推倒"坐标复现"路线）**：对 4 回放 318 个 hash6 的字节分布
> 检验**证伪了 method8 hash6 的坐标语义**——b2 有 40%（126/318）落在 0x00/0xff、
> b4 有 26% 为 0xff（真实坐标不可能 40% 精确压在盒边缘），且全部 hash6 组合唯一。
> hash6 = **[弹种 u16][来向 yaw u16][抵达 pitch u16]（方向/弹种编码）**。二进制事实
> 不变：游戏客户端确实机械地把这些字节经 DecodeShotSegment 当 AABB 点解码放置受击
> 特效（位置近似/失真，玩家不察觉）；**精确弹孔位置由 hitMarks 属性
> （HitMarkPlacementInfo protobuf，仅发受击者本人客户端）的 segment 携带**，回放中
> 对任意受击者不可得。此前"1.87m 全局最近射线"指标无法区分盒内随机点与真实位置
> （命中射线本就穿过目标盒），判据无效。
>
> **⚠️ 2026-09 五轮综合（客户端代码链补全后的最终结论，撤销上段"仅以文本展示"）**：
> 完整反汇编 `showDamageFromShot`（0x16A9130；wire 入口 showDamageFromShotBase，
> `BW::SequenceValueType<uint64,16>` = 最多 16 段）确认客户端的处理链：
> ①逐元素 result/cmpIndex（死亡时 result 4→2 改写）；②DecodeShotSegment AABB 解码；
> ③部件矩阵变换 + out1/out2 入/出点；④特效位置合成（归一化方向符号翻转
> @0x16A99A3-0x16A99DD、GetComponent(0x456B898) 按 result 取系数 @+0x50、
> 累加器 0x757B70）——**全函数无角度解码常量、无 Bullet 射线调用**，即客户端
> 没有任何隐藏的精确位置数据，游戏内弹孔就放在这个近似解码点上（AddHitMarks
> 延迟链 → HitMarksController::SetNewHitMarks → TankHitMarkComponent →
> DAVA DeferredDecal；精确位置仅经 hitMarks 属性发给受击者本人客户端）。
> **因此橙色 ◆（AABB 解码点）= 与游戏内弹孔同位的忠实复现**，恢复显示并随受击方
> tick/滑块位姿重放；红色 ●（弹道射线 × 部件约束）= 真实命中的最佳估计；
> 二者偏差真实存在于游戏本体，非复现错误。
>
> **可证伪预测（J39 shot#5，hash6=ff6dc5ef6fba，部件1 车体，result=2）**：b2=0xFF
> → 客户端弹孔必然画在车体盒**右缘极值**（x=+2.14）、高 +1.94m（b4=197）、前 −0.87m
> ——即车体右侧上部偏后，而弹道入射点在另一侧（水平差 ~2.6m）。在游戏客户端播放
> 该回放至 t≈50.6s 即可裁决：弹孔在右侧上部 → 机械解码结论成立；弹孔在弹道入射处
> → 客户端存在未发现的精化步骤（需继续逆向，重点检查 0x6DB140 发布事件的消费方）。
>
> **⚠️ 2026-09 六轮（玩家实测 + 追加逆向的最终状态）**：玩家确认游戏内弹孔位置准确
> （与弹道入射一致）——与"机械解码"预测矛盾，说明可见弹孔位置并非（仅仅是）segment
> 字节的解码点。追加逆向：`OnExplodeProjectile`（0x15F1700，148 个调用）无直接
> Bullet 射线调用；按 result 分派的 4 类事件（0x456ADA4/B1F4/AE44/B22C）经
> `HitExtraEffectEvent`/`ExtraEffectsManager`/`VehicleExtraEffectHandler`
> （RTTI 实证）消费——消费链为虚调用，静态不可达。**最可信模型**：客户端用
> showTracer 广播的弹道线（全客户端可见）与坦克模型求交得弹孔位置（= 本项目
> 红点语义），segment 字节用于特效种类/朝向。项目侧：红点即准确弹孔标记；
> 橙标保留为 segment 解码可视化（调试语义）。
> 【2026-09-23 移除】：橙◆"客户端弹孔复现"（viewer 标记 + web `decal_hit` +
> combat.rs `decode_hit_mark_points`/`compute_decal_hit_world`/`DecalPartFrame`
> 解码链）整体删除——hash6 坐标语义不实（独立复验：b0 35% 压 0x00/0xff 端点；
> 解码点 vs 弹道×部件盒交点中位差 2.3m/最大 8m），误导性大于调试价值。
> 弹着点统一以弹道射线 × 部件约束（调试红点）为准。
>
> **渲染层语义切换（同轮落地）**：viewer 世界模式默认选中"◎渲染位"采样
> （滤波器命中帧输出 = 玩家所见位姿），弹着点射线在该位姿下求交——与游戏
> 客户端弹孔的位姿基准一致；判定层锚点保留为回退（渲染采样缺失时）与对照
> （可切回判定层 tick 对照两种语义的偏差）。滑块插值域同时剔除了命中后
> （dt>0）的兜底采样——数据包源切换致瞬移/朝向跳变（96 段实测 40 段反转），
> 混入滑块末端 keyframe 会产生模型翻转行为；修复后 91 发滑块末端 yaw 跳变 = 0。
>
> **炮塔/炮管实时接入（同轮落地）**：ShotReplayData 新增三条时间线——
> （炮塔相对角时间线为**镜像不变量**：车体角与炮塔世界角在镜像下同时取反，相对角不变——
> 初版误加镜像取反导致炮管方向反转 180°，已修正为直接透传。）
> `target_turret_timeline`（受击方炮塔相对角，prop2，命中 −3~+2s）、
> `shooter_turret_timeline`（射手炮塔相对角，prop2，开火 −2~+2s）、
> `shooter_gun_timeline`（射手炮管俯仰，prop9，仅作者数据流）。viewer 滑块拖动时：
> 受击方炮塔节点按插值相对角实时转动（updateTurretGun），射手炮塔/炮管经
> poseShooterTurretGun 按 prop2/prop9 时间线驱动。实测炮塔世界朝向随滑块连续
> 变化（T110 shot4：−175.5°→−176.3°）且模型位姿联动正常。
>
> **滤波时间线严格对齐（同轮落地，替代原始采样线性插值）**：ShotReplayData 新增
> `target_render_timeline`（命中 −1.0~+0.2s）与 `shooter_render_timeline`（开火
> ±0.2s），0.05s 步长的滤波器输出序列（filter.rs FilteredTimeline 降采样，pos 相对
> 锚点、roll=0）；viewer 滑块插值域优先使用该时间线——扫出的就是游戏每帧实际显示
> 的位姿序列（含 latency 移位/误差盒钳位/外推）。原始采样线性插值保留为回退。
> 验证：时间线 dt=0 与 ◎渲染位采样零差；滑块末端段前向向量恒定无翻转。
>
> **窗口扩展（同轮）**：受击方时间线扩至命中 **−3.0~+2.0s**、射手方开火 −2.0~+2.0s
> （0.1s 步长，51/41 帧），滑块行程同步扩展并纳入时间线 dt。命中后数据保留——滤波器
> 误差盒钳位会渐进滑向通道切换后的真相，即游戏当时渲染的画面。实测 J39 shot#5：
> 5 秒窗口完整呈现受击车折返机动（x 88→70.7→88，含命中后 +0.75~1.0s 处通道切换
> 的快速转向——滤波滑移，与游戏渲染一致）。

### 2.5 兄弟函数 DecodeHitMarkSegment（hitMarks 属性）[实证]

`SBWUtils::DecodeHitMarkSegment`（断言 "outHitMarkSegment < 4"/"Incorrect hitMarks segment"，
外层 @0x1337C40 内层同 0x1334E30）：

- 输入 = Vehicle 属性 `hitMarks` 的 HitMarkPlacementInfo protobuf 中的 segment u64
  （`[ecx+0]/[ecx+4]` 双字）+ 4 个部件盒指针（栈上构建 bboxes[4]）；
- 同样解出 P1/P2 两点，`out1 == out2` 单点分支；
- 调用者 @0x133E750（0x1A8 字节工作区，0x17F09F0 取模型资源）→ 贴花放置
  （TankHitMarkComponent → DAVA DeferredDecal）。
- 意义：**`hitMarks` 属性（受击者客户端收到的弹孔描述符）与 method8 元素共用同一
  segment 解码数学**——修复了旧文档"hitMarks 属性未解码"的缺口：其核心字段
  segment 的解码即 2.1 公式，其余 protobuf 字段为贴花样式/寿命等表现参数。

---

## 三、射击事件处理链总览（包 → 表现）[实证]

```
method29 (0x1d) showTracer ──→ TracerManager::ShowTracer
                                 ├─ 作者自己 → MovementStrategy（640点折线 + 30Hz 步进推进）
                                 └─ 他人     → 0x15E3A90 直接表现路径
method20      explodeProjectile → TracerManager::OnExplodeProjectile（爆炸标记/特效）
0x1b          地形命中          → 同上 + EnvironmentHitMarksController（材质贴花，raycast）
method38      showShotResults   → PrivateShotResult::initFromStream → 结果反馈 UI/音效
method8       showDamageFromShot→ 逐元素 DecodeShotSegment：
                                  result（死亡时 4→2 改写）+ cmpIndex 部件 + hash6→两点
                                  → 部件矩阵变换 → 受击特效/贴花（位置 = 解码点，非 raycast）
hitMarks 属性 set_hitMarks     → HitMarkPlacementInfo → DecodeHitMarkSegment → 贴花（同数学）
type=32        showOwnVehicleHitDirection → 来袭方向指示
```

**结论**：客户端命中表现的每一环都有确定性数据来源；"精确命中位置"在协议中只存在
AABB 量化近似（每客户端一致），不存在明文几何——与服务器 hitscan 判定（§6.1 引擎实证）
互补：判定精确但几何不广播，表现广播但量化到部件盒 1/255。

---

## 四、回放文件读取与播放链（详证 tmp_analysis/re_agent1/report.md）[实证]

- **文件结构**：`.wotbreplay` = zip 或同名目录（`ReplayZipBridge`/`ReplayDirectoryBridge`，
  vtable 0x36EADC0/0x36EADA0），内含 `meta.json`、`data.wotreplay`、`battle_results.dat`；
  data 段 magic `0x12343210`（旧）/`0x12345678`（新，多 42B 子头，检查 @0x13C6280，
  解压上限 30MB）；其后为记录流，**每条 12 字节头 `{u32 size; u32 type; float time}`+payload**
  （索引构建 @0x13D95A0：16 字节索引项 {size,type,time,文件偏移} 存 ReplayFile+0x50 vector）；
- **读取**：`readData` @0x13C3610 整体载入/流式两路；`LoadMeta`/`LoadData`
  （0x189EAA0/0x189ED80）JSON StrictParse meta；
- **播放时钟**：`StartPlayback` @0x13CA980 以索引中 type=17（ON_CLIENT_READY）时间戳为
  起点；每帧 `Update` @0x13CE660 `dt ×= 倍速`（replayTimeWarp），double 主时钟累加
  （0x276F560），到期记录派发 @0x275D4C0；REWIND 为状态机模式 + GES 事件；
- **分发**：`ReplayMessageHandler`（0x28B 对象，函数表 @0x36E8078 按 type×4 索引）——
  `onEntityMethod` @0x134AF90 先把参数流送 BW 连接侧 @0x2759B40（与 PacketReceiver 同源）
  再转发二级 handler @0x13BD7A0：**回放与实战共用同一 Bundle 解码管线（二进制证实）**；
- **完整性**：CRC32(zlib)——DVPL footer 20 字节 {uncSize,compSize,crc32,type,'DVPL'}，
  实测与 zlib.crc32 一致；"Ignore Invalid Replay Digest" 选项 = PlayerOptions+0x52C，
  把 LoadError=8（DATA_INCORRECT_DIGEST）折叠为 0（@0x13CFF60）；
- **录制**：StartRecording/StopRecording/AddBattleResults（0x13CAD40/0x13CB400/0x13A7880），
  异步写线程 @0x13D8BA0，临时目录 `recording_*`，完成后 SaveDirectoryAsArchive 打包。

## 五、坦克位置渲染链（详证 tmp_analysis/re_agent2/report.md）[实证]

- **每帧驱动**：@0x13C6FC0 取回放主时钟 double t（与播放时钟同源！），对每个车辆实体
  依次 `getLastInput → markPending → input(回灌最新样本) → output(t, target)`——
  **滤波器状态机与输出共用同一时间基**；
- **滤波输出**：`output()` @0x276E980（WG 覆盖 0x1442A20 → 基类 0x276E0C0 → 采样器
  0x276D8C0，即逆向分析文档 §7 的 latency 状态机）经 MovementFilterTarget 回调 4 出参：
  p1/p2 = f32（yaw/pitch 类），p3 = Vector3 位置，p5 = Vector3（速度类）；
- **搬运**：`VehicleFilterSyncSystem`（Process @0x17292E0）**不调 output()，只搬运**：
  滤波 helper 的角速度三元组 → 变换组件 +0x10 起三个弧度角（0x275A920 节点链求和），
  炮塔角 helper+0x354/0x358 → TankVisualTransformComponent，helper+0x500 矩阵 → comp6；
- **旋转换算**：`VehicleTransformSystem::Process` @0x83F010：读三个**弧度角**，
  各乘 0.5（半角），绕 −Y/−X/−Z 轴（.data 0x4034060/6C/78 处轴向量的符号位翻转）构造
  3 个四元数，相乘归一化写 comp+0x34（x,y,z,w）——**车体朝向 = yaw/pitch/roll 欧拉
  四元数积，绕轴取负**（本轮已抽查反汇编证实）；
- **炮塔链**：WGVehicleFilterHelper 内部 @0x1440C70 对 **(yaw,pitch,t) 关键帧时间线**
  （helper+0x4E0 vector，16B/帧，按回放倍速缩放）插值 → TVTC(+0x10/+0x14) →
  `TankVisualUtils::SetTurretAngle` @0x840A30（角度归一化 (−π,π]、绕 Z 四元数角=−yaw/2、
  位置混合）写炮塔节点；`SetGunAngle` @0x8406A0 作用炮管。网络侧 u16 满量程 0x10000
  （OnReadTurret ~0x7E24D8）；平滑 = 时间线插值 + VehicleGunRotator 转速限制，
  **无逐帧滤波**；
- **击飞炮塔**：DetachedTurretFilterSyncSystem（Process @0x1728880 → ProcessEntity
  @0x17294D0）独立滤波同步 + 附带弹道积分 0x17288B0。

---

## 六、对本项目的落地建议

> **2026-09 落地状态：1a/4 已实现（含一轮偏差审计修复），1b 数据源升级、1c 全姿态
> 修正与 2 改写规则待做。**
> 实现链：combat.rs `decode_hit_token_hex` + `compute_decal_hit_world`（`DecalPartFrame`
> 节点层级几何 + 单测）→ web 层 `load_decal_geoms`（game_data 部件盒 + 节点几何缓存）→
> shots JSON `decal_hit` `{part, entry[3], exit[3], same}` → viewer 世界模式橙色 ◆（入点）/
> ○（出点，`same` 时不画）+ 调试行"游戏弹孔(部件)" + 面板行 + **弹孔随受击方 tick 位姿
> 重放（锚点局部系，"贴"在坦克上）**。
>
> **二轮偏差审计（2026-09）修复的三处变换 bug**：
> 1. 炮塔/炮管件原先绕车体中心旋转 → 改为绕**座圈**（ring = −turret_points 水平分量）。
>    座圈偏移大的坦克（grille-15 1.45m、type-71 0.81m、kranvagn 0.69m）此前随炮塔转角
>    差产生最高 2 倍偏移的误差；
> 2. 炮管节点偏移 gun_origin 被完全忽略 → 补上（= −gun_points − ring，turret-relative，
>    J39 达 1.73m 前向 + 0.52m 高）；
> 3. 车体件未加 hull_position（157/684 辆非零，最大前向 ~1m）→ 补上。
>    约定实证：**部件节点零位姿位置 = −collision.points**，与 models.pb model_origins
>    交叉验证 6/7 辆严格相等（Ho-Ri 为多炮塔变体差异）；bbox = 节点局部系 {右,前,上}。
>
> **三轮修复（用户实测 shot#5 反馈）**：viewer 跟随代码把 rel 的 [x, 高度, z] 分量序
> 误按 [x, z, 高度] 解释（高度/z 调换），模型加载回调自动触发 applyWorldTick 时立即用
> 0.147m 的 z 偏移当高度重摆标记 → 弹孔贴地；同时补上时间滑块路径缺失的跟随（该路径
> 此前完全绕过 applyWorldTick）。修复后 shot#5 弹孔 = 锚点 +1.054m（车体侧面 55% 高度），
> 与红点垂直差 0.09m、与弹道通过高度（~0.96m）互证；tick 下拉与连续滑块两条路径跟随
> 均正确（GB109 目标 1s 内移动 10.5m 的快车场景验证）。
> 端到端验证（`examples/decal_check.rs`，与生产公式逐行同构）：修复后 J39 med 1.76m
> （持平——J39 座圈偏移仅 7cm，符合预期）/ J20 1.36 / GB109 0.75 / XM551 5.03；
> 43 单测全过（新增座圈旋转/炮管节点用例）；浏览器复验：炮塔弹孔与本地 raycast 弹着点
> 相距 ~1.5m（shot#6 双链路交叉印证）、换 tick 弹孔跟随模型、无 JS 错误。
> 指标注释：批配对射线 med 受延迟链/多弹同飞影响选不中真实命中弹，全局最近射线才是
> 量化语义的合理指标；XM551 回放的 20–38m 极端样本属 AoI 稀疏目标的锚点问题
> （其基线同为 300m+ 级），非弹孔数学问题。

1. **弹着点收敛路线**（按收益排序）：
   a. ✅ 已实现：2.1 公式（`compute_decal_hit_world`）+ `server_part_index` 部件盒 +
      炮塔件 prop2 绝对角叠加 + 座高（turret_points/gun_points |z|）——与游戏客户端
      弹孔同构，作为"游戏看到的命中点"基准（JSON `decal_hit` 字段）；
   b. 数据源升级：从 `CollisionMeshes/*.scg.dvpl` 提取 4 部件 AABB（替换 item_defs
      轴对齐盒），并按坦克型号建立 {右,高,前}→JSON 轴映射表——预期把 ~1m 级中位
      残差压到分米级；
   c. 全姿态修正：现行 yaw-only（探针验证过的离散姿态近似），坡地交战可加
      type10 pitch/roll 完整旋转（参照 §五 VehicleTransformSystem 的 −Y/−X/−Z 轴序）；
2. **method8 result 改写规则（待实现）**：受害者死亡/零血时 result 4（跳弹）→2
   （showDamageFromShot @0x16A9271 实证）——需受击者命中时刻存活状态上下文，
   combat.rs 血量链可提供；量级小（死亡车辆身上的跳弹表现差异），优先级低；
3. **示踪线**：维持直线弹道（与游戏一致）；如需动画级复现，按 30Hz 固定步长 +
   匀速推进 + 0.35 速度阻尼系数即可；
4. ✅ **out1==out2 判据**已实现（`decal_hit.same`，JSON 透传；样本回放中暂未观测到
   单点分支，游戏语义为点终止——未穿/跳弹的可视信号）。

## 七、证据索引

| 结论 | 证据 |
|---|---|
| 6 策略类 vtable | RTTI TD @0x431A3C8/0x439AD74/0x439AD98/0x439ADC0/0x439ADE8/0x439AE10 → COL → vtable |
| 继承关系 | 4 处 ctor 双 vtable 写入（0x17113AB/0x17117D0/0x1712832 + 0x17110ED） |
| 基类布局 | ctor @0x17129D0（reserve(640)×24B @0x170FB00 调用） |
| 共享 input 槽 | thunk @0x171A310 → vtable[1]=0x17187B0（压栈 24B 样本） |
| 折线推进 | Shell 槽[2] @0x1722E40（dt×速度模，吸附，+0x44 完成标志） |
| 30Hz 步长 | @0x1718D61 comiss 0.03（@0x363A524） |
| 快速 rsqrt | @0x1718875 mov eax,0x5F3759DF + 牛顿迭代（0.5/1.5） |
| 阻尼/基准 | 0.35 @0x3638988、70.0 @0x3632A80 |
| DecodeShotSegment 数学 | 内层 @0x1334E30 逐指令；int→float @0x2529660；255.0f @0x364B110 |
| 断言串 | "cmpIndexInt < 4"@0x36E7F10、"bboxes[cmpIndexInt]"@0x36E7F50、BWUtils.cpp@0x36E7EA4 |
| showDamageFromShot 链 | @0x16A9130；元素 stride 8（>>3）；result 4→2 @0x16A9271；bboxes=模型+{0xA8,0xC0,0xD8,0xF0}；矩阵=0x779480/0x779950/0x77A0C0/0x779910 |
| DecodeHitMarkSegment | @0x1337C40（断言 0x3F "outHitMarkSegment < 4"@0x36E7E8C）；调用者 @0x133E750 |
| 轴序补偿验证 | hitpoint_probe（直序）J39 med 1.87 m vs 交错序变体 15.4 m |
| 无重力常量 | 常量区 f32 扫描：无 −9.8/−9.81 |
